dist
node_modules
coverage

# npm writes this file, not a human. Gating `format:check` on another tool's serialization
# means an npm version bump can red the gate on a commit that never touched it.
package-lock.json

# Generated by c2-3 from the backend's own OpenAPI schema — formatting it here would fight
# the generator and break that story's `git diff --exit-code` drift check. Does not exist yet.
src/api/types.d.ts
