docs/_sathias.txt
temp/

# Claude Code — track shared skills/config, keep machine-local settings out
.claude/settings.local.json

# Codex — repo-scoped personal config (the .codex analog of settings.local.json)
/.codex/

# Dev process tooling — untracked via `git rm --cached` but kept ON DISK locally so the bmad
# workflow still runs. NOTE: _bmad-output/ is intentionally NOT ignored — its planning artifacts
# (PRD, architecture, UX, research, specs) are the public design record. Implementation artifacts
# (stories, sprint status, retros, deferred-work ledger) live on the orphan `process` branch,
# checked out locally as a worktree under .worktrees/ (ignored below) so the bmad skills still
# resolve them.
/_bmad/
/.worktrees/
.claude/skills/bmad-*/
# BMAD installer dump for Codex-style agents — dev tooling, not shipped. Scoped to skills/ only:
# .agents/plugins/ (the Codex marketplace file) IS committed, so never blanket-ignore .agents/.
.agents/skills/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# Anchored to the repo root: an unanchored `lib/` would silently ignore any future `ui/src/lib/`
# (ui/.gitignore already covers ui/dist and ui/node_modules).
/build/
develop-eggs/
/dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Node (a root-level vitest cache was once committed by accident; ui/ has its own rule)
node_modules/

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/

# Type checking and linting
.mypy_cache/
.ruff_cache/
.pytype/
.dmypy.json
dmypy.json

# Environment variables
.env
.env.local

# Database (incl. WAL-mode sidecars, which *.db/*.sqlite don't match)
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal
*.sqlite-shm
*.sqlite-wal

# OS cruft
.DS_Store
Thumbs.db

# Chainlit
.chainlit/
chainlit.md

# Data files (top-level runtime data dir only; anchored so it does NOT match src/data/)
/data/
/temp/

# Debugging scripts
scripts/check_*.py
.files/

# Claude Code plugin: the assembled tree at /plugin/ IS committed on purpose — it is the
# marketplace `source` (.claude-plugin/marketplace.json), so `/plugin marketplace add` clones a
# repo that already contains the built plugin. Do NOT ignore it. Its runtime cruft (.venv/,
# __pycache__/, *.egg-info/) is already covered by the rules above. Rebuild with
# `uv run python -m scripts.build_plugin` and commit the result. (`dist/` stays for scratch builds.)

