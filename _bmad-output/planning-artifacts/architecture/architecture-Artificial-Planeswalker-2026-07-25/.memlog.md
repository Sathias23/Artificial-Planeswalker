---
scope: Companion app (PRD 2026-07-22 + addendum 2026-07-25, UX spine 2026-07-25): FastAPI companion backend + Vite/React SPA + new companion MCP tools, layered onto the existing MCP-server codebase. Phases 1-3, FR-01..FR-23, NFR-01..NFR-09.
purpose: build-substrate
altitude: feature
updated: 2026-07-25T12:54
---

- (constraint) Brownfield layering is binding: strict import direction data -> logic -> (mcp_server | ui); src/data + src/logic stay framework-free; repositories return Pydantic schemas, NEVER ORM models. Source: _bmad-output/project-context.md.
- (constraint) MCP server is stateless per PRD CM-3 and project-context D5: no per-session server state; deck_id/format are tool params. The companion BACKEND may legitimately hold ephemeral display state (active deck, connections, tickets).
- (constraint) Existing tooling floor (NFR-07): ruff line-length 100 py312, mypy --strict on src/, pytest asyncio_mode=auto, pre-commit, uv. No Alembic - hand-written scripts/migrate_*.py. Google docstrings, module docstrings required, logging with %-style lazy args.
- (constraint) Node is dev/CI-only (NFR-07, SC-4): built SPA ships as package data inside the Python wheel; fresh install needs no Node toolchain. CI builds the SPA and drift-checks committed output, mirroring the existing plugin/ tree pattern.
- (constraint) UX spine (EXPERIENCE.md/DESIGN.md, approved 2026-07-25) is a binding input, not a suggestion: zustand slices activeDeck/agentViews/inspection/connectionStatus fed by exactly two inputs (REST responses + WebSocket messages); full-window modal agent views; persistent non-modal card detail panel; self-hosted font, no CDN.
- (constraint) Read-only glass premise (PRD NG1 + UX Foundation): the SPA issues no writes. Every state mutation flows through MCP tools. This is the hardest boundary in the feature.
- (question) OPEN (PRD OQ-A): payload schemas for suggestions/swaps/tier-list/groups - fields, optionality, max sizes, empty-payload semantics, ID-validation locus. FR-23 groups extend beyond OQ-A's parked constraints (title + prose rationale + cards outside the active deck).
- (question) OPEN (PRD OQ-B): TS type-generation tooling - datamodel-code-gen vs json-schema-to-typescript vs manual+drift-test. CI drift-check required regardless (NFR-03).
- (decision) Companion backend is a SECOND IMPERATIVE SHELL over the shared core: src/companion/ is a peer of src/mcp_server/, both sitting on the same src/data + src/logic. Binds: NFR-02, NFR-03, FR-02/03. Prevents: a second card/deck truth drifting from the repositories. Rejected: independent read service (duplicate card shape), backend-as-MCP-client (process-lifetime mismatch - MCP is per-session, backend is long-running).
- (decision) The companion app SUPERSEDES src/viewer + view_deck. Brad 2026-07-25: one visual surface, one truth. view_deck is deprecated once the companion ships; src/viewer moves toward removal. Binds: G1, FR-05, FR-17, FR-19. Prevents: two renderers of one entity diverging. NOTE: this is a scope expansion beyond the PRD (the PRD is silent on view_deck) and touches a shipped public tool at v0.4.0 - deprecation timing/mechanics still open.
- (decision) Discovery file lives at src.paths.data_dir()/companion.json (platformdirs: %LOCALAPPDATA%/artificial-planeswalker on Windows), NOT the PRD's ~/.artificial-planeswalker/. Binds: FR-01, FR-14, NFR-09. Prevents: product data split across two locations; loss of PLANESWALKER_DATA_DIR test/dev isolation. Consequence: image cache (NFR-09) also homes under data_dir(). REQUIRES a PRD amendment - FR-14 and the glossary name the ~ path.
- (direction) Brad chose 'same engine, read-only by discipline' over mode=ro, contra NFR-02 which states file:...?mode=ro as a requirement. Pushback raised: discipline alone leaves the single-writer invariant (CM-3) as a habit a future story can break silently. Counter-proposal pending: keep one engine (avoids the WAL/-shm landmine the addendum flagged) but move the guarantee to a CI-enforced import boundary rather than convention.
- (decision) Single-writer invariant is enforced by a CI import boundary, NOT mode=ro. One engine (avoids the WAL/-shm landmine flagged in the addendum's delegated checklist). A test AST-walks src/companion/** and fails on any repository write method, session.add or session.commit. Binds: NFR-02, CM-3, NG1. Prevents: a Phase-3 UI-edit story finding the write path already open; the read-only-glass premise decaying into a review convention. REQUIRES PRD amendment - NFR-02 states file:...?mode=ro as the mechanism.
- (decision) view_deck deprecation schedule: marked deprecated at Phase 1 (docstring names the companion as replacement) but keeps rendering HTML through Phases 1-2; src/viewer removed at the next minor once the companion is proven. Prevents: breaking a shipped v0.4.0 public tool mid-flight and violating SC-3 (all agent workflows complete with the companion closed) during the transition.
- (decision) ONE WebSocket envelope, discriminated by kind: {kind, id, ts, payload}. kind covers agent pushes (suggestions|swaps|tier_list|groups) AND system signals (deck_changed) alike. Binds: FR-06, FR-08/09/10/23, FR-11, FR-18, NFR-03. Prevents: two builders shipping {type,data} and {kind,payload,ts}; FR-18 history having no common key. NOTE the distinction from OQ-2: OQ-2 rejected a generic companion_display TOOL (agent-facing affordance); it does not constrain the WIRE shape. Separate tools + one envelope is consistent with both.
- (decision) src/companion/ splits into a dependency-free LEAF (contracts.py, discovery.py, client.py - pydantic + httpx only) and app/ (FastAPI). CI-checked import rule: src/mcp_server/** may import src.companion.{contracts,discovery,client} but NEVER src.companion.app.*. Binds: FR-06, FR-08/09/10/12/23, FR-14, NFR-03, NFR-07. Prevents: a stdio MCP session transitively importing FastAPI/uvicorn just to read a port; and the two peer shells growing a mutual dependency. Same CI test file as the write boundary.
- (decision) OQ-B RESOLVED: openapi-typescript over FastAPI's own app.openapi() output, into a committed .d.ts, drift-checked with git diff --exit-code (mirrors the existing plugin/ tree CI pattern). ONE generator covers REST responses AND the WS envelope, because POST /agent/events declares the envelope union as its request body so it lands in OpenAPI components - no second tool, no dummy endpoint. Binds: NFR-03, NFR-07, the schema-drift risk row.
- (version) openapi-typescript verified current 2026-07-25 (npm, last published Feb 2026; requires Node >=20; reads OpenAPI 3.0/3.1). FastAPI emits OpenAPI 3.1 - compatible. CORRECTION to PRD OQ-B: datamodel-code-generator runs the OPPOSITE direction (JSON Schema/OpenAPI -> Python models), so it was never a viable TS-generation candidate. pydantic-to-typescript v2+ is pydantic-v2 compatible and was the runner-up.
- (decision) TWO distinct credentials that never touch: the AGENT TOKEN (discovery file, authorises POST /agent/events) and the WS TICKET (authorises a browser tab to receive). The browser never sees the agent token. Ticket is single-use with a 30s TTL, minted by same-origin Host-validated GET /api/session and destroyed at WS upgrade; every reconnect attempt mints a fresh one. Binds: NFR-01, NFR-04, the addendum's delegated ticket-lifecycle item. Prevents: an XSS or open devtools escalating from 'read the UI' to 'push arbitrary content into the session'; and a replayable long-lived WS credential.
- (decision) Image proxy: LAZY on request, paced at ONE backend-global choke point (semaphore + ~100ms spacing), never trusted to the UI. Cache at data_dir()/image_cache/<id[0:2]>/<id>/<size>_<face>.<ext>, sharded 2 hex chars (a flat dir would reach ~60k entries), written temp+rename. Failure -> placeholder response + negative cache with backoff. Cards with no image_uris get a stable placeholder and are never fetched. NO eviction in MVP: unbounded, documented location + a clear command (NFR-09). Binds: FR-04, NFR-05, NFR-06, NFR-08, NFR-09, CM-2. Prevents: request storms; per-tile hotlinking; two components each implementing their own rate limiter. Accepted consequence: a cold 100-card deck is ~12MB / ~10s to fully paint (NFR-05 excludes first-fetch paint).
- (decision) Built SPA lives at src/companion/app/static/, committed to git and mirrored into plugin/ by the existing rebuild + drift-check machinery. FORCED by the distribution model, not a free choice: the project ships as a Claude Code plugin via a cloned tree, so a build-time-only hook would leave plugin users with no static assets. Binds: SC-4, NFR-07, FR-01. Accepted cost: the bundle is committed in two places; both copies are generated artifacts nobody hand-edits.
- (decision) Test seams: bulk in-process (httpx ASGITransport for routes, the existing in-process MCP client for tools) PLUS exactly one integration-marked real-socket test - real uvicorn on an ephemeral port, real companion.json, real client, real WS upgrade + ticket consume, and a restart-mints-new-token retry case (FR-12). Binds: NFR-07, FR-01, FR-12, FR-14, NFR-01. FORCES a spine rule: build_app() must be constructible with ZERO side effects - the lifespan owns the discovery file, the port and the cache dir, never module import - or the fast path is untestable and FR-22 (start with no DB) cannot hold.
- (decision) Companion MCP tools are async def using httpx.AsyncClient, matching the Epic-1 async tools. Prevents: a blocking POST inside a sync def tool occupying a FastMCP threadpool worker for a whole network round-trip.
- (decision) CLI: the existing console script artificial-planeswalker becomes a subcommand dispatcher - bare/no-args runs the MCP server exactly as today, 'companion' runs the backend. VERIFIED SAFE: both .mcp.json and plugin/.mcp.json invoke 'python -m src.mcp_server' directly, so no MCP client config touches the console script. Matches the copy already written into PRD UJ-1/SC-4 and EXPERIENCE.md Flows 1-2. Binds: SC-4, FR-01. Prevents: a second console script requiring PRD+UX copy amendments in three places.
- (decision) OQ-A RESOLVED (payload contract). Shared primitive is a bare card reference (Scryfall printing UUID only, FR-13) - the UI hydrates everything else via FR-03/FR-04. Every payload carries an optional agent-authored title (view header reads 'Resilience options', not 'Suggestions'). Per-kind item shapes, NOT one fat optional-bag: suggestions {card_id, reason, category?}; swaps {out_card_id, in_card_id, rationale, out_qty, in_qty}; tiers {letter: Literal[S,A,B,C,D], name: str, note?, card_ids[]}; groups {title, rationale, card_ids[]}. Tier letter stays a closed enum so DESIGN's five-colour ramp is total; the free 'name' carries MTG meaning ('Auto-include', 'Filler', 'Cut') - the design's tier chip already renders both. Binds: FR-08/09/10/13/23, NFR-03, CM-1, EXPERIENCE ruling 4.
- (decision) Payload caps enforced by pydantic at the endpoint; over-cap is REJECTED (422), never truncated. Caps: <=60 items or card_ids per list, <=12 groups/tiers, reason <=200 chars, rationale <=600, title <=80, whole envelope <=64KB. FR-12 turns the 422 into a text result so the agent presents in chat and nothing is lost. Binds: CM-1, FR-12, NFR-03. Prevents: a silently truncated push rendering as if it were the complete answer; an unbounded WS broadcast.
- (decision) NO card-ID validation at ingest. The backend shape-validates (pydantic) and relays; it never touches the DB on the push path. Binds: FR-13, SC-1/NFR-05 (250ms), FR-06. Prevents: a DB read on the latency-critical push path, and a whole push failing because one ID is stale. Unknown IDs degrade per entry in the UI exactly as EXPERIENCE.md specifies. Empty payloads are ACCEPTED and render the deliberate empty state (EXPERIENCE.md confirmed ruling 2026-07-22), never rejected.
- (decision) deck_changed emission: ONE shared fire-and-forget notifier in the companion leaf, called by every mutation tool AFTER commit, never inside the transaction, with all exceptions swallowed and logged. Deletion counts as a mutation. Binds: FR-11, NFR-04, the addendum's 'mutation persists but event POST fails' item. Prevents: each mutation tool inventing its own emit path; an emission failure degrading a mutation result that already succeeded. Accepted staleness window until FR-16 (Phase 3).
- (decision) Companion tool outcomes are a CLOSED token enum, mirroring the project's existing status-token convention (AD-6/AD-7 of the deck-power spine): displayed | app_not_running | no_clients_connected | payload_rejected | backend_error. Every companion tool returns a compact text result carrying one token plus the connected-client count; NEVER raises, never echoes the payload (CM-1, <=200 tokens). On auth failure the tool re-reads the discovery file and retries ONCE, so a restarted backend's new token is picked up transparently mid-session. Binds: FR-06, FR-12, CM-1.
- (decision) build_app() has ZERO import-time and construction-time side effects. The FastAPI lifespan owns everything with an external effect: port binding + ephemeral fallback, discovery-file write and removal, image-cache dir creation, DB engine creation. The DB engine is created LAZILY and its absence is a served UI state, not a startup failure - this is what makes FR-22 (start with no database) and the fast ASGI test path both possible. Binds: FR-01, FR-14, FR-22, NFR-07. Prevents: an untestable app object, and a fresh install erroring instead of guiding.
- (decision) Operational envelope: the companion backend is a FOREGROUND, user-launched, single-instance local process. It logs to stdout/stderr freely (unlike the MCP server, where stdout is the JSON-RPC stream and diagnostics must go to stderr). Single-instance is enforced at startup via the discovery file + GET /health instance_id echo: live instance -> exit with 'already running'; stale/dead entry -> take over. Clean shutdown removes the discovery file; a crash leaves a stale one that the next start reclaims and that tools treat as 'app not running' after a failed /health. Discovery-file writes are atomic (temp + rename); a parse failure is treated as 'app not running', never an error. No daemon, no service install, no auto-restart in MVP. Binds: FR-01, FR-12, FR-14, NFR-01, the addendum's corrupt-discovery-file item.
- (constraint) Two PRD amendments are required by this spine and must be carried back: (1) FR-14 + glossary name ~/.artificial-planeswalker/companion.json - the discovery file now homes under platformdirs data_dir(); (2) NFR-02 states file:...?mode=ro as the read-only mechanism - enforcement is now a CI import boundary on one engine. Also worth correcting: OQ-B lists datamodel-code-gen, which generates Python from schemas, not TypeScript.
- (version) Versions verified on the web 2026-07-25: FastAPI 0.139.2 (2026-07-16, requires Python >=3.10 - project is >=3.12, compatible); uvicorn 0.51.0 (2026-07-08, requires Python >=3.10); Vite 8.0.x stable (2026-03-12, Rolldown Rust bundler); React 19.2.x (2026-06); zustand 5.0.14 (2026-05); openapi-typescript published 2026-02, requires Node >=20. Existing deps already cover httpx (>=0.28.1), pydantic v2, platformdirs. Bound as >= floors matching the project's existing pyproject.toml convention, not exact pins.
- (event) Reviewer gate run 2026-07-25: lint_spine clean (3 low findings are false positives - {port} and {scryfall_id} are real URL/route patterns). Three lenses run inline (subagent dispatch unavailable in this session): tech-currency, adversarial-seam, rubric-walker. 10 findings, ALL fixed in place. Reviews in reviews/.
- (decision) GATE FIX (critical): AD-11 face handling is driven by the PRESENCE of per-face image_uris inside card_faces, NOT by a layout string. Verified: src/data/models/card.py has NO layout column - it has card_faces (JSON) and image_uris (JSON). The presence test is also the more precise signal: split/adventure/flip carry card_faces WITHOUT per-face image_uris so they fall out as single-image automatically. No migration, no re-import. PRD FR-04 says 'driven by the card's Scryfall layout' and carries the same defect - amendment needed.
- (decision) GATE FIX (critical): TypeScript pinned >=5.9,<6.1, NOT an open floor. TypeScript 7.0 went stable 2026-07-08 but typescript-eslint declined TS7 support on day one and publishes a peer range of <6.1.0 - npm ci fails outright and ESLint crashes if forced. NFR-07 requires eslint in CI, so an open floor would resolve to TS7 and break the frontend gate on first run. Root cause: no stable programmatic API until TS 7.1. Adoption moved to Deferred.
- (decision) GATE FIX: NEW AD-16 - REST is HTTP-native, not MCP-shaped. Status codes carry outcome; success bodies are the existing Pydantic schemas unwrapped; the MCP status-enum convention stops at the MCP boundary. One typed error body with a closed reason token mapping 1:1 to UX states: deck_not_found(404), database_not_initialized(503), database_unavailable(503), invalid_request(400), payload_too_large(422). Prevents: the UI being unable to distinguish deleted-deck (FR-11) from missing-DB (FR-22) from transient-read-failure - three distinct UX surfaces through one endpoint. Also assigns companion_set_active_deck deck-existence validation to the MCP TOOL.
- (decision) GATE FIXES (high/medium/low): AD-11 - the backend NEVER serves a substitute image; failure and no-image-data are distinguishable so the client can render DESIGN.md's named placeholder (name+pips+type line), which only the client has data for. AD-9 - fire-and-forget means a bounded-timeout await (~1s), NOT a detached task; create_task can be torn down before it runs, losing the event silently. AD-5 - the WS upgrade validates Origin as well as Host. AD-12 - ONE card-hydration cache in the zustand store keyed by card ID, deduping in-flight requests; no second data-fetching library (the detail panel updates on hover across a 100-tile grid). AD-6 - envelope id is opaque, FR-18 history orders by ts. AD-11 - pacing is async throughout, never blocking the event loop.
- (event) spine finalized 2026-07-25. Deliverables: ARCHITECTURE-SPINE.md (16 ADs, status final), EPIC-SPLIT.md (work split view -> bmad-create-epics-and-stories), walkthrough.html (interactive, self-contained, Voltglass), reviews/ (3 lenses). Memory updated (companion-app-intake) with the 4 spine-overrides-PRD deltas.
