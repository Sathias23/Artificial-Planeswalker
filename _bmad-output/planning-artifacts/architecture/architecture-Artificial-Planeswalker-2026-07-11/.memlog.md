---
scope: assess_deck_power MCP tool — deterministic, explainable Commander-first + Standard deck power assessment (FG1–FG6), single live Spellbook dependency, dual output
purpose: Architecture spine for the Deck Power-Level Assessment feature (PRD 2026-07-11): fix the invariants for scoring pipeline, Game Changer schema change, combo integration, confidence, and output shape so implementation across FGs stays consistent
altitude: feature
updated: 2026-07-11T15:37
---

- (direction) Working mode: Coaching path. Deliverable: spine only (companion can come later).
- (constraint) INHERITED (project-context.md): layer import direction data->logic->mcp_server->ui; src/data & src/logic framework-free; repos return Pydantic schemas never ORM; mypy --strict; async in data/logic; MCP tools stateless (deck_id/format are params, no session state); no Alembic (hand-written migration scripts in scripts/); graceful status-enum degradation (index_unavailable pattern); Google docstrings; ruff.
- (question) CONFLICT to resolve: PRD NFR7 says assess_deck_power is a stateless SYNC def (FastMCP threadpool). But its three sibling deck-analysis tools (analyze_mana_curve/detect_synergies/validate_deck) are ASYNC def awaiting get_deck_with_cards on the FastMCP event loop (D-1.3a). Only the sqlite-vec search tools are sync/threadpool. Tool shape is a real fork and couples to where the live Spellbook HTTP call runs.
- (decision) [ADOPTED] assess_deck_power is an ASYNC def MCP tool, registered alongside the Epic-1 analysis tools (awaits get_deck_with_cards on the FastMCP event loop, D-1.3a). Overrides PRD NFR7's 'sync def (threadpool)' claim, which was misinherited from the Epic-2 sqlite-vec search tools (sync only because the vec index needs the sync connection — N/A here). Binds: tool-registration style + how the live Spellbook call runs (async httpx on event loop). Prevents: a threadpool/sync tool diverging from its three sibling analysis tools and needing a separate connection story.
- (decision) [ADOPTED] Hard split: impure EDGE (tool/adapter layer, does all I/O — async deck load, Spellbook fetch+cache) vs PURE deterministic SCORER in src/logic that takes already-resolved inputs (list[Card], combo data as plain values, format profile) and returns the Assessment. Scorer has no network/DB/clock. Combo data is fetched+cached at the edge then passed in FROZEN, so a cache hit and cache miss yield identical scores. Binds: NFR1 determinism, NFR2 explainability, the regression-test surface. Prevents: network state leaking into scores; two builders tangling fetch into the math and breaking the diff guarantee.
- (decision) [ADOPTED] Weights + mappings + rubric metadata live as typed frozen constants in an in-repo Python FormatProfile module (one profile per format), NOT an external data file (no runtime deploy/edit gap on a local single-user server; keeps mypy --strict coverage + determinism) and NOT inline magic numbers. Profile is a PASSIVE versioned data bag (weights, signal->0-100 mappings, expected-win-turn band, rubric selector brackets|heuristic_only, flags multiplayer_variance/combos_enabled, list-version strings); the ONE deterministic scorer holds all behavior and branches on the profile. Binds NFR8 (documented/adjustable/benchmark-validated) + FR4 + the Commander/Standard fork. Prevents: scattered magic numbers, untyped data-file drift, scoring logic split across per-format strategy classes.
- (decision) [ADOPTED] Emit a SINGLE monotonic format_profile_version string per format (FR4/NFR5), bumped by hand whenever any weight/band/list-version inside the profile changes. Sub-versions (GC list, bracket rules) are recorded as fields inside the profile for display but do not version independently. Binds: score traceability. Prevents: a score that can't be tied back to the exact profile that produced it.
- (decision) [ADOPTED] game_changer is a NULLABLE column on CardModel/Card (bool | None); NULL = not-yet-populated (the window between the additive migration and backfill/re-import). Added to transform_scryfall_card (from Scryfall bulk 'game_changer'), shipped via a hand-written migration script in scripts/ (no Alembic, per convention). Invariant: any deck card with game_changer IS NULL => GC data stale => edge adds confidence reason 'game_changer_data_unavailable' and the absent count MUST NOT lower the Commander Bracket floor. Downstream readers MUST treat None as a distinct state, never coalesce to False. Binds FR11 + NFR3 honesty. Prevents a confidently-wrong Bracket 2 on a pre-backfill DB corrupting a diff.
- (decision) [ADOPTED] Effective Game-Changers-list version = the Scryfall snapshot date (GC membership is a per-card boolean sourced from the bulk snapshot; freshness couples to import cadence, an accepted PRD risk). The FormatProfile carries a game_changers_version string as documentation of the ASSUMED list (e.g. Feb 2026 = 53 cards); no hard snapshot-freshness gate in v1. Binds NFR5 traceability. Prevents overbuilding a freshness-check subsystem for v1.
- (decision) [ADOPTED] Combo cache = a dedicated EPHEMERAL table in cards.db (D2 single-file topology), not a separate cache file. Key = content hash of (sorted commanders + sorted mainboard names); row carries fetched_at for a 24h TTL and the DISTILLED combo records actually used (bucket included|almostIncluded, bracket_tag, produces, popularity, earliest_turn_estimate) not raw Spellbook JSON; cache-row schema is versioned so a shape change invalidates cleanly. Table is truncatable/rebuildable (like card_vec). The assess edge is the SOLE writer -- this is the first analysis tool to WRITE to cards.db (curve/synergy/validate are read-only); single upsert-by-hash under WAL, relying on the existing concurrent-writer hardening. Binds NFR1 (cache is deterministic input) + NFR4. Prevents cold re-fetch per call and network state leaking into scores.
- (decision) [ADOPTED] Combo-fetch degradation ladder (mirrors index_unavailable): fresh cache hit (<24h) -> use it, high confidence contribution; expired entry + fetch fails (network/429 after backoff) -> use STALE entry + confidence reason 'combo_data_stale'; no entry at all + fetch fails -> combos absent + reason 'combo_data_unavailable' + degrade confidence. Never crash, never silently score combos as zero-with-high-confidence. Binds NFR3. Prevents a Spellbook outage from either crashing assess or silently lowering the Bracket floor at high confidence.
- (decision) [ADOPTED] Output = ONE versioned Pydantic AssessDeckPowerResult (matches sibling *Result convention): status enum {ok, deck_not_found, unsupported_format, database_not_initialized, error} + human 'summary' string + structured 'assessment' (7-dim vector, bracket, for_format_score, 1-10 projection, confidence{level,reasons[]}, flags{game_changers,combos,structural_gaps,mass_land_denial,extra_turn_chains,cedh_candidate}) + schema_version. Shape = docs/deck-assess.md schema MINUS absolute_score, per-score low/high band, percentile, EDHREC. Binds FR22/FR23/FR24. Prevents caller mis-diffing across a shape change (schema_version guards it).
- (decision) [ADOPTED] DETERMINISTIC SERIALIZATION invariant for the Result (this makes goal #2 survive NFR1): (1) all flag lists + confidence.reasons[] emitted in deterministic SORTED order, never set/insertion order; (2) dimension scores are integer 0-100, the 1-10 projection rounded to fixed precision; (3) NO call-time clock in the deterministic core -- the Result must not embed assessed_at/now() even though project-context mandates datetime.now(UTC) elsewhere; 'as of' info comes only from inputs (snapshot date, cache fetched_at) which are equal across two runs of the same deck; (4) the diff surface is the 'assessment' block, the human 'summary' is a pure deterministic projection of it. Two runs of the same deck+snapshot+cache MUST yield byte-identical JSON. Binds NFR1 at the output boundary. Prevents an implementer reaching for now()/unsorted sets and silently destroying every diff.
- (decision) Placement (boundary invariant, exact filenames are seed): (1) EDGE = async tool src/mcp_server/tools/assess_deck_power.py + registration in server.py -- orchestrates only. (2) PURE SCORER + FormatProfile + Karsten land/pip math + hypergeometric consistency + oracle-text classifiers + combo->bracket mapping = new src/logic/assessment/ package, framework-free, no network/DB/clock. (3) Live Spellbook client = async httpx I/O adapter at the DATA layer (sibling to importers/scryfall_api.py precedent), NOT in src/logic. (4) Combo-cache = a repository in src/data/repositories returning Pydantic schemas (never ORM). Binds the data->logic->mcp_server import direction. Prevents network/DB creeping into the pure scorer or the tool layer accreting domain logic.
- (decision) Oracle-text classifiers (ramp/draw/removal/tutor counts FR6, win-con tags FR10, mass-land-denial + extra-turn detection FR12) are NEW pure functions in src/logic/assessment, following the existing src/logic/synergy.py oracle-text/keyword conventions (lowercase match, Card.keywords + oracle_text) -- a single shared taxonomy, NOT a parallel one duplicated in the tool layer. Binds NFR2. Prevents two divergent oracle-text vocabularies.
- (constraint) Operational envelope: NO new deploy target/infra (same local stdio MCP server). Two NEW operational surfaces: (a) first outbound network egress to Commander Spellbook -- requires User-Agent, polite throttle, 429 backoff, >=24h cache (NFR4); (b) a one-time ADDITIVE migration script (adds nullable game_changer) + backfill/re-import to populate it (heavy ~60k cards, FR11 risk). Both degrade gracefully per NFR3.
- (question) DEFERRED to first-implementation/calibration (PRD sec9, structural home already decided = FormatProfile): (1) the actual per-dimension signal->0-100 mapping curves + aggregate weight VALUES (NFR8; hand-tuned, documented, benchmark-validated); (2) benchmark-set composition (which precons + cEDH lists) = first impl task, becomes the acceptance signal; (3) combo earliest-turn heuristic method + confidence. Spine fixes WHERE these live and that they are frozen/versioned; it does NOT invent the numbers.
- (event) Reviewer gate ran: lint clean (0); rubric-walker PASS-WITH-FIXES; tech-currency PASS w/ nuances; adversarial-seam found 9 incompatible pairs (2 critical). Reviews in reviews/. Applying fixes.
- (decision) FIX (F1 critical): ONE canonical frozen ComboRecord is the single combo shape across cache, core, and output. Spellbook-sourced fields only: {spellbook_id, cards[] sorted names, bucket, bracket_tag, produces, popularity}. Derived fields (type e.g. two-card-infinite, earliest_turn_estimate) are computed in the PURE core, NOT cached -- resolves the AD-5(cache)/AD-7(flags) shape clash AND the earliest_turn_estimate double-ownership (F7). New AD-11.
- (decision) FIX (F3 high): bracket_tag is a closed enum {RUTHLESS->4, SPICY->3, POWERFUL->3, ODDBALL->2, PRECON_APPROPRIATE->2, CASUAL->1}, exact Spellbook wire casing, normalized once at the edge; the core combo->bracket map keys on it exactly. Prevents a casing mismatch silently producing a wrong Bracket floor.
- (decision) FIX (F2 critical): confidence.reasons[] and structural_gaps[] are CLOSED snake_case token enums; a token NEVER embeds a count or free phrase (embedded counts break AD-8 sorted determinism and manufacture diff noise). Any count lives in a separate structured field; human phrasing appears ONLY in the summary string. Tightens AD-6.
- (decision) FIX (F6 high): New AD-12 -- cache key is sha256 of canonical JSON {commanders: sorted names, main: {name: qty}} INCLUDING multiplicities (Standard 4-ofs must not collide with 1-ofs), via one named pure helper both read and write paths call. Builtin hash() forbidden (PYTHONHASHSEED-salted, non-deterministic).
- (decision) FIX (F5 high): AD-8 pins the 1-10 projection: value = 1 + for_format_score*9/100, half-up rounding to 1 decimal via Decimal, emitted as a JSON number (7.0 not 7). All output lists sort ascending by their token/string (bytewise). Fixes byte-drift across builds.
- (decision) FIX (F4 high, F9/F10 med): AD-7 -- the 7 dimension keys are a fixed closed set, ALL present in every assessment regardless of format (never omitted). Commander-only flags (bracket, mass_land_denial, extra_turn_chains, cedh_candidate) are format-gated: absent/false for Standard; cedh_candidate homed once in flags. On non-ok status, assessment is null (only status + summary populated); schema_version always present.
- (decision) FIX (tech-currency med + rubric FR14 high): AD-5 -- combo-cache migration ensures PRAGMA journal_mode=WAL (persistent per-file) so the write path does NOT depend on the search-index build having run first (the async engine sets only busy_timeout=5, not WAL). AD-9 -- Spellbook client policy pinned: read the paginated 'results' envelope, feed only 'included'+'almostIncluded' buckets (exclude change-commander/add-color); required User-Agent+Accept headers; polite throttle; manual exponential-backoff retry on 429/network mirroring scryfall_api.py's max_retries/retry_delay/2**attempt loop (tenacity is NOT a dep).
- (decision) FIX (rubric low-med): AD-4 notes the backfill requires a Scryfall re-import to populate game_changer; the interim degraded-confidence window is expected. binds gains NFR6 (benchmark testability). Stack row corrected to sqlalchemy[asyncio]>=2.0.44 (the [asyncio] extra is why no new async dep is needed).
- (event) All reviewer fixes applied (F1-F10 + tech-currency WAL/buckets/[asyncio] + rubric FR14/NFR6/backfill). Added AD-11 (canonical ComboRecord) + AD-12 (canonical multiplicity-aware cache key); tightened AD-4/5/6/7/8/9. Re-lint clean (0 findings). Spine finalized.
- (event) Source sync: PRD NFR7 updated (sync def -> async def, citing AD-1 + D-1.3a) so upstream no longer contradicts the spine. project-context.md's general 'MCP tools are sync def' line is a pre-existing imprecision (Epic-1 tools are already async) not introduced here; left as-is per scope.
