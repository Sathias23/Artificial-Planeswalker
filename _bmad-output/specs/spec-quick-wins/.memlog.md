---
topic: The five quick wins of docs/future-ideas.md (2026-09-06): metadata/quantity tools, clone, export, recommendation provenance, visible legality coverage
updated: 2026-09-06T15:16
---

- (direction) Sathias: /bmad-spec 'the quick wins from docs/future-ideas.md'; the doc's Suggested delivery sequence step 1 names them: metadata/quantity tools, clone, export, recommendation provenance, visible legality coverage
- (decision) Slug quick-wins; one spec for the five items; express mode (input is a ranked review with evidence links, verified against code in-session)
- (note) Scope boundary: ranks 1 (full format rules), 2 (transactional preview/apply), the plain-text IMPORT half of rank 5, and ranks 7-15 are steps 2/3 of the source's sequence and are non-goals here; the doc calls itself 'proposals, not committed scope'
- (note) Evidence CAP-1: DeckRepository.update_deck(name, strategy=_UNSET, tags=_UNSET) and update_card_quantity(deck_id, card_id, quantity>=1, sideboard) exist (src/data/repositories/deck.py:145,592) but no MCP tool calls either; server.py registers create/load/delete/add/remove/import only; remove_card_from_deck has no quantity parameter, so lowering a count today means remove then re-add
- (capability) CAP-1 deck metadata edit tool: caller can rename a deck and set or clear its strategy and tags through one MCP tool, omitted vs cleared preserved via _UNSET; success: rename changes only name; omitting strategy leaves it; passing null clears it; unknown deck answers not_found; companion receives deck_changed; updated_at advances
- (capability) CAP-2 set card quantity tool: caller can set the exact copies of a card in a board (by card_id or name, mainboard or sideboard); success: quantity N yields N on reload; a card absent from that board answers card_not_found without adding it; quantity 0 removes the row; identity and updated_at refreshed; deck_changed emitted
- (decision) CAP-2 quantity 0 removes the association rather than erroring; reason: a 0 that errors forces the remove-then-reload dance the item exists to kill; the repository keeps its >=1 contract and the tool routes 0 to remove_card_from_deck
- (note) Evidence CAP-3: no clone anywhere; compare_deck_power docstring (compare_deck_power.py:23) tells callers to import_decklist again to freeze a before copy; merge_decks is the closest write path; DeckCard rows carry quantity/sideboard/commander
- (capability) CAP-3 clone deck tool: caller can copy a deck into a new deck (optional new name, default '<name> (copy)'), preserving every card row's quantity, board and commander flag plus format, strategy, tags and colour identity; success: clone's card rows equal source's row for row; new id and created_at; source untouched; a failure mid-copy leaves no partial deck; compare_deck_power docstring points at clone_deck
- (note) Evidence CAP-4: import parses only Arena lines '<qty> <name> (<SET>) <collector_number>' under Commander/Deck/Companion/Sideboard headers (deck_import.py:46); cards carry set_code and collector_number so the same shape can be emitted; no export tool exists; companion is read-only so a download must be a GET of a text body
- (capability) CAP-4 decklist export: caller can export a deck as Arena text (the shape import_decklist accepts) or plain '<qty> <name>' text, commander/mainboard/sideboard sectioned; success: export then import into a fresh deck round-trips every row (quantity, board, commander) for the Arena shape; plain shape has no set/collector suffix; companion exposes the same text at a GET route with a copy control; empty deck exports headers only
- (decision) CAP-4 shape: Arena export uses the section headers the importer understands (Commander, Deck, Sideboard) so round-trip is provable with the existing tool; a Companion section is never emitted because the importer folds it into sideboard on the way in
- (note) Evidence CAP-5: SuggestionsPayload/SwapsPayload carry title + items only; the envelope carries id + ts; nothing names the deck a push was about; agentView.ts keeps HISTORY_CAP=20 pushes keyed by id/ts/kind; SuggestionItem/SwapItem carry agent confidence (high/medium/low or None); no computed-evidence field exists
- (capability) CAP-5 recommendation provenance: every suggestions/swaps/tier/groups push names the deck it was made for, and the companion shows that name on the view and in history, marking a push stale when its deck has changed since the push or is not the displayed deck; success: contract accepts an optional deck_id per payload; a push for deck A viewed while B is active is labelled with A's name; a push older than the deck's updated_at is marked stale; pushes without deck_id render as today (no label, never stale); history entries keep their label after a deck switch
- (decision) CAP-5 staleness = push ts earlier than the deck's current updated_at, or push deck_id differs from the displayed deck; reason: updated_at is maintained on every card mutation since spec-quality-review-fixes CAP-4, so it is the only revision marker available without new persistence; a true revision id is rank 12 and out of scope
- (decision) CAP-5 confidence: the contract already documents the field as 'how sure the agent is', so the companion labels it agent confidence in copy; no computed-evidence field is added because no MCP path computes one for a swap yet (rank-2 preview would)
- (note) Evidence CAP-6: validator applies 60-min/15-max regardless of format (deck_validator.py:179-181,318); format_check emits six rows with rotation permanently advisory; nothing says commander size, colour identity, commander eligibility, restricted, or copy-exception rules were NOT checked; the docstring itself calls Commander size a known gap
- (capability) CAP-6 visible legality coverage: validate_deck and format_check report which format rules were checked and which known rules were not, per format, so a pass never reads as full coverage; success: a commander report lists commander size (100 exact), colour identity and commander eligibility as unchecked; a vintage report lists restricted as unchecked; a standard report lists nothing beyond rotation; the companion format panel renders the unchecked list; the format-legality skill states the same coverage
- (decision) CAP-6 is reporting only: no new rule enforced, no verdict changes; the source says expose coverage first, then implement the rule profiles, and the profiles are rank 1 (core investment)
- (constraint) All deck mutations (CAP-1..4 write side) live in src/mcp_server; the companion export route and coverage panel read only (AD-2); the import-boundary tests stay green
- (constraint) Every new or extended tool is stateless (deck_id supplied), emits deck_changed after a successful write, and updates the five shipped skills plus plugin/ and the OpenAPI/types artifacts in the same PR
- (constraint) No schema change: CAP-5 uses updated_at as the revision marker and CAP-6 reports coverage from a table in src/logic; a migration would make these not quick wins
- (constraint) Analysis and coverage reporting stay observational; CAP-6 changes no verdict; CAP-4 export never mutates
- (constraint) Repository write semantics stay: _UNSET for omitted, None for clear, commit-and-refresh, rollback on IntegrityError; clone is one transaction
- (decision) Suggested order: CAP-1 and CAP-2 together (one deck_management PR), CAP-3, CAP-4, then CAP-6, CAP-5 last; reason: 1-4 are MCP-only and share the repository; 5 and 6 each cross the contract boundary and need gen:api
- (assumption) Companion export is a GET returning text/plain at /api/deck/{id}/export?shape=arena|plain with a copy-to-clipboard control; a download link is optional since the sandboxed browser may block it
- (assumption) Default clone name is '<source name> (copy)'; names need not be unique so collisions are fine
- (assumption) CAP-6's per-format unchecked-rule table is hand-maintained in src/logic/deck_validator.py and pinned by a test that every FormatType member has an entry
- (question) CAP-2: should set_card_quantity accept quantity 0 as remove, or reject it? Spec assumes 0 removes
- (question) CAP-5: should deck_id become required on companion_show_* payloads, or stay optional for older callers? Spec assumes optional
- (question) CAP-4: is a companion download button wanted, or is copy-to-clipboard enough?
- (question) CAP-6: should the unchecked-rule list also appear in validate_deck's LLM-facing summary text, or only in the structured result?
- (event) Self-validate pass 1 (coherence): PASS. Six CAPs each carry intent+success; intents are WHAT; every constraint rules something out; non-goals name ranks 1, 2, 7-15 and the import half of 5; success signal is a runnable scenario; IDs 1-6 unique
- (event) Self-validate pass 2 (preservation): PASS. Rank 3 -> CAP-1/2; rank 4 -> CAP-3 (boards, commanders, quantities, metadata); rank 5 export half -> CAP-4 (plain + Arena, copy; download = open question); rank 6 -> CAP-5 (deck label, predates-edit staleness, agent-confidence copy; computed-evidence field deferred as non-goal); rank 1 'explicitly identify unchecked rules' -> CAP-6; Existing-strengths guardrails and omitted-vs-cleared + deck_changed -> Constraints. Wrapper-only content dropped: review framing, ranking table columns, evidence hyperlinks, 'report export' under history provenance (not listed in step 1; belongs with rank 12)
- (event) SPEC.md rendered from memlog; no companions (no diagrams, no multi-row catalogs); story breakdown not run this session (non-interactive); offer stands on request
- (direction) Sathias: 'break this into stories'
- (decision) Story 1 = CAP-1 + CAP-2 (one deck_management PR; shared pattern and skills ripple)
- (decision) Story 2 = CAP-3 clone (repository method beside merge_decks, one transaction)
- (decision) Story 3 = CAP-4 export, formatter in src/logic shared by MCP and companion; spec_checkpoint + done_checkpoint (first contract-crossing story, Greptile batch boundary)
- (decision) Story 4 = CAP-6 coverage reporting; spec_checkpoint (extends result models the skill and panel consume)
- (decision) Story 5 = CAP-5 provenance, last; spec_checkpoint + done_checkpoint (contracts + UI state + skills; closes the spec)
- (decision) Checkpoint defaults set without live confirmation (autonomous session): spec_checkpoint on the three contract-crossing stories (3, 4, 5), done_checkpoint after 3 and 5 as Greptile batch boundaries; Sathias may flip any of them
- (event) stories.yaml written: 5 stories; schema check PASS (ids quoted, unique, prefix-free, no status field, all required fields present)
