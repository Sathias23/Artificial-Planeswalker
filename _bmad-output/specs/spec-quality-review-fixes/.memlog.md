---
topic: Fixes for the six findings of the 2026-09-06 external quality review, in the order 6, 1, 2, 3, 4, 5
updated: 2026-09-06T08:31
---

- (direction) Sathias: 'write a spec using that suggested order' after asking for comment on docs/quality-review-2026-09-06.md (external review at 5f302f4); input = the review + the in-chat verification of its six findings
- (decision) Slug quality-review-fixes; one spec for all six findings; express mode, input rich
- (note) In-chat verification: all six findings confirmed in code; finding 1 confirmed live (project engine reports PRAGMA foreign_keys = 0); findings 2 and 3 confirmed by reading, not in a browser
- (decision) Order of work: finding 6 (project context) first, then 1 (FK enforcement), then 2 and 3 (companion), then 4 and 5 (deck metadata, index metadata); reason: 6 is zero-risk and loaded on every bmad-build run, 1 is the only P1, 4 and 5 have no consumer or a synthetic trigger
- (capability) CAP-1 referential integrity: every project SQLite connection enforces foreign keys, deck delete removes its associations, existing orphan rows are cleaned; success: pragma reads 1 on a fresh connection, no deck_cards rows survive delete_deck, dangling inserts raise IntegrityError, orphan cleanup proven on a seeded db, Scryfall import and integration suite pass with enforcement on
- (note) CAP-1 evidence: model declares ondelete CASCADE but SQLite ignores it without the pragma; delete_deck is a Core DELETE so ORM cascade never runs; the existing cascade test asserts only the deck row; importer scryfall.py:312-321 already deletes DeckCardModel rows before deleting stale cards; cards upsert uses on_conflict_do_update not REPLACE
- (capability) CAP-2 companion first-connect reconciliation: the deck view reflects the active deck as of the moment the socket goes live, not only as of the initial snapshot; success: a test that switches the active deck between snapshot response and first socket open ends with the new deck on the glass; reconnect re-drive unchanged; no boot on re-render
- (note) CAP-2 evidence: socket.ts succeed() calls onReconnected only when failures > 0; a later deck_changed for the new deck does nothing because deck.ts driveDeckChanged ignores a different id against the settled deck, so the gap does not self-heal
- (capability) CAP-3 transient deck refusal recovers: a deck request refused with database_unavailable while the poll is healthy or stopped recovers without user action or a socket frame; success: both orderings (poll recovers before and after the deck refusal) end with the deck loaded; a permanently refusing id still does not loop; a superseded request never overwrites newer state
- (note) CAP-3 evidence: useDeckState listener fires only on the edge into no-active-deck and skips a booting deck; the poll stops on no-active-deck; the straddle ordering leaves the deck refused with nothing to restart it; smaller fix than the review's retry loop: restart the stopped poll when the boot settles on a transient refusal
- (capability) CAP-4 deck metadata maintained: a deck's reported colour identity and modification time reflect its cards after every mutation path (add, bulk add, remove, quantity update, import, merge) and identity derives from card color_identity; success: after each path updated_at advanced and color_identity equals the WUBRG-sorted union of card color_identity; empty colors with blue identity yields U
- (note) CAP-4 evidence: only merge_decks calls update_deck_color_identity; helper reads card.colors; nothing in ui/src or the companion reads deck color_identity or updated_at today (types.d.ts carries them) so the defect is latent
- (capability) CAP-5 search-filter metadata refreshed: an incremental index build refreshes a card's colour flags and mana value when only those changed, without re-embedding unchanged text; success: after changing only colors/cmc a filtered search reflects the new values and the embedder is not called for that card
- (note) CAP-5 evidence: index_builder hashes compose_card_text only; colors and cmc ride alongside; the review's repro changed colors and cmc while leaving mana_cost intact, which real Scryfall data rarely does; realistic trigger is a Scryfall correction; search deliberately filters on colors not color_identity (index_builder docstring)
- (capability) CAP-6 agent-facing project context current: the context every bmad skill loads as a persistent fact describes the current MCP server + companion architecture, entry points, gates and commands, and nothing loaded describes the Chainlit/PydanticAI monolith as current; success: no file matched by any skill's persistent_facts glob mentions Chainlit or PydanticAI as current; the loaded context names the uv/ruff/mypy/pytest/npm gates and the plugin rebuild rule
- (note) CAP-6 evidence: _bmad-output/project-context.md dated 2026-06-20, predates the MCP server, companion, and both releases; 31 skill customize.toml files load it by glob **/project-context.md, bmad-spec points at a root project-context.md that does not exist; no CLAUDE.md or AGENTS.md in the repo; bmad-project-context skill produces an AGENTS.md block and offers to absorb a retired project-context.md
- (constraint) Order: CAP-6 merges first; CAP-1 before CAP-4 and CAP-5 (same repository file, enforcement changes what the metadata tests may insert); CAP-2 and CAP-3 together
- (constraint) Two Greptile runs: backend PR (CAP-1, CAP-4, CAP-5) and companion PR (CAP-2, CAP-3); CAP-6 lands straight to master without Greptile; full local gate before the first push of each Greptile PR
- (constraint) The foreign-key pragma and orphan cleanup ride the existing connect-hook path and never fail a connection; the companion engine (read-only shell, AD-2) gets the pragma but never the cleanup write
- (constraint) CAP-2 must not boot on re-render; CAP-3 must not add a level-triggered retry that loops against a permanently refusing id
- (constraint) Deck identity derives from card color_identity; search filters keep using colors
- (constraint) CAP-5 never re-encodes a card whose embedding text is unchanged
- (constraint) Regression tests are behaviour tests through real entry points, never source scans; the docs-drift ban from spec-quality-audit-p1 stands
- (constraint) Generated artifacts (plugin/, companion static, openapi.json, types.d.ts) rebuilt and committed in the PR that moves them; layering, mypy --strict on both platforms, no new UI runtime dependency; all work is [Unreleased] toward 0.6.0
- (assumption) Batching CAP-4 and CAP-5 into the CAP-1 PR trades the strict 6,1,2,3,4,5 order for one fewer Greptile run; a third run buys the strict order
- (assumption) Orphan cleanup runs once from the MCP engine's connect path, guarded like the NOCASE index migration, so an existing database is repaired without initialize_database
- (assumption) The stored deck color_identity and updated_at columns stay (the companion OpenAPI contract carries them); deriving on read is acceptable if the contract is unchanged
- (question) CAP-6 location: regenerate _bmad-output/project-context.md in place (zero config churn, the glob keeps working) or produce an AGENTS.md block via bmad-project-context and repoint 32 customize.toml persistent_facts entries
- (question) CAP-1 stance on the cards foreign key: should an association that references a card missing from cards.db be rejected (strict) or should the constraint stay CASCADE-only on decks; the MCP add path already pre-validates card_not_found, so strict is expected to be free
- (event) Self-validate pass 1 (coherence): PASS. Six capabilities each carry intent and success; intents state observable outcomes; every constraint rules something out; five non-goals; success signal is demonstrable; IDs CAP-1..CAP-6 unique and new for this slug
- (event) Self-validate pass 2 (preservation): PASS. All six findings, their recommendations and reproductions landed in SPEC.md or batches.md; the review's bounded-backoff retry recommendation for finding 3 is deliberately not adopted (constraint) with the smaller fix noted; the review's own implementation order is superseded by the user's. Wrapper-only content dropped: the validation-run pass counts and lint results (CI reproduces them), the 'review did not modify source files' statement
- (decision) Sathias ruled CAP-1 strict: an association whose card is missing from cards.db is rejected (IntegrityError), not merely the decks cascade; open question closed
- (decision) CAP-6 location recommended and accepted pending confirmation: bmad-project-context produces the AGENTS.md block and absorbs then retires _bmad-output/project-context.md; a one-line CLAUDE.md importing AGENTS.md (@AGENTS.md) makes Claude Code load it in every session including bmad-build; Codex reads AGENTS.md natively; no customize.toml edits, the 31 globs simply match nothing; reason: zero config churn that a BMad update would overwrite, and one file of truth for both agent hosts
- (constraint) CAP-6: no customize.toml persistent_facts entry is edited; the retired project-context.md is deleted, not left as a pointer, so no skill loads a stale fact
- (event) Spec re-derived after the two rulings; open questions empty
