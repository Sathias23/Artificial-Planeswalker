---
topic: P1 fixes from the 2026-09-03 quality audit, batched to minimise Greptile runs
updated: 2026-09-03T20:03
---

- (direction) Sathias: 'lets spec these fixes' after asking how to implement all P1 audit items with limited Greptile runs; input = audit (research/quality-audit-2026-09-03.md) + in-chat batching plan
- (decision) Slug quality-audit-p1; one spec for all eight P1 items plus the three P2 riders the batching plan folded in (11-part, 17-part, 18)
- (note) v0.5.0 already tagged (CHANGELOG 2026-08-25); all work lands in 0.6.0, nothing is pre-cut
- (decision) Express mode: input rich, extract directly, gaps become open questions
- (capability) CAP-1 indexed case-insensitive exact card lookup + single-transaction import; success: index SEARCH in query plan, ~1 ms lookup, 100-line import <0.5 s, one commit
- (capability) CAP-2 search/index/initialize tools run off the event loop; success: a concurrent tool call completes while build_search_index runs; server.py docstring corrected
- (capability) CAP-3 companion cold open request diet (deck list w/o card rows, cards batched or embedded, card route cacheable, image route cache-first, booting panel, hashed+recompressed hero); success: no hero fetch with an active deck, cold-open median down >=150 ms vs 529 baseline on cdp_harness budget
- (capability) CAP-4 provenance out of code comments in src/ ui/ ci.yml package.json ui/README; success: zero hits for maintainer name/Greptile/review round/dates in src and ui/src, six worst files below 50% comment share
- (capability) CAP-5 lint-as-tests removed or moved to stylelint/eslint; docs-drift suites out of the unit job; success: only the five named gates read source text, unit job >=25% faster, ui node project no longer needs 180 s timeout
- (capability) CAP-6 behaviour tests for import layer + MCP wire; success: spellbook/scryfall_api/combo_snapshot >=85% each, every registered tool has a call_tool round trip
- (capability) CAP-7 version single-sourced from package metadata, SECURITY.md current incl. companion surface
- (capability) CAP-8 implementation-artifacts out of master, local path scrubbed, pre-commit guard; success: git grep for the path returns nothing, planning-artifacts retained
- (capability) CAP-9 (rider, P2-17) tracked vitest cache removed, root node_modules ignored, lib/build/dist anchored, graphify rule removal committed
- (capability) CAP-10 (rider, P2-11) quantity ceiling reused from import, deck string/tag caps, isfinite+page ceiling, destructiveHint on rebuild tools, HTTP MCP transports dropped from .env.example or guarded
- (capability) CAP-11 (rider, P2-18) one lazy shared companion client with probe dropped; fastembed imported lazily; success: one round trip per push, MCP import time excludes fastembed
- (constraint) Exactly three Greptile-reviewed PRs (tests, MCP latency, cold open); doc/deletion/comment-only work goes straight to master or a no-Greptile PR
- (constraint) Full local gate before first push of every Greptile PR (ruff, format, mypy both platforms, pytest; ui lint/format/typecheck/test/build/gen:types; plugin build; clean porcelain) - PR #105 burned a run on a Prettier fix-up
- (constraint) Order: test trims (CAP-5) land before comment prune (CAP-4) and cold-open UI (CAP-3) because prose/CSS-text tests would fail them
- (constraint) Generated artifacts (plugin/, SPA bundle, openapi.json+types.d.ts) rebuilt and committed in the same PR as the source change; CI drift checks enforce
- (constraint) Retained gates: import boundary, viewer freeze (shrunk to ~3 tests), openapi byte-equality, tokens contract, package contract, no-scryfall-hosts
- (constraint) Layering (data->logic->mcp_server, companion import boundary, single port literal) and mypy strict stay green; no new UI runtime deps; companion security invariants (loopback, Host/Origin, tickets, body cap) untouched
- (constraint) Comment prune keeps the invariant sentence; drops story ids, dates, review rounds, names; AD-nn citations may stay where they link an architecture decision
- (decision) Non-goals: package rename, FTS5, App.test.tsx split, fixture re-scoping, UI validation/error boundary, docs refresh, ruff/coverage/dependabot tooling, history rewrite, viewer deletion - all P2/P3 for later specs
- (assumption) Riders CAP-9/10/11 are in scope because the batching plan folded them in and Sathias did not object; each is S effort
- (assumption) Batch 0 and batch 4 PRs skip Greptile by the standing rule that non-behavioural changes need no adversarial review
- (question) CAP-8: wiki vs orphan branch for implementation-artifacts, and does sprint-status.yaml keep living on master for the bmad skills that read it?
- (question) CAP-3: full cards on deck detail (schema change, gen:api) vs a new batch endpoint (additive)?
- (question) CAP-5: delete the docs-drift suites or keep them as a separate docs CI step?
- (question) CAP-10: drop the HTTP MCP transports entirely or guard them with the companion host middleware plus a token?
- (event) Self-validate pass 1 (coherence): PASS - 11 CAPs each with intent+success; CAP-1/CAP-3 intents name mechanisms (index, cache-first) because the observable behaviour IS the mechanism change, accepted; 9 constraints all rule something out; 7 non-goals; success signal testable; IDs CAP-1..11 unique
- (event) Self-validate pass 2 (preservation): PASS - audit P1 items 1-8 -> CAP-1..8; riders 11/17/18 -> CAP-10/9/11; out-of-scope list -> non-goals; baselines, deletion and addition lists -> batches.md. Wrapper-only content dropped: headline coverage/bundle numbers not tied to a target, and the 'already good' list (carried only as the security-invariants constraint)
- (event) Spec rendered: SPEC.md + batches.md; 4 open questions, 2 assumptions; story breakdown offered (batches 0-4 map to stories)
- (decision) CAP-8 resolved (Sathias): implementation-artifacts incl. sprint-status.yaml and deferred-work.md move to an orphan 'process' branch in the same repo; bmad skills read it via a worktree
- (decision) CAP-3 resolved (Sathias): GET /api/decks/{id} embeds full Card objects; schema + generated types change, gen:api in the same PR; no batch endpoint
- (decision) CAP-5 resolved (Sathias): delete the three docs-drift suites; keep only the two 'documented port/filename equals shipped constant' checks
- (decision) CAP-10 resolved (Sathias): drop HTTP transports from .env.example; code path stays; SECURITY.md names stdio as the supported transport
- (event) Open questions: 0 remaining; SPEC.md and batches.md re-derived
- (direction) Sathias: spec checkpoint on stories 2 and 4; done checkpoint on story 2 only; every story carries the drafted dispatch notes (stops at review, preflight gate, generated artifacts in same PR, Greptile flag)
- (decision) Story 1 = batch 0: CAP-7, CAP-9, CAP-10, CAP-8; no checkpoints; no Greptile
- (decision) Story 2 = batch 1: CAP-5, CAP-6; spec+done checkpoints; Greptile run 1
- (decision) Story 3 = batch 2: CAP-1, CAP-2, CAP-11; no checkpoints; Greptile run 2
- (decision) Story 4 = batch 3: CAP-3; spec checkpoint; Greptile run 3
- (decision) Story 5 = batch 4: CAP-4 as two PRs (src, ui+config); no checkpoints; no Greptile
- (event) stories.yaml validity check: PASS - 5 entries, ids '1'..'5' quoted and prefix-free, no status field, descriptions point into SPEC.md
