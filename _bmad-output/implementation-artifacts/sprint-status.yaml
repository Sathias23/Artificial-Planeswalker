# generated: 2026-06-20
# last_updated: 2026-07-27 (Story c2-3 IMPLEMENTED → review; AD-12 closed, both drift halves gated and proven by breaking)
# project: Artificial-Planeswalker
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# KEY NAMESPACES (three epic files feed this one status file):
#   epic-1..3   → _bmad-output/planning-artifacts/epics.md                        (MCP-server pivot, Phase 1)
#   epic-4..7   → _bmad-output/planning-artifacts/epics-deck-power-assessment.md  (renumbered from its raw 1-4)
#   epic-c1..10 → _bmad-output/planning-artifacts/epics-companion-app.md          (c-prefixed, numbering UNCHANGED)

generated: 2026-06-20
last_updated: 2026-07-27 (Story c2-3 IMPLEMENTED → review on branch feat/companion-c2-3-openapi-types, 2 commits. AD-12 is CLOSED: src/companion/contracts.py is now the single source of truth for every wire shape and the TypeScript half is no longer hand-writable. All 17 ACs met. ALL FOUR OPEN QUESTIONS WERE ANSWERED "as proposed" BEFORE Task 0 (Brad, 2026-07-27), so AC 17/Task 5 were in scope from the start and nothing surfaced mid-story — the c2-2 discipline held for a second story. The pipeline: contracts.py --(uv run python -m scripts.dump_openapi)--> committed ui/src/api/openapi.json --(npm run gen:types)--> committed ui/src/api/types.d.ts --(import type)--> ui/src/api/schema.ts, the ONE module the rest of ui/src reads wire types from. Both halves gated: a pytest snapshot test in `quality` (on both matrix Pythons, with a failure message that NAMES the fix) and a git-status drift step in `frontend` (with the git ls-files non-vacuity guard c2-2 shipped, and working-directory: . so the repo-root pathspec is not evaluated from ui/). THREE DEVIATIONS, none silent: (1) AC 5 is a test rather than a CI regenerate-and-diff step — identical coverage, runs locally and on both Pythons, names its fix; (2) AC 6 uses `git status --porcelain` not `git diff --exit-code`, consistent with the two drift checks already in the workflow and catching the untracked/newly-ignored cases; (3) AC 4's literal gen:api command DOES NOT WORK — after `cd ..` the trailing `npm run gen:types` runs at the repo root where there is no package.json (measured: npm errno -4058 ENOENT), so a `cd ui` was added; `uv run --directory ..` also works and was declined so the shipped script stays the AC's own form, with the rationale recorded in package.json's "//" block. FOUR MUTATION PROOFS pasted and all reverted (three owed, one free): a Pydantic field added without regenerating turns the AC 5 test red with its instructive message; a regenerated schema with stale types turns the AC 6 command red with its ::error:: annotation; mutating the generated `status: "ok"` to `string` leaves vitest 71/71 GREEN while tsc -b exits 2 with the exact TS2344 the story predicted (which is why schema.test.ts carries a comment saying npm test does not gate it); and planting an `interface HealthResponse` outside src/api/ turns AC 10's guard red. AC 17 (Q1) shipped: _CompanionFastAPI.openapi() now composes without_python_docstring_sections() with the existing without_auto_validation_schema() on the ONE schema-build path, truncating every description at the first Google-style section header (Args/Arguments/Attributes/Example/Examples/Raises/Returns/Yields — deliberately NOT Note:) at any depth, so a c3-1 Field(description=...) is covered with no further work; generated types 186 -> 160 lines, schema 7,059 -> 6,146 bytes, /docs improves identically, and ErrorResponse's per-token enumeration (what c2-9's state panels are written against) provably survives. ONE UNFORESEEN FINDING the story could not have predicted: AD-3's leaf/app guard SCANS scripts/, so scripts/dump_openapi.py's module-level `from src.companion.app.main import build_app` was an import-boundary violation — resolved with the function-local import form the guard ALREADY permits for the outside_app role (and documents in a comment), not by widening the guard; it is also honest, since a build-time generator should not drag FastAPI in merely by being imported, which is what the snapshot test does. ONE NEAR-MISS worth carrying: the first AC 7b proof used a PowerShell regex with a `$` anchor against a CRLF working tree, so the mutation never landed and the drift check reported a vacuous "no drift" that was one step from being pasted as a finding — `git status --porcelain` on the mutated file is what caught it, same class as c2-2's Set-Location vacuity. VERIFY THE MUTATION LANDED BEFORE BELIEVING THE CHECK'S VERDICT. Deliberately NOT done per Q4: no runtime fetch helper, no store, no getHealth() — c3-1 is the first real consumer and c4-1 owns the store and its in-flight deduping. Suites 1,742 -> 1,751 Python / 59 -> 71 frontend (7 -> 9 files); all four Python gates and all five frontend gates green; both CI drift checks run locally against the committed tree clean; AC 15 scope proof EMPTY (no dependency added on either side, pyproject/uv.lock/package-lock untouched); plugin/ mirror regenerated exactly as the story predicted for the Q1 case. STILL UNVERIFIED BY DESIGN: node 24 locally vs node 20 in CI — openapi-typescript reruns were byte-identical here but cross-version determinism can only be confirmed by CI; per gotcha 6, drift on an unmodified tree is a finding to raise, NOT a reason to loosen the check. Previously: Story c2-3 contexted → ready-for-dev. The AD-12 close: openapi-typescript wired against the backend's own app.openapi(), types committed at ui/src/api/types.d.ts, drift-checked. FIVE landmines measured at 9b612eb: (1) CI cannot run the pipeline in one job — `quality` has uv and no Node, `frontend` has Node and no Python deps — so the drift check SPLITS by toolchain with a committed ui/src/api/openapi.json as the hand-off (pytest snapshot test for the Python half, CI git-status step for the Node half), adding no toolchain to either job; (2) prettier would reformat the generated .d.ts (4-space indent, a 152-char union vs printWidth 100) — c2-1's pre-placed .prettierignore entry is load-bearing and its "Does not exist yet" comment stops being true here; (3) prettier also reformats python's json.dumps(indent=2) (collapses `"required": ["reason"]`, 7059→7009 bytes) so the schema needs its own ignore line; (4) the WHOLE Python docstring lands in the generated TypeScript as JSDoc — 186 generated lines for one endpoint and two models, ~130 of them prose including `Args: request:` and a `>>>` doctest — which is open question Q1 (recommend truncating each description at the first Google-style section header on the same schema-build path where without_auto_validation_schema already lives, so /docs improves too); (5) expectTypeOf is erased at runtime, so vitest green proves nothing about the type pins and `tsc -b` is the gate (measured: mutating the generated `status: "ok"` to `string` → tsc exit 2 with TS2344, vitest still 2/2 green). FOUR risks measured DEAD rather than carried: the schema is byte-identical on CPython 3.12.13 and 3.13.13 (so the quality matrix cannot fight itself), openapi-typescript reruns are byte-identical and its banner carries no version string, the generated .d.ts is linted by eslint with 0 problems and type-checked inside the existing tsconfig.app.json with no config change, and ui/.gitattributes already forces LF on both new files. 17 ACs: five beyond the epic's blocks (the snapshot test, the schema.ts single-reader barrel, the tsc-gated type pins, a guard test that reads components.schemas keys out of the committed schema and bans any re-declared wire shape outside src/api/, and a codegen ban list) — and the epic's fifth block (openapi-typescript dev/CI-only) is ALREADY satisfied by c2-1's package-contract.test.ts, so it is re-verified rather than duplicated. Two flagged deviations from the epic's `git diff --exit-code` letter, per the c1-9/c2-1/c2-2 precedent. Story adds no route, model, endpoint, dependency or runtime fetch layer, and does not touch plugin/ unless Q1 lands (scripts/ and ui/ are not mirrored). Baselines re-measured: 1,742 passed / 1 skipped / 45 deselected Python, 59 tests / 7 files frontend. Previously: Story c2-2 CODE REVIEW → done. Three-layer bmad-code-review; auditor verdict 21/21 ACs met, dev-record bug fixes verified real. 2 decisions ruled by Brad: AC 5's second scoped .gitattributes line ACCEPTED; AC 17's browser-render half DEFERRED to the epic manual-testing checklist. 14 patches applied same-session, headliners: _SpaMount now declines non-http scopes (a WebSocket handshake to an unreserved path was reaching StaticFiles' `assert scope["type"] == "http"` and dying as a server error instead of the router's clean rejection); the CI SPA drift check gained a git ls-files non-vacuity guard (git status --porcelain reports nothing for gitignored paths, so an ignored tree would have passed vacuously while hatchling dropped the bundle from the wheel); mimetypes registration re-runs inside install_spa because mimetypes.init() discards add_type entries (and the hostile-database test now actually calls init() and proves the repair); conftest.keep_spa_mount_last re-installs the mount rather than moving it, so test apps re-derive reservations from the completed route table; extension-less misses under /assets/ are typed 404s not the index; the reserved-prefix pin is exact-set equality (guards over-reservation too); the /docs regression test asserts a Swagger marker (a shadowed route would also 200); mirror parity compares asset BYTES not just names; the plugin sentinel also requires a non-empty assets/; plus test-status asserts, _asset() prefers index-*, and ui/README gains the five-gate correction and a "which URLs the SPA may own" contract section for future client-route authors. 1 deferred (this very line's unbounded growth — pre-existing pattern, homed in deferred-work.md), 3 dismissed (case-sensitive prefix matching is standard HTTP semantics; the dotted-final-segment rule is Q1 as ruled; drift-log legibility is adequate). Suites 1,739 → 1,742 Python / 59 frontend; ruff, format, mypy both platforms, prettier all green; plugin mirror regenerated byte-identical. Previously: Story c2-2 IMPLEMENTED → review on branch feat/companion-c2-2-serve-spa. All 21 ACs met; the build output now lives at src/companion/app/static/, committed, mirrored into plugin/, served at / (AD-13, FR-01, SC-4). All four open questions were answered "as proposed" by Brad BEFORE implementation, so none surfaced mid-story. Python suite 1,684 → 1,739 passed / 1 skipped / 45 deselected; frontend 55 → 59; all nine gates green (ruff lint+format, mypy both platforms, pytest; eslint+stylelint, prettier, tsc -b, vitest, vite build). TWO DEVIATIONS, neither silent: (1) AC 12 ships `git status --porcelain` as ruled — and the deviation is now PROVEN rather than argued: the first scenario tried (edit App.tsx, rebuild) turned BOTH commands red because index.html changed too, so it did not justify anything; the case that actually separates them is a new emitted asset with no tracked file changing (a file added to ui/public/), where `git diff --exit-code` returns 0 and ships a stale bundle while porcelain shows the `??` entry. (2) AC 5 called for exactly ONE scoped .gitattributes line; TWO shipped, because one is measurably insufficient — the plugin mirror of the same bundle is compared byte-for-byte by the plugin drift check and by AC 15's test, and had no attribute. Measured by deleting both copies and letting git restore them (applying the core.autocrlf smudge filter exactly as a fresh clone does): src/ index.html 1093 bytes (w/lf) vs plugin/ index.html 1119 bytes (w/crlf) — 26 line endings apart, so AC 15 fails and the plugin drift check reports drift on an untouched tree, on any fresh WINDOWS clone. CI is ubuntu with autocrlf off, so CI could never have caught it. Still path-scoped: the repo-wide w/crlf count is 83 before and 83 after, so AC 5's actual concern (renormalising every tracked file, c2-1's reason for declining the file) is untouched. TWO REAL BUGS the story's analysis could not foresee, both found by running things rather than reading them: (a) FastAPI 0.140 wraps include_router results in an `_IncludedRouter` carrying NEITHER .path NOR .routes (its routes hang off .original_router, its prefix off .include_context), so the Decide-once-#1 reserved-prefix derivation found NOTHING from any included router — /health was never reserved, and every prefix c3-1 (/api) and c5-5 (/agent) will register the same way would have fallen through to the SPA index instead of the typed 404; `api` masked it because it is seeded. Fixed with a recursive _route_paths walk over all three nesting shapes, the private-attribute reads guarded by getattr and PINNED by a test whose failure message names the consequence, so a FastAPI upgrade goes red instead of quietly emptying the reservation. (b) A Mount("/") reports Match.FULL for every path and Starlette's router takes the first FULL and returns immediately — beating the Match.PARTIAL a real route reports on a method mismatch, which is exactly how Starlette produces 405 with the RFC-9110-mandated Allow header that errors.py deliberately forwards. POST /health was answering 405 with NO Allow, and once c5-5 adds a POST-only /agent/events a plain GET would have answered 404 instead of 405 Allow: POST — a silent downgrade of the c1-4 contract. Fixed with _SpaMount.matches() returning Match.NONE for reserved prefixes (handing those paths back to the router, which knows each route's real method set) plus Allow: GET, HEAD on the mount's own 405, which StaticFiles omits. This is the error-contract-enumeration standing agreement earning its keep. AC 10's mount-order guard also immediately caught 32 EXISTING tests in test_errors/test_deps/test_security that attach throwaway routes to a real build_app() with @app.get — i.e. below install_spa(app), the one thing main.py forbids — and were being served index.html instead of running; fixed in the tests via conftest.keep_spa_mount_last(), not by weakening the rule. Implementation choices worth carrying: html=False on StaticFiles removes the 404.html hazard (gotcha 6) ENTIRELY rather than commenting on it, since GET / reaches the index through the same explicit fallback rule every client route uses; cache headers ship here per Q2 (no-cache on index, immutable on hashed assets), closing the blank-page-after-git-pull that emptyOutDir creates. AC 17 done live from a fresh worktree with NO node_modules: uv built a venv, installed 81 packages, and every surface answered correctly (/, /decks/42, both hashed assets with correct content types, /health, /docs, /openapi.json, typed 404s for /api/unknown and a missing asset, 405 for POST, cache headers, ETag/304, and POST /health -> 405 allow: GET). The served bundle is byte-identical to the committed one. THE BROWSER RENDER ITSELF IS NOT CONFIRMED BY THE DEV — everything short of eyes on pixels is green; "the page paints" belongs on Brad's manual-testing checklist. Worktree, temp venv and the stale discovery file the killed test process left behind were all cleaned up. Also recorded: a self-inflicted vacuity caught and redone (a PowerShell Set-Location drift planted the AC 2 junk file at ui/src/companion/app/static/, a directory Vite never touches, so the first emptying proof was meaningless — redone with absolute paths on both sides), and AC 4's command needed adjusting because `git check-ignore $(git ls-files -o ...)` gets no arguments after `git add` and exits 128; it now runs over every path on disk, which is the stronger form. AC 16 needed no packaging change — all four bundle files are in the wheel with pyproject.toml untouched. Cross-platform build determinism is still UNVERIFIED locally by design: Brad builds on Windows/node 24, CI rebuilds on ubuntu/node 20 — the story says push early and let CI speak, and NOT to loosen the check if it fires. Previously: Story c2-2 contexted → ready-for-dev. The story that joins c2-1's ui/ toolchain to C1's FastAPI backend: build output moves from ui/dist into src/companion/app/static/, committed, mirrored into plugin/, served at / (AD-13, FR-01, SC-4). FIVE landmines measured at 9d47b24 and recorded in the story rather than left to be rediscovered: (1) Vite REFUSES to empty an outDir outside the project root and only warns ("Use --emptyOutDir to override", read from the installed chunks/node.js) — without an explicit emptyOutDir:true every rebuild accumulates dead hashed bundles that all ship; (2) emptyDir's skip list is exactly [".git"], so NO hand-written file can live in static/ (no .gitattributes, no README, no __init__.py — the first rebuild eats it), which is why the byte-determinism attribute goes in a new ROOT .gitattributes as a single scoped `src/companion/app/static/** -text` line (not `text eol=lf` — c2-5 puts .woff2 fonts in that tree); (3) the epic's `git diff --exit-code` drift check CANNOT see a stale bundle — a content-hash rename is one deletion (seen) plus one untracked addition (unseen) — so the check ships as `git status --porcelain`, the pattern the same AC sentence points at, flagged as a deliberate deviation per the c1-9/c2-1 precedent; (4) Starlette serves an unknown extension as text/plain (FileResponse: guess_type()[0] or "text/plain") and .woff2/.map resolve to None here, while .js is Windows-registry-dependent — a module script downgraded to text/plain blanks the page behind a 200, so the serving module registers its mimetypes explicitly; (5) a Mount at "/" shadows every route registered after it, silently returning index.html with a 200 for /api/decks — so install_spa(app) is last in build_app() and a guard test with an instructive failure message enforces it for c3-1/c5-2/c5-5. Two ACs added beyond the epic's five blocks: AC 9 pins the c1-4 typed error contract THROUGH the fallback (unknown /api path, missing asset, POST to a client route, and /health + /docs + app.openapi() all unchanged), and AC 10 is the mount-order guard. FOUR open questions homed with recommendations rather than left to surface mid-implementation (C1 retro item 2): Q1 the fallback rule (GET/HEAD + no extension + not a registered prefix, `api` reserved from the start), Q2 Cache-Control (no-cache index + immutable assets — emptyOutDir deletes the old assets, so a heuristically-cached index after a git pull is a blank page a reload does not fix), Q3 the root .gitattributes, Q4 the git status deviation. Cross-platform build determinism (Windows/node 24 commits vs ubuntu/node 20 rebuilds) is the one risk the dev cannot verify locally — the story says push early for a CI signal and NOT to loosen the check if it fires. Previously: Story c2-1 REVIEW ROUND 2 → done. Second 3-layer bmad-code-review over the final branch state: auditor verified all 17 round-1 patches present, 22/22 ACs met at the letter, 0 critical/high. 23 raw findings → 3 decisions (all ruled by Brad: ADD `npm run build` as a fifth CI step — AC 1's "bad lock dies in CI" was aspirational without it, a deliberate AC-15 deviation by ruling; WIDEN the DR46 ban fully — case-insensitive `/^outline$/i` keys + token-anywhere value regexes + outline-color:transparent banned, fixture now proves 10 declarations, and Prettier itself was measured lowercasing CSS property names so the uppercase-property axis is closed by the format gate; GUARD TEST for the three gate-geometry file-class holes — new tests/gate-geometry.test.ts bans test files outside the two vitest roots, .test.tsx under tests/, and .jsx/.mjs/.cjs anywhere) + 8 patches, all applied same-session: proxy anchors now admit query strings (^/api(?:[/?]|$), ^/health(?:\?|$) — Vite regex-matches the FULL req.url, so /health?verbose=1 was being served SPA HTML with a 200; round-trip + regex tests extended), stylelint gate now ignores coverage/** (the one tool that didn't — first local vitest --coverage made npm run lint red on istanbul CSS), resolveBackendPort's not-an-integer warning no longer overclaims (Python int() accepts Unicode digits that \d rejects), a11y clean-fixture assertion filtered to the two gate rules (was coupled to every future rule), @types/node relocked 24→20.19.43 to match the Node floor (new package.json "//" note), README dev loop says npm ci not npm install, the stale .stylelintignore fixture comment now names the real CLI --ignore-pattern mechanism and the trap, and the Five/six miscount comment fixed. 1 dismissed (blank-but-set COMPANION_PORT no-warn is deliberate + zero-divergence). TWO EMERGENT FIXES found while patching, both pre-existing: vite.close() awaits a cold-cache dep-optimizer scan (~40s vs 10s hook timeout; any lockfile change would fail the next local npm test unkillably — fixed with optimizeDeps noDiscovery in the round-trip tests) and all round-trip Vite servers shared origin 5173 (port:0 is falsy to Vite; undici pools sockets by origin → intermittent ECONNRESET — fixed with a real ephemeral port per server + strictPort). Suite 50 → 55 tests; lint/format/typecheck/test/build all green locally; 6 consecutive round-trip runs green incl. cold cache. Previously: Story c2-1 REVIEW PATCHES APPLIED → review. All 17 patches applied + the 1 decision already dismissed; 0 findings left open. Frontend suite 32 → 50 tests, all five gates green (lint/format/typecheck/test/build), Python side untouched and still 1,684 passed / 1 skipped / 45 deselected, AC 21 scope proof still empty. AC 9's partial is now CLOSED — the stylelint gate covers all of ui/, not just src/. Five behavioural changes, each re-proved: (a) DR46 ban widened from 2 to 6 spellings (outline none|0|0px, outline-style none, outline-width 0|0px) — fixture reports exactly 6 errors, clean fixture still silent; (b) proxy keys are now ANCHORED REGEXES (^/api(?:/|$), ^/health$) not bare prefixes, because Vite treats a string key as a prefix — new round-trip pair proves /health + /api/deck/1 reach the backend while /healthcheck + /api-docs do not, and a live two-process run confirmed /healthcheck is served by Vite as HTML while the backend would have 404'd it; (c) resolveBackendPort now mirrors Python int() via regex instead of Number(), so 0x50/1e3 no longer resolve to 80/1000 (split-brain ports) and 8_080 is accepted as the backend accepts it, with every discarded value warned and port 0 given its own ephemeral-port explanation; (d) stylelint fixtures excluded via CLI --ignore-pattern NOT .stylelintignore, deliberately — an ignore file would also be honoured by the Node API and silently neuter lint-gates.test.ts; (e) .npmrc engine-strict=true, verified safe for CI rather than assumed: all 331 installed packages declaring engines.node satisfy node 20.19.0/20.19.5/20.99.0 (checked with the real semver lib — a hand-rolled range parser first gave 12 false positives on ranges like ">= 8") and npm ci still exits 0. R1 re-verified live after the proxy change: proxied /health still 200 with the real instance_id. Previously: Story c2-1 CODE-REVIEWED: 3-layer bmad-code-review (Blind Hunter + Edge Case Hunter + Acceptance Auditor) on diff 50dddc3..HEAD. Auditor re-ran every provable Dev Agent Record claim — all reproduced; 21/22 ACs met, AC 9 partial (stylelint glob src/ vs ui/). 0 critical/high. 26 raw findings → 4 decisions (all ruled by Brad: widen the DR46 outline ban now; replace the Bolt-lookalike favicon with a placeholder; add ui/.npmrc engine-strict=true; frontend CI job stays unconditional — dismissed) + 17 patches left as ACTION ITEMS in the story's Review Findings section (notable: react-hooks rules only cover *.tsx so .ts custom hooks escape rules-of-hooks; tests/**/*.test.tsx matches neither vitest project — silent test loss; URL.pathname→C:\C:\ in devProxyRoundTrip root; resolveBackendPort diverges from backend parser incl. port 0). 4 dismissed as noise. Patches to be applied in a follow-up session, then re-review/commit. Previously: Story c2-1 IMPLEMENTED → review on branch feat/companion-c2-1-frontend-scaffold. All 22 ACs met; ui/ scaffolded with eslint+stylelint+prettier+tsc -b+vitest gating in CI (32 frontend tests), Python suite unchanged at 1,684 passed / 1 skipped / 45 deselected. Lock verified at lockfileVersion 3 / 20 platform bindings / typescript@5.9.3 / eslint@9.39.5 exactly as predicted. THREE DEVIATIONS, none silent: (1) AC 16 ships `mypy src/ --platform win32`, NOT the epic's `--platform linux` — CI is ubuntu where the bare run IS the linux run, so the epic's letter was a no-op; proved by AC 17's deliberate break. (2) A THIRD load-bearing pin was required that no planning doc predicted: @testing-library/jest-dom ~6.9.1, because latest (7.0.0) and 6.10.0 both declare engines.node >=22, above the >=20.19.0 floor and CI's node 20 — and unlike the other two pins npm does NOT fail on it (engines is advisory), so it would have broken only on the CI job. The Spine's stack table now lags reality by two entries (eslint ^9 + this). (3) "strict": true added to both tsconfigs — create-vite@9's react-ts template omits it, and a non-strict tsc is not the NFR-07 equivalent of mypy --strict. Also closed: c1-9 mypy-gate deferred entry, and incidentally the C1 COMPANION_PORT live-confirmation residual (Task 10 ran it at 9125 end to end). New deferred entries: 8 dev-only npm audit highs NOT force-fixed (the fix downgrades eslint-plugin-jsx-a11y across a major and would delete the UX-DR47 gate), the Node>=20 → >=20.19.0 copy fix for c8-4, the third pin, and a pre-existing order-flake in test_list_decks_with_strategy_field found at Task 0 (created_at tie with no secondary sort key; passed 5/5 isolated and on re-run, unrelated to this story). Previously: Story c2-1 contexted → ready-for-dev; epic-c2 in-progress. Frontend scaffold carries R1 changeOrigin + C1 retro action item 3 (--platform mypy gate). Four landmines measured and recorded in the story: eslint@10 ERESOLVE against eslint-plugin-jsx-a11y (a SECOND load-bearing pin, eslint ^9, not in the Spine), create-vite@9 now ships oxlint not eslint, prettier --check vs core.autocrlf=true needs ui/.gitattributes, and @eslint/css has no declaration-value rule so UX-DR46 goes to stylelint. All 5 open questions asked and ANSWERED before implementation — rulings B1-B5 in the story: B1 stylelint owns the CSS rules, B2 openapi-typescript installed in c2-1 (pins the lock to typescript@5.9.3 so c2-3 never has to downgrade), B3 ESLint stays pinned ^9 and oxlint is declined, B4 frontend gates are CI-only, B5 one CI job on node 20 with no matrix. No open questions remain. Previously: Epic C1 retrospective done — epic-c1-retro-2026-07-26.md. 9/9 stories, PRs #9-#16, suite 1,310 → 1,683 passed / 1 skipped / 45 deselected, ~77 review patches, 0 Critical/Major, 5 of 13 deferrals closed in-epic. Epic-7 items 1+2 closed and promoted to standing agreements; non-vacuity pairing promoted as a new one. 3 rulings: R1 Vite dev proxy needs changeOrigin (homed on c2-1, closes c1-5 OQ2), R2 /docs + /openapi.json stay enabled as a keep-decision (closes c1-2 OQ2), R3 integration PR feat/companion-app → master now, merge != release. 6 action items below; 8-item manual-testing checklist in the retro doc. Epic C2 unblocked)
project: Artificial-Planeswalker
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1: MCP Server & Core Tool Surface
  epic-1: done
  1-1-repository-restructure-dependency-reshape: done
  1-2-sqlite-connectionfactory-with-wal-extension-loading: done
  1-3-fastmcp-server-with-card-lookup-bug-report: done
  1-4-advanced-card-search-tool: done
  1-5-deck-management-tools: done
  1-6-deck-analysis-tools: done
  epic-1-retrospective: done
  # Pre-Epic-2 tech-debt prep gate (from epic-1-retro-2026-06-21.md, action items 1-3 + optional wins)
  epic-1-tech-debt-prep-gate: done

  # Epic 2: Semantic Card Search (RAG)
  epic-2: done
  2-1-embedder-port-fastembed-singleton-persistent-cache: done
  2-2-card-vec-schema-with-metadata-columns: done
  2-3-card-embedding-index-builder-idempotent-incremental: done
  2-4-semantic-search-cards-tool-hybrid-query: done
  2-5-find-similar-cards-tool: done
  2-6-rag-sanity-eval: done
  epic-2-retrospective: done

  # Pre-Epic-3 tech-debt gate (from epic-2-retro §7: G1/G2/G3 + nullability audit + D1 docs)
  pre-epic-3-targeted-gate: done

  # Epic 3: Deckbuilding Skills Suite
  epic-3: done
  3-1-magic-deckbuilding-orchestrator-skill: done
  3-2-synergy-discovery-skill: done
  3-3-mana-curve-analysis-skill: done
  3-4-format-legality-skill: done
  epic-3-retrospective: done

  # Pre-Phase-2 Gate (from epic-3-retro-2026-06-27.md §6) — all green before Arena import/export build starts
  # G-SD2a: validate_deck lowercase-format .lower() [done, deck_validator.py:302]
  #         + valid-key guard [done 2026-07-11 fix/pre-phase-2-gate — _KNOWN_FORMATS + unknown_format violation]
  # G-SD2b: detect_synergies '//' double-faced-card token fix [done 2026-07-11 fix/pre-phase-2-gate — per-face parse in _extract_creature_types]
  # G-R1: Brad live-smokes all four skills end-to-end through Claude Code (Phase-1 acceptance) [done 2026-07-11 — Brad hands-on accepted]
  pre-phase-2-gate: done  # all three green: G-SD2a/G-SD2b (code) + G-R1 (Brad acceptance) — Phase 2 import/export unblocked

  # ============================================================================
  # DECK POWER-LEVEL ASSESSMENT
  # Source epics: _bmad-output/planning-artifacts/epics-deck-power-assessment.md
  # Readiness:    _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-11.md (READY; 0 critical / 0 major)
  #
  # Feature epics are renumbered 4-7 here to avoid key collision with the
  # completed Phase-1 MCP-pivot Epics 1-3 above. Mapping (feature → sprint):
  #   feature Epic 1 (Game Changer card data)      → epic-4
  #   feature Epic 2 (Deterministic scoring core)  → epic-5
  #   feature Epic 3 (Combo detection)             → epic-6
  #   feature Epic 4 (assess_deck_power tool)      → epic-7
  # When running create-story, use the epic-4..7 keys below (NOT the epic
  # file's raw 1-4). Story titles are unchanged from the epic file.
  #
  # ORDERING CONSTRAINT: 5-6 (ComboRecord) must precede 6-2 / 6-3
  #   (the combo client + cache consume the record shape the core defines).
  # ============================================================================

  # Epic 4 (feature Epic 1): Game Changer card data
  epic-4: in-progress
  4-1-add-the-nullable-game-changer-field-end-to-end: done
  4-2-migrate-and-backfill-existing-databases: done
  epic-4-retrospective: done  # epic-4-retro-2026-07-12.md; game_changer backfilled live (53 TRUE / 38180 FALSE / 0 NULL)

  # Epic 5 (feature Epic 2): Deterministic scoring core
  epic-5: done
  5-1-compose-the-calibration-benchmark-set: done
  5-2-format-profile-frozen-data-module: done
  5-3-shared-oracle-text-classifiers: done
  5-4-mana-base-curve-signals: done
  5-5-consistency-interaction-structural-coverage-signals: done
  5-6-combo-record-combo-bracket-mapping: done
  5-7-dimension-vector-commander-bracket-floor-cedh-candidacy: done
  5-8-for-format-aggregate-tier-label-standard-fork-confidence-vocabulary: done
  5-9-pure-score-entry-point-benchmark-validation: done
  epic-5-retrospective: done  # epic-5-retro-2026-07-15.md; benchmark gate green (1136 full suite), branch stays feat/deck-power-assessment until feature complete ("experimental" release)

  # Pre-Epic-6 Importer Gate (from epic-5-retro-2026-07-15.md) — blocks Epic 6 story creation.
  # G-I1: upsert keyed by oracle_id / stale-printing reconcile + oracle-level field propagation
  # G-I2: import errors surfaced per-card (name + exception class) in the import summary
  # G-I3: import-path regression test (oracle_id dedup + game_changer propagation survive re-import)
  #       — absorbs epic-4 action item 3 (persisted nullable-bool round-trip test)
  pre-epic-6-importer-gate: done  # spec-pre-epic-6-importer-gate.md; live-DB acceptance 2026-07-15: 38,261 rows (was 51,189), 0 dup oracle_ids, 0 game_changer NULL (53 TRUE), 241 deck refs repointed / 0 dangling; 33 rejects all reversible_card missing top-level type_line (fix = own spec, see deferred-work) — Epic 6 unblocked

  # Epic 6 (feature Epic 3): Commander identity & local combo snapshot
  # (re-scoped 2026-07-12 per sprint-change-proposal-2026-07-12.md — bulk snapshot replaces live client/cache/key)
  epic-6: done
  6-1-commander-flag-end-to-end: done
  6-2-spellbook-bulk-combo-snapshot-import: done
  6-3-combo-snapshot-repository: done
  epic-6-retrospective: done  # epic-6-retro-2026-07-17.md; benchmark re-run w/ real snapshot data = zero divergences; epic-5 items 4/6/7/8 closed, item 5 escalated to gate below

  # Pre-Epic-7 Real-Deck Gate (from epic-6-retro-2026-07-17.md) — blocks Epic 7 story creation.
  # G-R2: real-deck sanity pass — throwaway harness loads Sathias's real saved decks (live DB),
  #       pulls variants via ComboSnapshotRepository.get_variants_for_names (the exact Epic 7 path),
  #       runs pure score(); Sathias compares tier/Bracket/cEDH outputs against his own judgment.
  #       Done when accepted as plausible OR each divergence logged as a named Epic 7 calibration
  #       input (CEDH_TUTOR_MIN=3 caveat carries over). Closes epic-5 action item 5.
  pre-epic-7-real-deck-gate: done  # CLOSED 2026-07-17 (commit e2b03ff): all 20 decks accepted as plausible wholesale; calibration deferred to Epic 7 — named inputs in pre-epic-7-real-deck-gate-report-2026-07-17.md §Cross-deck observations. Epic 7 unblocked.

  # Epic 7 (feature Epic 4): The assess_deck_power + compare_deck_power MCP tools & deterministic output
  epic-7: done  # all 5 stories done 2026-07-18; final epic of the deck-power-assessment roadmap
  7-1-register-the-async-tool-load-deck-resolve-format: done  # code review 2026-07-17: all ACs MET; 1 patch applied (unresolved_count comment), empty-mainboard ok accepted, orphan-crash deferred to data-layer story
  7-2-combo-provisioning-the-degradation-ladder: done  # code review 2026-07-17: all ACs MET; 1 patch applied (combo-provisioning DB reads brought under DatabaseError→error contract + regression test), 2 deferred (transient-lock mislabel → data-layer; almost_included "matched" wording → 7.3), 7 dismissed

  7-3-assess-deck-power-result-assembly-deterministic-serialization-human-summary: done  # code review 2026-07-17: all 9 ACs MET (auditor: zero violations); 3 low patches applied (bracket→Literal[2,3,4]|None per FR18 decision, "one card away" grammar, format→Literal), 7 dismissed; 1,313 tests green, mypy/ruff clean
  7-4-end-to-end-tool-test-determinism-diff-regression: done
  7-5-compare-deck-power-tool: done  # code review 2026-07-18: all 9 ACs MET (auditor: zero violations); 2 decision-needed accepted as-spec'd, 2 low patches applied (asymmetric both-fail token test + Deck A/B summary casing), 10 dismissed; compare suite 22 green, mypy/ruff clean
  epic-7-retrospective: done  # epic-7-retro-2026-07-18.md; ship-first ruling (merge → manual-testing acceptance → "experimental" release); calibration gated below, non-blocking

  # Post-Epic-7 Calibration Gate (from epic-7-retro-2026-07-18.md) — NON-BLOCKING for release
  # (ship-first ruling 2026-07-18). Homes the five named G-R2 calibration inputs
  # (pre-epic-7-real-deck-gate-report-2026-07-17.md §Cross-deck observations) + anything
  # Sathias's post-PR comprehensive manual testing surfaces:
  #   C1: card_advantage saturation at 80
  #   C2: interaction railing 0/100
  #   C3: Brawl mana_efficiency floor (brawl-family currently unsupported_format by 7.1 decision)
  #   C4: almost_included dominance of combo_potential
  #   C5: format-blind almost_included inflation (missing piece not format-legal; deferred-work.md)
  # CEDH_TUTOR_MIN=3 caveat carries over. Done when each input is tuned, or closed with a
  # documented keep-decision (the 5.7 card_advantage-ceiling precedent).
  post-epic-7-calibration-gate: backlog

  # Data-layer orphan handling (epic-7 retro action item 3; deferred from 7.1 review):
  # an orphaned deck_cards row (FK enforcement off) raises pydantic.ValidationError inside
  # get_deck_with_cards — shared ungraceful crash across analyze_mana_curve / detect_synergies /
  # validate_deck / assess_deck_power; also the only live source that could ever make
  # cards_unresolved fire. Includes the 7.2-deferred transient-lock mislabel
  # (combo_snapshot.py broad OperationalError → "unavailable") if the repo catch is narrowed.
  data-layer-orphan-handling: backlog

  # ============================================================================
  # COMPANION APP  (added 2026-07-25)
  # Source epics: _bmad-output/planning-artifacts/epics-companion-app.md
  #   (NOT epics.md — that file is the MCP pivot. This is a feature-level epic
  #    set layered onto the brownfield repo: src/companion/, ui/, new MCP tools.)
  # Inputs: PRD 2026-07-22 (+addendum) · UX Voltglass 2026-07-25 · ARCHITECTURE-SPINE 2026-07-25 (16 ADs)
  #
  # KEY SCHEME — c-prefix, epic-file numbering PRESERVED (Brad's call 2026-07-25).
  #   epic file "Epic 4"    → epic-c4
  #   epic file "Story 4.6" → c4-6-<kebab-title>
  # Chosen over renumbering to 8..17 so the 76 story references never need a
  # +7 translation. When running create-story / dev-story, use the c-keys below.
  #
  # WORK BRANCH: feat/companion-app. Each dev-story starts a NEW branch off it
  # (feat/companion-<story-slug>); story PRs target feat/companion-app; one
  # integration PR to master at the end.
  #
  # PHASING: Epics c1-c8 are Phase 1 (MVP, 70 stories). Epics c9-c10 are Phase 2
  # (6 stories). Phase 3 (FR-16, FR-21, Tauri, UI-initiated edits) is OUT OF SCOPE
  # for this breakdown — the last needs its own brief per AD-2.
  #
  # DEPENDENCY ORDER (from the epic file's "Depends on" lines):
  #   c1 ← nothing         c2 ← c1        c3 ← c1 (independent of c2)
  #   c4 ← c2, c3          c5 ← c2, c3    c6 ← c4, c5
  #   c7 ← c6              c8 ← c7        c9 ← c6        c10 ← c7
  #   c3 is the SC-4 close; c4 makes SC-5 answerable; c6 closes SC-1 + SC-3; c7 closes SC-2.
  #
  # SUCCESS CRITERIA (human gates):
  #   SC-5 = Brad's own judgement against DESIGN.md + EXPERIENCE.md (UX-DR49) — story c8-6.
  #          Cannot be automated or delegated.
  #
  # KNOWN OPEN ITEM: FR-18's session-history home (nav extension vs per-view header
  # strip) is undecided. Blocks c10-2 ONLY — no Phase-1 story waits on it.
  # ============================================================================

  # Epic C1: Launch the Companion
  # FR-01, FR-14, FR-22 (backend half) · NFR-01, NFR-02 (both CI boundary tests), NFR-07
  # AD-1/2/3/4/10/14/15/16 · depends on nothing
  # NOTE: EPIC-SPLIT calls c1-1's two boundary tests the highest-leverage stories in the
  # whole feature — they land BEFORE the code they guard, so AD-2/AD-3 are structural not aspirational.
  epic-c1: done  # 9/9 2026-07-26; PRs #9-#16 merged into feat/companion-app (integration PR to master = R3)
  c1-1-companion-package-skeleton-with-ci-enforced-import-boundaries: done
  c1-2-side-effect-free-asgi-app-with-a-lifespan-and-a-health-endpoint: done
  c1-3-port-selection-with-ephemeral-fallback-and-a-printed-launch-url: done
  c1-4-typed-rest-error-contract-with-closed-reason-tokens: done
  c1-5-localhost-only-security-envelope-host-validation-and-cors: done
  c1-6-lazy-database-engine-so-a-fresh-install-starts-instead-of-erroring: done
  c1-7-discovery-file-as-the-sole-rendezvous: done
  c1-8-single-instance-enforcement-with-verified-identity: done
  c1-9-one-console-script-that-dispatches-without-disturbing-the-mcp-server: done
  epic-c1-retrospective: done  # epic-c1-retro-2026-07-26.md; 3 rulings (R1 changeOrigin → c2-1, R2 /docs keep-decision, R3 integration PR now), 6 action items, 8-item manual-testing checklist

  # Epic C2: The Glass — Foundation, Identity & Honest States
  # FR-20, FR-22 (state-panel surface + copy) · NFR-03, NFR-06, NFR-07, NFR-08 · AD-12, AD-13
  # depends on c1
  # NOTE: c2-1 carries the greenfield ui/ scaffold — the only new toolchain in the feature.
  # The TypeScript >=5.9,<6.1 pin is load-bearing (typescript-eslint peer range <6.1.0) and belongs to it.
  # ALSO load-bearing, discovered while contexting c2-1 (2026-07-26): an `eslint ^9` pin. ESLint's
  # `latest` is 10.8.0 and eslint-plugin-jsx-a11y@6.10.2 (latest, no newer) peers at `^9` — measured
  # `npm ci` ERESOLVE, not a warning. Not in the Spine's stack table; c2-1 adds it.
  epic-c2: in-progress
  c2-1-frontend-scaffold-with-the-full-quality-gate-from-the-first-commit: done  # review round 2 (2026-07-26): 3 rulings + 11 patches applied, suite 50→55, five gates green incl. new CI build step
  c2-2-the-backend-serves-the-built-spa-as-a-committed-artifact: done  # code review 2026-07-26: 2 decisions ruled + 14 patches applied, suites 1,742/59, nine gates green
  c2-3-typescript-types-generated-from-the-backends-own-openapi-drift-checked-in-ci: review  # implemented 2026-07-27: all 17 ACs, Q1-Q4 answered as proposed before Task 0, suites 1,751 Python / 71 frontend
  c2-4-the-voltglass-token-layer: backlog
  c2-5-self-hosted-space-grotesk-with-offline-parity-and-tabular-numerals: backlog
  c2-6-the-two-column-application-shell: backlog
  c2-7-presentation-only-primitives-panel-badge-statchip-group-header: backlog
  c2-8-manapip-and-manacost-with-complete-scryfall-cost-parsing: backlog
  c2-9-the-shared-state-panel-and-every-system-state-message: backlog
  c2-10-footer-attribution-on-every-surface: backlog
  epic-c2-retrospective: optional

  # Epic C3: Deck Data & Card Imagery on Tap
  # FR-02, FR-03, FR-04, FR-07 (backend state slot + both transports), FR-22 (closes SC-4)
  # NFR-02 (WAL reads), NFR-06, NFR-08, NFR-09, CM-2 · AD-11, AD-16 · depends on c1 (independent of c2)
  # NOTE: only externally-paced work in the feature. A cold 100-card deck at ~12 MB / ~10 s to
  # fully paint is an EXPECTED OBSERVATION in acceptance, not a defect.
  epic-c3: backlog
  c3-1-deck-list-and-deck-detail-endpoints: backlog
  c3-2-card-detail-endpoint: backlog
  c3-3-format-check-endpoint-over-the-existing-validators: backlog
  c3-4-the-active-deck-readable-by-the-glass-settable-by-the-agent: backlog
  c3-5-card-image-endpoint-with-face-resolution-and-a-defined-parameter-contract: backlog
  c3-6-paced-concurrency-capped-cdn-fetching-at-one-global-choke-point: backlog
  c3-7-sharded-atomically-written-disk-cache: backlog
  c3-8-distinguishable-failure-signalling-and-negative-caching: backlog
  c3-9-fresh-install-guides-instead-of-erroring-and-comes-alive-on-its-own: backlog
  epic-c3-retrospective: optional

  # Epic C4: The Deck on the Glass
  # FR-05, FR-17, FR-19 · NFR-05 (1 s deck render, warm cache) · AD-11, AD-12, AD-16 · depends on c2, c3
  # NOTE: largest UX surface in the feature; first epic where SC-5 becomes answerable.
  # c4-6 (DFC flip control, UX-DR15) is the densest single component in the whole feature.
  epic-c4: backlog
  c4-1-a-single-card-hydration-cache-with-in-flight-deduping: backlog
  c4-2-deck-state-bootstrap-and-the-type-grouped-decklist: backlog
  c4-3-card-placeholders-named-unknown-and-loading-wells: backlog
  c4-4-card-tile-and-the-card-art-grid: backlog
  c4-5-persistent-card-detail-panel-with-transient-and-pinned-inspection: backlog
  c4-6-double-faced-card-flip-control: backlog
  c4-7-deck-list-panel: backlog
  c4-8-mana-curve-panel: backlog
  c4-9-colour-distribution-panel: backlog
  c4-10-format-check-panel: backlog
  c4-11-keyboard-floor-skip-link-tab-order-and-focus-management: backlog
  c4-12-empty-deck-state-and-the-cold-open-render-budget: backlog
  epic-c4-retrospective: optional

  # Epic C5: The Agent's Channel
  # FR-06, FR-13 (ID-only contract) · NFR-01 (ticket, Origin, token separation), NFR-03, NFR-04
  # AD-5, AD-6, AD-7 · depends on c2, c3
  # NOTE: all four payload shapes are frozen HERE in Phase 1 (c5-1), not added incrementally —
  # the discriminated union needs every member; a late change ripples through the committed
  # .d.ts and both mirrored plugin bundles. This is why Phase 2 is only 6 stories.
  # c5-8 is the ONE integration-marked real-socket test in the feature (AD-10).
  epic-c5: backlog
  c5-1-the-event-envelope-and-every-per-kind-payload-contract: backlog
  c5-2-same-origin-session-endpoint-minting-single-use-websocket-tickets: backlog
  c5-3-authenticated-websocket-upgrade-with-host-and-origin-validation: backlog
  c5-4-broadcast-to-every-connected-client: backlog
  c5-5-token-authenticated-event-ingest-that-reports-who-is-listening: backlog
  c5-6-client-reconnection-with-backoff-and-a-fresh-ticket-per-attempt: backlog
  c5-7-connection-pill: backlog
  c5-8-the-one-real-socket-integration-test: backlog
  epic-c5-retrospective: optional

  # Epic C6: The Agent Pushes to the Glass
  # FR-07 (MCP tool + deck-existence validation), FR-08, FR-12 · NFR-05 (250 ms push)
  # AD-8 · depends on c4, c5 · closes SC-1 and SC-3
  epic-c6: backlog
  c6-1-leaf-client-with-health-verification-retry-once-and-the-closed-outcome-vocabulary: backlog
  c6-2-companion-set-active-deck-the-agent-chooses-what-the-glass-shows: backlog
  c6-3-the-glass-follows-the-agents-active-deck-choice: backlog
  c6-4-companion-show-suggestions-the-agents-first-push: backlog
  c6-5-agent-view-shell-with-focus-management-and-dismissal: backlog
  c6-6-a-push-opens-its-view-and-a-repeat-push-replaces-it-in-place: backlog
  c6-7-suggestions-view: backlog
  c6-8-agent-views-nav-unread-markers-re-open-and-kind-switching: backlog
  c6-9-degradation-with-the-app-closed-and-the-250-ms-push-budget: backlog
  epic-c6-retrospective: optional

  # Epic C7: The Deck Updates Itself
  # FR-11, FR-12 (notifier swallow) · NFR-04 (refetch coalescing) · AD-9 · depends on c6 · closes SC-2
  # NOTE: AD-9 — "fire-and-forget" means a bounded-timeout await (~1 s), NOT a detached task.
  # asyncio.create_task is BANNED on this path. Emission is always after commit, never inside the txn.
  epic-c7: backlog
  c7-1-one-shared-notifier-with-a-bounded-await-and-no-detached-tasks: backlog
  c7-2-every-deck-mutation-tool-emits-after-its-transaction-commits: backlog
  c7-3-the-glass-refetches-on-deck-changed-coalesced-and-latest-wins: backlog
  c7-4-refetch-never-tears-down-whats-on-screen: backlog
  c7-5-the-change-is-announced-once-and-motion-is-never-the-only-signal: backlog
  c7-6-deck-deletion-and-agent-views-during-a-refetch: backlog
  c7-7-the-loop-closes-uj-1-end-to-end: backlog
  epic-c7-retrospective: optional

  # Epic C8: Release Readiness
  # NFR-08 (release notice), NFR-09 (README + uninstall notes) · AD-15 · depends on c7
  # NOTE: c8-3 owns folding the six story-writing gaps + the four confirmed UX rulings back
  # into the PRD, plus the three PRD amendments owed (NFR-02 mode=ro, FR-14 path, FR-04 layout).
  # c8-6 is the SC-5 human gate — Brad only, cannot be automated or delegated.
  epic-c8: backlog
  c8-1-deprecate-view-deck-and-freeze-src-viewer: backlog
  c8-2-image-cache-stewardship-documented-location-inspection-and-removal: backlog
  c8-3-reconcile-the-prd-with-what-was-built: backlog
  c8-4-release-documentation-for-the-companion-app: backlog
  c8-5-plugin-distribution-parity: backlog
  c8-6-the-sc-5-gate: backlog
  epic-c8-retrospective: optional

  # Epic C9: The Remaining Push Kinds  *(Phase 2)*
  # FR-09, FR-10, FR-23 · UX-DR25/26/27 · depends on c6
  # NOTE: cheap because c5-1 already froze all four payload shapes — each story is tool + view only.
  epic-c9: backlog
  c9-1-proposed-swaps-tool-and-view: backlog
  c9-2-tier-lists-tool-and-view: backlog
  c9-3-card-groups-tool-and-view: backlog
  epic-c9-retrospective: optional

  # Epic C10: Session History, Status Detail & Performance Polish  *(Phase 2)*
  # FR-15, FR-18 · NFR-05 hardening · depends on c7
  # BLOCKED ITEM: c10-2 waits on the FR-18 session-history home decision (extend the nav,
  # or a strip inside each view's header) — the one open UX item carried into story work.
  epic-c10: backlog
  c10-1-connection-pill-status-detail: backlog
  c10-2-session-history: backlog
  c10-3-measure-the-latency-budgets-and-close-the-gaps: backlog
  epic-c10-retrospective: optional

# Action items carried out of retrospectives (see epic-N-retro-*.md for full context)
action_items:
  - epic: 4
    action: "'end-to-end field' story checklist must grep ALL four write-path touch-points, incl. importer.py::_insert_batch card_dict + on_conflict_do_update set_ mapping (not just model/schema/transformer)"
    owner: "Brad"
    status: done  # closed 2026-07-17: first real exercise was story 6.1 AC 6 (11-path deck_cards table) — passed; successor discipline (construction-site enumeration) logged as epic-6 item
  - epic: 4
    action: "do not assume build-plugin-sync auto-mirrors: .git/hooks/pre-commit is absent in this checkout, so after any src/ commit run 'uv run python -m scripts.build_plugin' + diff-check the plugin mirror (or 'pre-commit install')"
    owner: "Brad"
    status: done  # closed mid-Epic-5: hook installed as of 88b1e66 (story 5.5); verified present at epic-5 retro
  - epic: 4
    action: "add a persisted-SQLite round-trip test proving a nullable-bool field (game_changer=False) survives DB write/read — the gap that let the importer.py omission through; also covers the power/toughness precedent"
    owner: "Brad"
    status: done  # closed via epic-5 G-I3 (import-path regression tests landed in pre-epic-6-importer-gate, 2026-07-15)
  - epic: 5
    action: "G-I1: stop stale-printing accumulation — key the upsert by oracle_id (or reconcile stale printings, minding deck_cards FKs) and propagate oracle-level fields (game_changer) across same-oracle_id rows; done when a live refresh leaves 0 stale duplicates and 0 game_changer NULLs"
    owner: "Sathias"
    status: done  # 2026-07-15 live acceptance: 0 dup oracle_ids, 0 NULLs, 12,971 stale rows removed
  - epic: 5
    action: "G-I2: surface silent import errors — failing cards listed in the import summary with name + exception class, counted against a stale-rows-remaining warning"
    owner: "Sathias"
    status: done  # 2026-07-15: 33 rejects listed with reasons (all reversible_card type_line) on CLI + MCP
  - epic: 5
    action: "G-I3: committed import-path regression test proving oracle_id dedup + game_changer propagation survive a re-import (absorbs/closes epic-4 round-trip-test item)"
    owner: "Sathias"
    status: done  # 2026-07-15: two-snapshot e2e suite in test_scryfall_import_e2e.py + test_reconcile_plan.py
  - epic: 5
    action: "story-start state verification: any story whose notes assert live-DB/environment state must open with the cheap query that proves it (the 5.9 'imported AND backfilled' saga was one SELECT away from caught)"
    owner: "Sathias"
    status: done  # 2026-07-17: ran as Task 0 in all three Epic 6 stories — caught the httpx decoding trap (6.2) and a baseline off-by-one (6.3); promoted to standing team agreement (epic-6 retro)
  - epic: 5
    action: "real-deck sanity pass: throwaway harness runs pure score() over Sathias's real saved decks (live-DB resolution), tiers compared against his own judgment — early validity signal, ideally before Epic 7 tuning ossifies"
    owner: "Sathias"
    status: open  # escalated 2026-07-17 (epic-6 retro) to the blocking pre-epic-7-real-deck-gate (G-R2) — see gate key above Epic 7
  - epic: 5
    action: "story 6.1 must run the epic-4 end-to-end-field checklist: enumerate every deck_cards write path (add_card_to_deck, import_decklist, any upsert column list) before claiming end-to-end"
    owner: "Sathias"
    status: done  # 2026-07-17: story 6.1 AC 6 enumerated all 11 deck_cards write paths with per-path dispositions; re-verified against the final diff
  - epic: 5
    action: "story 6.2 must reuse _name_keys (src/logic/assessment/combos.py) for Spellbook->Scryfall name normalization — the DFC front-face hazard bit stories 5.3, 5.6 and 5.9"
    owner: "Sathias"
    status: done  # 2026-07-16 story 6.2: _name_keys relocated to src/data/schemas/combo.py as public name_keys(); importer + matcher share it (matcher tests passed untouched)
  - epic: 5
    action: "after 6.2/6.3 land, re-run the benchmark swapping 5.9's hand-built cEDH combo variants for real snapshot data (candidacy also depends on CEDH_TUTOR_MIN=3)"
    owner: "Sathias"
    status: done  # 2026-07-16 story 6.3 Task 5: all 6 Commander benchmark entries met expectations with real snapshot data — zero divergences, no retuning
  - epic: 6
    action: "G-R2 real-deck sanity gate (blocks Epic 7 story creation): harness loads Sathias's real saved decks from the live DB, pulls variants via ComboSnapshotRepository.get_variants_for_names, runs pure score(); Sathias compares tier/Bracket/cEDH outputs against his own judgment — done when accepted as plausible or each divergence is logged as a named Epic 7 calibration input. Closes epic-5 real-deck item"
    owner: "Sathias"
    status: done  # closed 2026-07-17 (commit e2b03ff): all 20 decks accepted wholesale; cross-deck observations stand as the named Epic 7 calibration inputs (gate report §Cross-deck observations)
  - epic: 6
    action: "construction-site enumeration: when a field threads through multiple constructors, grep EVERY construction site — including error/fallback-path result objects — before claiming end-to-end (6.1 review found the commander flag dropped in the DatabaseError-path DeckImportLineResult, a surface the write-path checklist missed)"
    owner: "Sathias"
    status: done  # closed 2026-07-18 (epic-7 retro): exercised in all five Epic 7 stories; 7.3's re-verification caught the stale site count (7, not 6). Promoted to a standing team agreement.
  - epic: 7
    action: "gate-output homing rule: any gate/review that produces 'named inputs' must attach each to a story key, spec, or sprint-status gate in the same commit that closes the gate — a label with no key is how the G-R2 calibration inputs went unowned for a whole epic"
    owner: "Sathias"
    status: done  # closed 2026-07-26 (C1 retro): exercised in all 9 stories — c1-7 homed the Windows replace-while-open hazard, c1-8 ruled on it and re-homed to c6-1, c1-9 closed the launch race + remove_discovery TOCTOU in place recording what shipped. Promoted to a standing team agreement.
  - epic: 7
    action: "error-contract enumeration: when a story adds awaited I/O to an existing tool path, enumerate every call against the error-contract guard that covers it (7.2 review found _provision_combos' three DB reads outside the DatabaseError→error guard — sibling discipline to construction-site enumeration)"
    owner: "Sathias"
    status: done  # closed 2026-07-26 (C1 retro) in its strongest form: c1-6 registered DatabaseError→503 database_unavailable inside install_error_handling BEFORE any data route existed, so c3-1 onward inherit the guard with no per-route ceremony. Promoted to a standing team agreement.
  - epic: 7
    action: "data-layer orphan story keyed in sprint-status (data-layer-orphan-handling) so the thrice-documented get_deck_with_cards ValidationError crash + dead cards_unresolved signal stop floating in story notes"
    owner: "Sathias"
    status: done  # keyed 2026-07-18 at this retro (backlog key above Epic 7's action-item source)
  - epic: 7
    action: "close + promote construction-site enumeration to the standing team agreements"
    owner: "Amelia"
    status: done  # executed in-retro 2026-07-18 (epic-6 item above flipped to done; agreement recorded in epic-7-retro-2026-07-18.md)
  - epic: c1
    action: "forward-dated-comment homing: a comment or docstring asserting a FUTURE story's state (a log level chosen around an absent root handler, a promised flag, 'story X does this here') is a deferred edit — give it a story key in the same commit that writes it, like a gate output. c1-9 had to repair six such sentences across four files as an AC; it only worked because they were greppable"
    owner: "Brad"
    status: open
  - epic: c1
    action: "open-question homing: an 'Open questions for Brad' item whose answer changes a LATER story's work is homed on that story key when raised, not left in the story record. 20 were raised across C1 and 5 answered; two of the survivors (c1-5 OQ2 dev-proxy Host, c1-2 OQ2 /docs) landed in C2's path and had to be ruled at the retro"
    owner: "Brad"
    status: open
  - epic: c1
    action: "close the --platform mypy gate gap in c2-1: add the opposite-platform mypy invocation to ci.yml (or .pre-commit-config.yaml) while c2-1 adds the frontend job. singleton.py branches on sys.platform and each run checks only its own half — today the POSIX half is covered only because CI happens to run on ubuntu. Annotated on epics Story 2.1"
    owner: "Brad"
    status: done  # closed 2026-07-26 by story c2-1: ci.yml's `quality` job gains `uv run mypy src/ --platform win32`. NOT `--platform linux` as the epic text said — CI is ubuntu, where the bare run IS the linux run, so that would have been a no-op and the retro's success criterion would still not hold. Proved by AC 17's deliberate break: with msvcrt.locking() one arg short, --platform win32 reports "Too few arguments for 'locking' [call-arg]" and exits 1 while --platform linux stays green on the identical code. Break reverted; src/ byte-identical.
  - epic: c1
    action: "c2-9 must ship the internal_error state-panel copy (the sixth token shipped without a UI state; AD-16 requires them together) and also inherits the c1-6 corrupt-database ruling — a durably corrupt cards.db answers the quiet-retry 'Database updating' state forever with no repair path. Both need verbatim EXPERIENCE.md copy before the panels are built"
    owner: "Brad"
    status: open
  - epic: c1
    action: "confirm the PLANESWALKER_COMPANION_PORT env-var name during manual testing (checklist item 5) so c8-4 documents a settled name — unconfirmed since c1-3"
    owner: "Brad"
    status: done  # RULED 2026-07-26 at the C1 retro: renamed to COMPANION_PORT. Vendor prefix dropped; the COMPANION disambiguator kept deliberately, because MCP_TRANSPORT already contemplates sse/streamable-http and an MCP server over HTTP would need its own port (a bare PLANESWALKER_PORT could not tell the two apart). One constant in server.py:58 + two docstring/usage sites in __main__.py; tests read server.PORT_ENV_VAR so none changed. Gates green (1,684 passed / 1 skipped); plugin mirror rebuilt.
  - epic: c1
    action: "annotate c2-1 with the R1 changeOrigin requirement in epics-companion-app.md, per the gate-output homing rule"
    owner: "Amelia"
    status: done  # executed in-retro 2026-07-26; Story 2.1 now carries the changeOrigin AC + the action-item-3 mypy note
