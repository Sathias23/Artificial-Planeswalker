# generated: 2026-06-20
# EPIC RENUMBERING 2026-08-16: companion epics c1..c10 renamed 8..17 (c1=8, c2=9, c3=10,
#   c4=11, c5=12, c6=13, c7=14, c8=15, c9=16, c10=17). Historical prose, story/retro
#   filenames, PR titles and branch names keep the cN-M ids.
#   See planning-artifacts/sprint-change-proposal-2026-08-16.md.
generated: 2026-06-20
last_updated: 2026-08-25
# last_updated: 2026-08-25 — journal history compressed to archive/sprint-status-journal-2026-08-25.md
project: Artificial-Planeswalker
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1: MCP Server & Core Tool Surface
  epic-1: done
  1-1-repository-restructure-dependency-reshape: done
  1-2-sqlite-connectionfactory-with-wal-extension-loading: done
  1-3-fastmcp-server-with-card-lookup-bug-report: done
  1-4-advanced-card-search-tool: done
  1-5-deck-management-tools: done
  1-6-deck-analysis-tools: done
  epic-1-retrospective: done
  # Pre-Epic-2 tech-debt prep gate (from epic-1-retro-2026-06-21.md, action items 1-3 + optional wins)
  epic-1-tech-debt-prep-gate: done

  # Epic 2: Semantic Card Search (RAG)
  epic-2: done
  2-1-embedder-port-fastembed-singleton-persistent-cache: done
  2-2-card-vec-schema-with-metadata-columns: done
  2-3-card-embedding-index-builder-idempotent-incremental: done
  2-4-semantic-search-cards-tool-hybrid-query: done
  2-5-find-similar-cards-tool: done
  2-6-rag-sanity-eval: done
  epic-2-retrospective: done

  # Pre-Epic-3 tech-debt gate (from epic-2-retro §7: G1/G2/G3 + nullability audit + D1 docs)
  pre-epic-3-targeted-gate: done

  # Epic 3: Deckbuilding Skills Suite
  epic-3: done
  3-1-magic-deckbuilding-orchestrator-skill: done
  3-2-synergy-discovery-skill: done
  3-3-mana-curve-analysis-skill: done
  3-4-format-legality-skill: done
  epic-3-retrospective: done

  # Pre-Phase-2 Gate (from epic-3-retro-2026-06-27.md §6) — all green before Arena import/export build starts
  # G-SD2a: validate_deck lowercase-format .lower() [done, deck_validator.py:302]
  #         + valid-key guard [done 2026-07-11 fix/pre-phase-2-gate — _KNOWN_FORMATS + unknown_format violation]
  # G-SD2b: detect_synergies '//' double-faced-card token fix [done 2026-07-11 fix/pre-phase-2-gate — per-face parse in _extract_creature_types]
  # G-R1: Brad live-smokes all four skills end-to-end through Claude Code (Phase-1 acceptance) [done 2026-07-11 — Brad hands-on accepted]
  pre-phase-2-gate: done  # all three green: G-SD2a/G-SD2b (code) + G-R1 (Brad acceptance) — Phase 2 import/export unblocked

  # ============================================================================
  # DECK POWER-LEVEL ASSESSMENT
  # Source epics: _bmad-output/planning-artifacts/epics-deck-power-assessment.md
  # Readiness:    _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-11.md (READY; 0 critical / 0 major)
  #
  # Feature epics are renumbered 4-7 here to avoid key collision with the
  # completed Phase-1 MCP-pivot Epics 1-3 above. Mapping (feature → sprint):
  #   feature Epic 1 (Game Changer card data)      → epic-4
  #   feature Epic 2 (Deterministic scoring core)  → epic-5
  #   feature Epic 3 (Combo detection)             → epic-6
  #   feature Epic 4 (assess_deck_power tool)      → epic-7
  # When running create-story, use the epic-4..7 keys below (NOT the epic
  # file's raw 1-4). Story titles are unchanged from the epic file.
  #
  # ORDERING CONSTRAINT: 5-6 (ComboRecord) must precede 6-2 / 6-3
  #   (the combo client + cache consume the record shape the core defines).
  # ============================================================================

  # Epic 4 (feature Epic 1): Game Changer card data
  epic-4: done  # 4-1 + 4-2 both done and epic-4-retrospective done (epic-4-retro-2026-07-12.md); the in-progress value was stale — corrected 08-18-2026 09:47 with no code change.
  4-1-add-the-nullable-game-changer-field-end-to-end: done
  4-2-migrate-and-backfill-existing-databases: done
  epic-4-retrospective: done  # epic-4-retro-2026-07-12.md; game_changer backfilled live (53 TRUE / 38180 FALSE / 0 NULL)

  # Epic 5 (feature Epic 2): Deterministic scoring core
  epic-5: done
  5-1-compose-the-calibration-benchmark-set: done
  5-2-format-profile-frozen-data-module: done
  5-3-shared-oracle-text-classifiers: done
  5-4-mana-base-curve-signals: done
  5-5-consistency-interaction-structural-coverage-signals: done
  5-6-combo-record-combo-bracket-mapping: done
  5-7-dimension-vector-commander-bracket-floor-cedh-candidacy: done
  5-8-for-format-aggregate-tier-label-standard-fork-confidence-vocabulary: done
  5-9-pure-score-entry-point-benchmark-validation: done
  epic-5-retrospective: done  # epic-5-retro-2026-07-15.md; benchmark gate green (1136 full suite), branch stays feat/deck-power-assessment until feature complete ("experimental" release)

  # Pre-Epic-6 Importer Gate (from epic-5-retro-2026-07-15.md) — blocks Epic 6 story creation.
  # G-I1: upsert keyed by oracle_id / stale-printing reconcile + oracle-level field propagation
  # G-I2: import errors surfaced per-card (name + exception class) in the import summary
  # G-I3: import-path regression test (oracle_id dedup + game_changer propagation survive re-import)
  #       — absorbs epic-4 action item 3 (persisted nullable-bool round-trip test)
  pre-epic-6-importer-gate: done  # 2026-07-15 — journal: archive/spec-pre-epic-6-importer-gate.md

  # Epic 6 (feature Epic 3): Commander identity & local combo snapshot
  # (re-scoped 2026-07-12 per sprint-change-proposal-2026-07-12.md — bulk snapshot replaces live client/cache/key)
  epic-6: done
  6-1-commander-flag-end-to-end: done
  6-2-spellbook-bulk-combo-snapshot-import: done
  6-3-combo-snapshot-repository: done
  epic-6-retrospective: done  # epic-6-retro-2026-07-17.md; benchmark re-run w/ real snapshot data = zero divergences; epic-5 items 4/6/7/8 closed, item 5 escalated to gate below

  # Pre-Epic-7 Real-Deck Gate (from epic-6-retro-2026-07-17.md) — blocks Epic 7 story creation.
  # G-R2: real-deck sanity pass — throwaway harness loads Sathias's real saved decks (live DB),
  #       pulls variants via ComboSnapshotRepository.get_variants_for_names (the exact Epic 7 path),
  #       runs pure score(); Sathias compares tier/Bracket/cEDH outputs against his own judgment.
  #       Done when accepted as plausible OR each divergence logged as a named Epic 7 calibration
  #       input (CEDH_TUTOR_MIN=3 caveat carries over). Closes epic-5 action item 5.
  pre-epic-7-real-deck-gate: done  # CLOSED 2026-07-17 (commit e2b03ff): all 20 decks accepted as plausible wholesale; calibration deferred to Epic 7 — named inputs in pre-epic-7-real-deck-gate-report-2026-07-17.md §Cross-deck observations. Epic 7 unblocked.

  # Epic 7 (feature Epic 4): The assess_deck_power + compare_deck_power MCP tools & deterministic output
  epic-7: done  # all 5 stories done 2026-07-18; final epic of the deck-power-assessment roadmap
  7-1-register-the-async-tool-load-deck-resolve-format: done  # code review 2026-07-17: all ACs MET; 1 patch applied (unresolved_count comment), empty-mainboard ok accepted, orphan-crash deferred to data-layer story
  7-2-combo-provisioning-the-degradation-ladder: done  # 2026-07-17 — journal: archive/7-2-combo-provisioning-the-degradation-ladder.md

  7-3-assess-deck-power-result-assembly-deterministic-serialization-human-summary: 
    done                                                                                 # code review 2026-07-17: all 9 ACs MET (auditor: zero violations); 3 low patches applied (bracket→Literal[2,3,4]|None per FR18 decision, "one card away" grammar, format→Literal), 7 dismissed; 1,313 tests green, mypy/ruff clean
  7-4-end-to-end-tool-test-determinism-diff-regression: done
  7-5-compare-deck-power-tool: done  # code review 2026-07-18: all 9 ACs MET (auditor: zero violations); 2 decision-needed accepted as-spec'd, 2 low patches applied (asymmetric both-fail token test + Deck A/B summary casing), 10 dismissed; compare suite 22 green, mypy/ruff clean
  epic-7-retrospective: done  # epic-7-retro-2026-07-18.md; ship-first ruling (merge → manual-testing acceptance → "experimental" release); calibration gated below, non-blocking

  # Post-Epic-7 Calibration Gate (from epic-7-retro-2026-07-18.md) — NON-BLOCKING for release
  # (ship-first ruling 2026-07-18). Homes the five named G-R2 calibration inputs
  # (pre-epic-7-real-deck-gate-report-2026-07-17.md §Cross-deck observations) + anything
  # Sathias's post-PR comprehensive manual testing surfaces:
  #   C1: card_advantage saturation at 80
  #   C2: interaction railing 0/100
  #   C3: Brawl mana_efficiency floor (brawl-family currently unsupported_format by 7.1 decision)
  #   C4: almost_included dominance of combo_potential
  #   C5: format-blind almost_included inflation (missing piece not format-legal; deferred-work.md)
  # CEDH_TUTOR_MIN=3 caveat carries over. Done when each input is tuned, or closed with a
  # documented keep-decision (the 5.7 card_advantage-ceiling precedent).
  post-epic-7-calibration-gate: backlog

  # Data-layer orphan handling (epic-7 retro action item 3; deferred from 7.1 review):
  # an orphaned deck_cards row (FK enforcement off) raises pydantic.ValidationError inside
  # get_deck_with_cards — shared ungraceful crash across analyze_mana_curve / detect_synergies /
  # validate_deck / assess_deck_power; also the only live source that could ever make
  # cards_unresolved fire. Includes the 7.2-deferred transient-lock mislabel
  # (combo_snapshot.py broad OperationalError → "unavailable") if the repo catch is narrowed.
  data-layer-orphan-handling: backlog

  # ============================================================================
  # COMPANION APP  (added 2026-07-25)
  # Source epics: _bmad-output/planning-artifacts/epics-companion-app.md
  #   (NOT epics.md — that file is the MCP pivot. This is a feature-level epic
  #    set layered onto the brownfield repo: src/companion/, ui/, new MCP tools.)
  # Inputs: PRD 2026-07-22 (+addendum) · UX Voltglass 2026-07-25 · ARCHITECTURE-SPINE 2026-07-25 (16 ADs)
  #
  # KEY SCHEME — c-prefix, epic-file numbering PRESERVED (Brad's call 2026-07-25).
  #   epic file "Epic 4"    → epic-c4
  #   epic file "Story 4.6" → c4-6-<kebab-title>
  # Chosen over renumbering to 8..17 so the 76 story references never need a
  # +7 translation. When running create-story / dev-story, use the c-keys below.
  #
  # WORK BRANCH: feat/companion-app. Each dev-story starts a NEW branch off it
  # (feat/companion-<story-slug>); story PRs target feat/companion-app; one
  # integration PR to master at the end.
  #
  # PHASING: Epics c1-c8 are Phase 1 (MVP, 70 stories). Epics c9-c10 are Phase 2
  # (6 stories). Phase 3 (FR-16, FR-21, Tauri, UI-initiated edits) is OUT OF SCOPE
  # for this breakdown — the last needs its own brief per AD-2.
  #
  # DEPENDENCY ORDER (from the epic file's "Depends on" lines):
  #   c1 ← nothing         c2 ← c1        c3 ← c1 (independent of c2)
  #   c4 ← c2, c3          c5 ← c2, c3    c6 ← c4, c5
  #   c7 ← c6              c8 ← c7        c9 ← c6        c10 ← c7
  #   c3 is the SC-4 close; c4 makes SC-5 answerable; c6 closes SC-1 + SC-3; c7 closes SC-2.
  #
  # SUCCESS CRITERIA (human gates):
  #   SC-5 = Brad's own judgement against DESIGN.md + EXPERIENCE.md (UX-DR49) — story c8-6.
  #          Cannot be automated or delegated.
  #
  # KNOWN OPEN ITEM: FR-18's session-history home (nav extension vs per-view header
  # strip) is undecided. Blocks c10-2 ONLY — no Phase-1 story waits on it.
  # ============================================================================

  # Epic C1: Launch the Companion
  # FR-01, FR-14, FR-22 (backend half) · NFR-01, NFR-02 (both CI boundary tests), NFR-07
  # AD-1/2/3/4/10/14/15/16 · depends on nothing
  # NOTE: EPIC-SPLIT calls c1-1's two boundary tests the highest-leverage stories in the
  # whole feature — they land BEFORE the code they guard, so AD-2/AD-3 are structural not aspirational.
  epic-8: done  # 9/9 2026-07-26; PRs #9-#16 merged into feat/companion-app (integration PR to master = R3)
  8-1-companion-package-skeleton-with-ci-enforced-import-boundaries: done
  8-2-side-effect-free-asgi-app-with-a-lifespan-and-a-health-endpoint: done
  8-3-port-selection-with-ephemeral-fallback-and-a-printed-launch-url: done
  8-4-typed-rest-error-contract-with-closed-reason-tokens: done
  8-5-localhost-only-security-envelope-host-validation-and-cors: done
  8-6-lazy-database-engine-so-a-fresh-install-starts-instead-of-erroring: done
  8-7-discovery-file-as-the-sole-rendezvous: done
  8-8-single-instance-enforcement-with-verified-identity: done
  8-9-one-console-script-that-dispatches-without-disturbing-the-mcp-server: done
  epic-8-retrospective: done  # epic-c1-retro-2026-07-26.md; 3 rulings (R1 changeOrigin → c2-1, R2 /docs keep-decision, R3 integration PR now), 6 action items, 8-item manual-testing checklist

  # Epic C2: The Glass — Foundation, Identity & Honest States
  # FR-20, FR-22 (state-panel surface + copy) · NFR-03, NFR-06, NFR-07, NFR-08 · AD-12, AD-13
  # depends on c1
  # NOTE: c2-1 carries the greenfield ui/ scaffold — the only new toolchain in the feature.
  # The TypeScript >=5.9,<6.1 pin is load-bearing (typescript-eslint peer range <6.1.0) and belongs to it.
  # ALSO load-bearing, discovered while contexting c2-1 (2026-07-26): an `eslint ^9` pin. ESLint's
  # `latest` is 10.8.0 and eslint-plugin-jsx-a11y@6.10.2 (latest, no newer) peers at `^9` — measured
  # `npm ci` ERESOLVE, not a warning. Not in the Spine's stack table; c2-1 adds it.
  epic-9: done  # 10/10 2026-07-30; PRs #18-#27 into feat/companion-c2, retro, then INTEGRATION PR #28 MERGED at a52d6f8 — Epic C2 is on master. Merge != release: no tag/CHANGELOG until c8-4
  9-1-frontend-scaffold-with-the-full-quality-gate-from-the-first-commit: done  # review round 2 (2026-07-26): 3 rulings + 11 patches applied, suite 50→55, five gates green incl. new CI build step
  9-2-the-backend-serves-the-built-spa-as-a-committed-artifact: done  # code review 2026-07-26: 2 decisions ruled + 14 patches applied, suites 1,742/59, nine gates green
  9-3-typescript-types-generated-from-the-backends-own-openapi-drift-checked-in-ci: 
    done                                                                                  # reviewed 2026-07-27: 14 patches applied (3 medium guard-vacuity fixes), 2 deferred, proofs re-run; suites 1,753 Python / 78 frontend
  9-4-the-voltglass-token-layer: done  # 2026-07-27 — journal: archive/c2-4-the-voltglass-token-layer.md
  9-5-self-hosted-space-grotesk-with-offline-parity-and-tabular-numerals: done  # 2026-07-28 — journal: archive/c2-5-self-hosted-space-grotesk-with-offline-parity-and-tabular-numerals.md
  9-6-the-two-column-application-shell: done  # 2026-07-28 — journal: archive/c2-6-the-two-column-application-shell.md
  # contexted 2026-07-28 off 2a22e19; first component of the feature; 12 landmines measured (no box-sizing reset in ui/ at all; the max-width breakpoint spelling is a lint error; geometry literals are the ungated family and cannot become tokens; the reference mock's absolute overlay is wrong in a real browser); 25 ACs; 5 open questions, 3 of them decide-once conventions ~35 stories inherit
  9-7-presentation-only-primitives-panel-badge-statchip-group-header: done  # 2026-07-29 — journal: archive/c2-7-presentation-only-primitives-panel-badge-statchip-group-header.md
  9-8-manapip-and-manacost-with-complete-scryfall-cost-parsing: done  # 2026-07-29 — journal: archive/c2-8-manapip-and-manacost-with-complete-scryfall-cost-parsing.md
  9-9-the-shared-state-panel-and-every-system-state-message: done  # 2026-07-30 — journal: archive/c2-9-the-shared-state-panel-and-every-system-state-message.md
  9-10-footer-attribution-on-every-surface: done  # 2026-07-30 — journal: archive/c2-10-footer-attribution-on-every-surface.md
  epic-9-retrospective: done  # epic-c2-retro-2026-07-30.md; 4 rulings, 6 action items, 14-item manual-testing checklist, 0 significant discoveries

  # Epic C3: Deck Data & Card Imagery on Tap
  # FR-02, FR-03, FR-04, FR-07 (backend state slot + both transports), FR-22 (closes SC-4)
  # NFR-02 (WAL reads), NFR-06, NFR-08, NFR-09, CM-2 · AD-11, AD-16 · depends on c1 (independent of c2)
  # NOTE: only externally-paced work in the feature. A cold 100-card deck at ~12 MB / ~10 s to
  # fully paint is an EXPECTED OBSERVATION in acceptance, not a defect.
  epic-10: done  # 2026-08-02 — journal: archive/sprint-status-journal-2026-08-25.md
  10-1-deck-list-and-deck-detail-endpoints: done  # 2026-07-31 — journal: archive/c3-1-deck-list-and-deck-detail-endpoints.md
  10-2-card-detail-endpoint: done  # 2026-07-31 — journal: archive/c3-2-card-detail-endpoint.md
  10-3-format-check-endpoint-over-the-existing-validators: done  # 2026-08-01 — journal: archive/c3-3-format-check-endpoint-over-the-existing-validators.md
  10-4-the-active-deck-readable-by-the-glass-settable-by-the-agent: done
  10-5-card-image-endpoint-with-face-resolution-and-a-defined-parameter-contract: 
    done                                                                                # REVIEWED -> done 2026-08-01: three-layer pass (Blind Hunter/Edge Case Hunter/Acceptance Auditor) over the prod diff; 17 findings -> 2 decisions (Brad: follow_redirects=False; the refused-URL token stays, ledgered on c3-8), 11 patches applied same-day (headlines: the size cap now streams instead of measuring an already-buffered body; redirects no longer bypass the allow-list; SVG refused + nosniff; AC 19/27's Scryfall-host scans landed as committed gates), 4 dismissed. Re-gated: Python 2240, frontend 568, ruff/mypy/tsc clean, mirror rebuilt. Previously: IMPLEMENTED 2026-08-01 (34797bf) — all six questions as proposed, the 16th story running. Two new reason tokens (no_image_data 404 / image_fetch_failed 502) across eight ripple sites twice over; CardFace typed with extra="allow"; the image discriminator reduced to ONE source with a family-keyed gate. Paths 6->7, components 11->12; suites 2140->2275 Python, 558->565 frontend. Five mutation probes all caught; two of this story's own guards caught their author first. Story record holds the narrative, incl. three booked deviations. Next = Brad's Task 9 three-layer review.
  10-6-paced-concurrency-capped-cdn-fetching-at-one-global-choke-point: done  # 2026-08-01 — journal: archive/c3-6-paced-concurrency-capped-cdn-fetching-at-one-global-choke-point.md
  10-7-sharded-atomically-written-disk-cache: done  # 2026-08-01 — journal: archive/c3-7-sharded-atomically-written-disk-cache.md
  10-8-distinguishable-failure-signalling-and-negative-caching: done  # 2026-08-02 — journal: archive/c3-8-distinguishable-failure-signalling-and-negative-caching.md
  10-9-fresh-install-guides-instead-of-erroring-and-comes-alive-on-its-own: done  # 2026-08-02 — journal: archive/c3-9-fresh-install-guides-instead-of-erroring-and-comes-alive-on-its-own.md
  epic-10-retrospective: done  # epic-c3-retro-2026-08-02.md; 3 rulings + 1 parked, 12 action items, 24-item manual checklist (A1 + Block B closed), 1 SIGNIFICANT DISCOVERY (Scryfall bulk-data API break = released-product P0)

  # Scryfall bulk-data API migration (C3 retro R1) — HOTFIX ON MASTER, blocks the C3 integration PR.
  # Scryfall replaced download_uri -> jsonl_download_uri and size -> compressed_size, and the payload
  # went from a top-level JSON array (.json) to gzip'd newline-delimited JSON (.jsonl.gz). Both keys are
  # GONE from every entry on both endpoints. Kills scripts/import_scryfall_data.py, the initialize_database
  # MCP tool and import_scryfall_bulk_data — i.e. every data path, on a PUBLIC RELEASED product (v0.4.0):
  # a fresh install today cannot obtain data by any supported route. Invisible to all 2,472 tests because
  # every one monkeypatches fetch_bulk_data_list; nothing has ever called api.scryfall.com/bulk-data.
  # Fix is a FORMAT change, not a key rename — parser.py:45 uses ijson.items(f, "item"), which requires an
  # array. Done when the importer completes against the live API AND a committed test reads the real endpoint.
  scryfall-jsonl-migration: done  # 2026-08-02 — journal: archive/sprint-status-journal-2026-08-25.md

  # Epic C4: The Deck on the Glass
  # FR-05, FR-17, FR-19 · NFR-05 (1 s deck render, warm cache) · AD-11, AD-12, AD-16 · depends on c2, c3
  # NOTE: largest UX surface in the feature; first epic where SC-5 becomes answerable.
  # c4-6 (DFC flip control, UX-DR15) is the densest single component in the whole feature.
  epic-11: done  # 2026-08-07 — journal: archive/sprint-status-journal-2026-08-25.md
  11-1-a-single-card-hydration-cache-with-in-flight-deduping: done  # 2026-08-02: 3-layer review, 10 patches applied (1 High), 2 deferrals, 6 dismissed; 816 frontend green
  11-2-deck-state-bootstrap-and-the-type-grouped-decklist: done  # 2026-08-03 — journal: archive/c4-2-deck-state-bootstrap-and-the-type-grouped-decklist.md
  11-3-card-placeholders-named-unknown-and-loading-wells: done  # 2026-08-04 — journal: archive/c4-3-card-placeholders-named-unknown-and-loading-wells.md
  11-4-card-tile-and-the-card-art-grid: done  # 2026-08-04 — journal: archive/c4-4-card-tile-and-the-card-art-grid.md
  # contexted 2026-08-04 off b47f603; the story where the deck finally appears on the glass, and where FOUR categories open at once: src/components/ is a CLOSED set-equality category the tile cannot join (Q1, ~15 stories inherit the answer); this is the FIRST motion in the codebase (measured: zero transition/animation/transform anywhere in ui/src) and the first extension of the reduced-motion registration block, which already names c4-4 twice; the focus-ring composite is banned inline by stylelint and absent from the 65-token inventory, so a token is added in the open with both pins moving together (Q2); and the caption's type role uppercases every card name by gate while the mock does the opposite (Q3). Plus: the tile announces its name twice (UX-DR48's justification is false for this component, Q4), an <img> onError carries no wire token, and a cached image can beat React's onLoad. 32 ACs, 10 open questions, 12 inherited deferrals, 15 don't-breaks; measured 99 tiles / 1,061 ids / 1-of-99 badges / 13-of-99 ellipsis. Baseline 1,024 frontend / 42 files
  11-5-persistent-card-detail-panel-with-transient-and-pinned-inspection: done  # 2026-08-05 — journal: archive/c4-5-persistent-card-detail-panel-with-transient-and-pinned-inspection.md
  # contexted 2026-08-05 off b26e8f4; the story where the deck view first RESPONDS, and the first
  # user-interaction state in the codebase — the spine's "state comes from exactly two inputs, REST
  # and WebSocket" has to be narrowed IN WRITING rather than quietly violated (Q5). Headline
  # finding, MEASURED against the shipped 38,261-card DB: CardSummary already carries name,
  # mana_cost, type_line AND oracle_text, so for a single-faced card hydrateCard adds NOTHING the
  # panel draws and "the rest fills in place" would be vacuous — but ALL 3,225 cards carrying
  # card_faces have a BLANK top-level oracle_text (100%, not a majority) and 2,274 carry the
  # degenerate `Card // Card` type line, so for them hydration is the ONLY source of a type line and
  # rules text. 6 of the 99 cards in the real deck are that population (all MDFC Pathways, all
  # per-face image_uris, all blank top-level type/cost/oracle). Q1 therefore rules hydrate-anyway +
  # render card_faces[0], which also makes this story — not c4-6 — the first consumer of the
  # `CardFace` alias schema.ts:101 had assigned to c4-6 (ledger correction, in the open). TWO
  # ARTEFACT DEFECTS found in this component: the unpin control is REQUIRED by UX-DR20 and specified
  # nowhere (no size/glyph/position/label/token, Q3), and components.card-detail.pinned-ring is
  # accent-dim on surface-overlay = 2.70:1 — the exact pairing DESIGN.md:305 bans and the exact
  # defect the UX gate closed as M4/C3 for card-tile.live-ring, never carried across (Q2). Also:
  # tokens.test.ts:433 ASSERTS the live ring's absence, so this story turns a green test red on
  # purpose (66 -> 68, both pins); the suite has NO hover instrument (no userEvent anywhere, and
  # package-contract.test.ts pins the deps, Q9); zustand v5 dropped create()'s equality fn, so an
  # object-returning selector loops — which is also the whole argument for a per-tile
  # useIsLiveTarget selector over a prop threaded to all 99 tiles (Q7). shell.test.ts ALREADY
  # exempts this panel from the clip ban, a decision made in advance for Q10. Q14 puts a KNOWN-OPEN
  # UX gap on the table rather than discovering it in code: validation L8 (right-column visibility
  # when the left column falls to a state panel) is recorded unactioned, and surfaceOf() already
  # forces App.tsx to answer it. 41 ACs, 15 open questions, 6 inherited deferrals homed here BY NAME (Panel's overlay eye-check, prices-by-
  # absence, the cache's terminal-after-three, the placeholder's undeclared vertical edge, the
  # <div>-in-<button>, the selector-text motion guard), 16 don't-breaks. Measured: oracle_text in
  # the real deck min 0 / median 156 / max 452, corpus max 1,489; 79 cards lack an image at `large`
  # — the SAME 79 as at `normal`. Baseline 1,086 frontend / 44 files, tokens 66, bundle 208,004 B.
  11-6-double-faced-card-flip-control: done  # 2026-08-06 — journal: archive/c4-6-double-faced-card-flip-control.md
  11-7-deck-list-panel: done  # 2026-08-06 — journal: archive/c4-7-deck-list-panel.md
  11-8-mana-curve-panel: done  # 2026-08-06 — journal: archive/c4-8-mana-curve-panel.md
  11-9-colour-distribution-panel: done  # 2026-08-06 — journal: archive/c4-9-colour-distribution-panel.md
  11-10-format-check-panel: done  # 2026-08-06 — journal: archive/c4-10-format-check-panel.md
  11-11-keyboard-floor-skip-link-tab-order-and-focus-management: done  # 2026-08-07 — journal: archive/c4-11-keyboard-floor-skip-link-tab-order-and-focus-management.md
  11-12-empty-deck-state-and-the-cold-open-render-budget: done  # 2026-08-07 — journal: archive/c4-12-empty-deck-state-and-the-cold-open-render-budget.md
  epic-11-retrospective: done  # 2026-08-07 — journal: archive/sprint-status-journal-2026-08-25.md

  # Epic C5: The Agent's Channel
  # FR-06, FR-13 (ID-only contract) · NFR-01 (ticket, Origin, token separation), NFR-03, NFR-04
  # AD-5, AD-6, AD-7 · depends on c2, c3
  # NOTE: all four payload shapes are frozen HERE in Phase 1 (c5-1), not added incrementally —
  # the discriminated union needs every member; a late change ripples through the committed
  # .d.ts and both mirrored plugin bundles. This is why Phase 2 is only 6 stories.
  # c5-8 is the ONE integration-marked real-socket test in the feature (AD-10).
  epic-12: done
  12-1-the-event-envelope-and-every-per-kind-payload-contract: done  # MERGED via PR #53 2026-08-07 — journal: archive/c5-1-the-event-envelope-and-every-per-kind-payload-contract.md
  12-2-same-origin-session-endpoint-minting-single-use-websocket-tickets: done  # MERGED via PR #54 2026-08-08 — journal: archive/c5-2-same-origin-session-endpoint-minting-single-use-websocket-tickets.md
  12-3-authenticated-websocket-upgrade-with-host-and-origin-validation: done  # 2026-08-08 — journal: archive/c5-3-authenticated-websocket-upgrade-with-host-and-origin-validation.md
  12-4-broadcast-to-every-connected-client: done  # MERGED via PR #56 2026-08-08 — journal: archive/c5-4-broadcast-to-every-connected-client.md
  12-5-token-authenticated-event-ingest-that-reports-who-is-listening: done  # MERGED via PR #57 2026-08-08 — journal: archive/c5-5-token-authenticated-event-ingest-that-reports-who-is-listening.md
  12-6-client-reconnection-with-backoff-and-a-fresh-ticket-per-attempt: done  # MERGED via PR #58 2026-08-08 — journal: archive/c5-6-client-reconnection-with-backoff-and-a-fresh-ticket-per-attempt.md
  12-7-connection-pill: done  # MERGED via PR #59 2026-08-08 — journal: archive/c5-7-connection-pill.md
  12-8-the-one-real-socket-integration-test: done  # MERGED via PR #60 2026-08-09 — journal: archive/c5-8-the-one-real-socket-integration-test.md
  epic-12-retrospective: done  # run 2026-08-09; epic-c5-retro-2026-08-09.md; 11 action items (R1-R11) appended below

  # Epic C6: The Agent Pushes to the Glass
  # FR-07 (MCP tool + deck-existence validation), FR-08, FR-12 · NFR-05 (250 ms push)
  # AD-8 · depends on c4, c5 · closes SC-1 and SC-3
  epic-13: done  # 2026-08-12 — journal: archive/sprint-status-journal-2026-08-25.md
  13-1-leaf-client-with-health-verification-retry-once-and-the-closed-outcome-vocabulary: 
    done
  13-2-companion-set-active-deck-the-agent-chooses-what-the-glass-shows: done
  13-3-the-glass-follows-the-agents-active-deck-choice: done
  13-4-companion-show-suggestions-the-agents-first-push: done
  13-5-agent-view-shell-with-focus-management-and-dismissal: done
  13-6-a-push-opens-its-view-and-a-repeat-push-replaces-it-in-place: done
  13-7-suggestions-view: done
  13-8-agent-views-nav-unread-markers-re-open-and-kind-switching: done
  13-9-degradation-with-the-app-closed-and-the-250-ms-push-budget: done
  epic-13-retrospective: done  # run 2026-08-13 (delegated mode, rulings overridable until the integration PR merges); epic-c6-retro-2026-08-13.md; 10 action items (R1-R10) appended below; C5 items R1-a/R6/R7 formally DECLINED per their clauses, R2/R4/R5/R1-b re-keyed, R8 closed as adopted

  # Epic C7: The Deck Updates Itself
  # FR-11, FR-12 (notifier swallow) · NFR-04 (refetch coalescing) · AD-9 · depends on c6 · closes SC-2
  # NOTE: AD-9 — "fire-and-forget" means a bounded-timeout await (~1 s), NOT a detached task.
  # asyncio.create_task is BANNED on this path. Emission is always after commit, never inside the txn.
  epic-14: done  # 7/7 2026-08-16; PRs #74-#81 merged into feat/companion-c7; retro run 2026-08-16. Merge != release: integration PR to master still to raise.
  14-1-one-shared-notifier-with-a-bounded-await-and-no-detached-tasks: done
  14-2-every-deck-mutation-tool-emits-after-its-transaction-commits: done  # 2026-08-14 — journal: archive/sprint-status-journal-2026-08-25.md
  14-3-the-glass-refetches-on-deck-changed-coalesced-and-latest-wins: done  # 2026-08-14 — journal: archive/sprint-status-journal-2026-08-25.md
  14-4-refetch-never-tears-down-whats-on-screen: done  # 2026-08-15 — journal: archive/sprint-status-journal-2026-08-25.md
  14-5-the-change-is-announced-once-and-motion-is-never-the-only-signal: done  # 2026-08-15 — journal: archive/sprint-status-journal-2026-08-25.md
  14-6-deck-deletion-and-agent-views-during-a-refetch: done  # 2026-08-15 — journal: archive/sprint-status-journal-2026-08-25.md
  14-7-the-loop-closes-uj-1-end-to-end: done  # 2026-08-16 — journal: archive/sprint-status-journal-2026-08-25.md
  epic-14-retrospective: done  # 2026-08-16 — journal: archive/sprint-status-journal-2026-08-25.md

  # Epic C8: Release Readiness
  # NFR-08 (release notice), NFR-09 (README + uninstall notes) · AD-15 · depends on c7
  # NOTE: c8-3 owns folding the six story-writing gaps + the four confirmed UX rulings back
  # into the PRD, plus the three PRD amendments owed (NFR-02 mode=ro, FR-14 path, FR-04 layout).
  # c8-6 is the SC-5 human gate — Brad only, cannot be automated or delegated.
  epic-15: in-progress  # 2026-08-20 — journal: archive/sprint-status-journal-2026-08-25.md
  15-1-deprecate-view-deck-and-freeze-src-viewer: done  # 2026-08-18 — journal: archive/spec-15-1-deprecate-view-deck-and-freeze-src-viewer.md
  15-2-image-cache-stewardship-documented-location-inspection-and-removal: done  # 2026-08-18 — journal: archive/spec-15-2-image-cache-stewardship-documented-location-inspection-and-removal.md
  15-3-reconcile-the-prd-with-what-was-built: done  # 2026-08-18 — journal: archive/spec-15-3-reconcile-the-prd-with-what-was-built.md
  15-4-release-documentation-for-the-companion-app: done  # MERGED via PR #88 2026-08-19 — journal: archive/spec-15-4-release-documentation-for-the-companion-app.md
  15-5-plugin-distribution-parity: done  # MERGED via PR #89 2026-08-20 — journal: archive/spec-15-5-plugin-distribution-parity.md
  15-6-the-sc-5-gate: done  # MERGED via PR #90 2026-08-20 — journal: archive/spec-15-6-the-sc-5-gate.md
  epic-15-retrospective: done

  # Epic C9: The Remaining Push Kinds  *(Phase 2)*
  # FR-09, FR-10, FR-23 · UX-DR25/26/27 · depends on c6
  # NOTE: cheap because c5-1 already froze all four payload shapes — each story is tool + view only.
  epic-16: done  # 2026-08-21 — journal: archive/sprint-status-journal-2026-08-25.md
  16-1-proposed-swaps-tool-and-view: done  # PR #92 MERGED into feat/companion-epic-16 at 77f2f0f (story commit 7e9fc2f): companion_show_swaps tool + SwapsView.
  16-2-tier-lists-tool-and-view: done  # PR #93 MERGED into feat/companion-epic-16 at 008337e (story commit d40fd66): companion_show_tier_list tool + TierListView.
  16-3-card-groups-tool-and-view: done  # PR #94 MERGED into feat/companion-epic-16 at eeb2288 (story commit 85a1d92): companion_show_groups tool + GroupsView — last push kind; drop pin deleted.
  epic-16-retrospective: done

  # Epic C10: Session History, Status Detail & Performance Polish  *(Phase 2)*
  # FR-15, FR-18 · NFR-05 hardening · depends on c7
  # BLOCKED ITEM: c10-2 waits on the FR-18 session-history home decision (extend the nav,
  # or a strip inside each view's header) — the one open UX item carried into story work.
  epic-17: in-progress
  17-1-connection-pill-status-detail: done
  17-2-session-history: done
  17-3-measure-the-latency-budgets-and-close-the-gaps: done
  17-4-open-the-companion-from-the-agent: done
  17-5-welcome-surface-first-impression: done
  epic-17-retrospective: done

# Action items carried out of retrospectives (see epic-N-retro-*.md for full context)
action_items:
  - epic: 4
    action: "'end-to-end field' story checklist must grep ALL four write-path touch-points,
      incl. importer.py::_insert_batch card_dict + on_conflict_do_update set_ mapping
      (not just model/schema/transformer)"
    owner: "Brad"
    status: done  # closed 2026-07-17: first real exercise was story 6.1 AC 6 (11-path deck_cards table) — passed; successor discipline (construction-site enumeration) logged as epic-6 item
  - epic: 4
    action: "do not assume build-plugin-sync auto-mirrors: .git/hooks/pre-commit is
      absent in this checkout, so after any src/ commit run 'uv run python -m scripts.build_plugin'
      + diff-check the plugin mirror (or 'pre-commit install')"
    owner: "Brad"
    status: done  # closed mid-Epic-5: hook installed as of 88b1e66 (story 5.5); verified present at epic-5 retro
  - epic: 4
    action: "add a persisted-SQLite round-trip test proving a nullable-bool field
      (game_changer=False) survives DB write/read — the gap that let the importer.py
      omission through; also covers the power/toughness precedent"
    owner: "Brad"
    status: done  # closed via epic-5 G-I3 (import-path regression tests landed in pre-epic-6-importer-gate, 2026-07-15)
  - epic: 5
    action: "G-I1: stop stale-printing accumulation — key the upsert by oracle_id
      (or reconcile stale printings, minding deck_cards FKs) and propagate oracle-level
      fields (game_changer) across same-oracle_id rows; done when a live refresh leaves
      0 stale duplicates and 0 game_changer NULLs"
    owner: "Sathias"
    status: done  # 2026-07-15 live acceptance: 0 dup oracle_ids, 0 NULLs, 12,971 stale rows removed
  - epic: 5
    action: "G-I2: surface silent import errors — failing cards listed in the import
      summary with name + exception class, counted against a stale-rows-remaining
      warning"
    owner: "Sathias"
    status: done  # 2026-07-15: 33 rejects listed with reasons (all reversible_card type_line) on CLI + MCP
  - epic: 5
    action: "G-I3: committed import-path regression test proving oracle_id dedup +
      game_changer propagation survive a re-import (absorbs/closes epic-4 round-trip-test
      item)"
    owner: "Sathias"
    status: done  # 2026-07-15: two-snapshot e2e suite in test_scryfall_import_e2e.py + test_reconcile_plan.py
  - epic: 5
    action: "story-start state verification: any story whose notes assert live-DB/environment
      state must open with the cheap query that proves it (the 5.9 'imported AND backfilled'
      saga was one SELECT away from caught)"
    owner: "Sathias"
    status: done  # 2026-07-17: ran as Task 0 in all three Epic 6 stories — caught the httpx decoding trap (6.2) and a baseline off-by-one (6.3); promoted to standing team agreement (epic-6 retro)
  - epic: 5
    action: "real-deck sanity pass: throwaway harness runs pure score() over Sathias's
      real saved decks (live-DB resolution), tiers compared against his own judgment
      — early validity signal, ideally before Epic 7 tuning ossifies"
    owner: "Sathias"
    status: done  # closed 2026-08-09 at the C5 retro as a STALE SURVIVOR: its escalated successor (the epic-6 G-R2 entry below) closed 2026-07-17 with all 20 decks accepted; this row had simply never been flipped
  - epic: 5
    action: "story 6.1 must run the epic-4 end-to-end-field checklist: enumerate every
      deck_cards write path (add_card_to_deck, import_decklist, any upsert column
      list) before claiming end-to-end"
    owner: "Sathias"
    status: done  # 2026-07-17: story 6.1 AC 6 enumerated all 11 deck_cards write paths with per-path dispositions; re-verified against the final diff
  - epic: 5
    action: "story 6.2 must reuse _name_keys (src/logic/assessment/combos.py) for
      Spellbook->Scryfall name normalization — the DFC front-face hazard bit stories
      5.3, 5.6 and 5.9"
    owner: "Sathias"
    status: done  # 2026-07-16 story 6.2: _name_keys relocated to src/data/schemas/combo.py as public name_keys(); importer + matcher share it (matcher tests passed untouched)
  - epic: 5
    action: "after 6.2/6.3 land, re-run the benchmark swapping 5.9's hand-built cEDH
      combo variants for real snapshot data (candidacy also depends on CEDH_TUTOR_MIN=3)"
    owner: "Sathias"
    status: done  # 2026-07-16 story 6.3 Task 5: all 6 Commander benchmark entries met expectations with real snapshot data — zero divergences, no retuning
  - epic: 6
    action: "G-R2 real-deck sanity gate (blocks Epic 7 story creation): harness loads
      Sathias's real saved decks from the live DB, pulls variants via ComboSnapshotRepository.get_variants_for_names,
      runs pure score(); Sathias compares tier/Bracket/cEDH outputs against his own
      judgment — done when accepted as plausible or each divergence is logged as a
      named Epic 7 calibration input. Closes epic-5 real-deck item"
    owner: "Sathias"
    status: done  # closed 2026-07-17 (commit e2b03ff): all 20 decks accepted wholesale; cross-deck observations stand as the named Epic 7 calibration inputs (gate report §Cross-deck observations)
  - epic: 6
    action: "construction-site enumeration: when a field threads through multiple
      constructors, grep EVERY construction site — including error/fallback-path result
      objects — before claiming end-to-end (6.1 review found the commander flag dropped
      in the DatabaseError-path DeckImportLineResult, a surface the write-path checklist
      missed)"
    owner: "Sathias"
    status: done  # closed 2026-07-18 (epic-7 retro): exercised in all five Epic 7 stories; 7.3's re-verification caught the stale site count (7, not 6). Promoted to a standing team agreement.
  - epic: 7
    action: "gate-output homing rule: any gate/review that produces 'named inputs'
      must attach each to a story key, spec, or sprint-status gate in the same commit
      that closes the gate — a label with no key is how the G-R2 calibration inputs
      went unowned for a whole epic"
    owner: "Sathias"
    status: done  # closed 2026-07-26 (C1 retro): exercised in all 9 stories — c1-7 homed the Windows replace-while-open hazard, c1-8 ruled on it and re-homed to c6-1, c1-9 closed the launch race + remove_discovery TOCTOU in place recording what shipped. Promoted to a standing team agreement.
  - epic: 7
    action: "error-contract enumeration: when a story adds awaited I/O to an existing
      tool path, enumerate every call against the error-contract guard that covers
      it (7.2 review found _provision_combos' three DB reads outside the DatabaseError→error
      guard — sibling discipline to construction-site enumeration)"
    owner: "Sathias"
    status: done  # closed 2026-07-26 (C1 retro) in its strongest form: c1-6 registered DatabaseError→503 database_unavailable inside install_error_handling BEFORE any data route existed, so c3-1 onward inherit the guard with no per-route ceremony. Promoted to a standing team agreement.
  - epic: 7
    action: "data-layer orphan story keyed in sprint-status (data-layer-orphan-handling)
      so the thrice-documented get_deck_with_cards ValidationError crash + dead cards_unresolved
      signal stop floating in story notes"
    owner: "Sathias"
    status: done  # keyed 2026-07-18 at this retro (backlog key above Epic 7's action-item source)
  - epic: 7
    action: "close + promote construction-site enumeration to the standing team agreements"
    owner: "Amelia"
    status: done  # executed in-retro 2026-07-18 (epic-6 item above flipped to done; agreement recorded in epic-7-retro-2026-07-18.md)
  - epic: 8
    action: "forward-dated-comment homing: a comment or docstring asserting a FUTURE
      story's state (a log level chosen around an absent root handler, a promised
      flag, 'story X does this here') is a deferred edit — give it a story key in
      the same commit that writes it, like a gate output. c1-9 had to repair six such
      sentences across four files as an AC; it only worked because they were greppable"
    owner: "Brad"
    status: done  # closed 2026-07-30 (C2 retro): met its success criterion — c2-6 AC 20 repaired four forward-dated sentences as ACs (and judged + KEPT a fifth, clean.tsx:4, as purpose-not-future-state), c2-7 AC 20 did the same for its own inventory. Found in the story's own ACs, not by grep. Promoted to a standing team agreement.
  - epic: 8
    action: "open-question homing: an 'Open questions for Brad' item whose answer
      changes a LATER story's work is homed on that story key when raised, not left
      in the story record. 20 were raised across C1 and 5 answered; two of the survivors
      (c1-5 OQ2 dev-proxy Host, c1-2 OQ2 /docs) landed in C2's path and had to be
      ruled at the retro"
    owner: "Brad"
    status: done  # closed 2026-07-30 (C2 retro): met its success criterion — no C2 story discovered a C1 question mid-implementation; c2-1 arrived carrying R1 changeOrigin pre-annotated on the epic. C2's own 50-odd open questions were answered per story, and the last five (c2-10 Q1-Q5) were all ruled 'as proposed' — the 9th story running. Promoted to a standing team agreement.
  - epic: 8
    action: "close the --platform mypy gate gap in c2-1: add the opposite-platform
      mypy invocation to ci.yml (or .pre-commit-config.yaml) while c2-1 adds the frontend
      job. singleton.py branches on sys.platform and each run checks only its own
      half — today the POSIX half is covered only because CI happens to run on ubuntu.
      Annotated on epics Story 2.1"
    owner: "Brad"
    status: done  # closed 2026-07-26 by story c2-1: ci.yml's `quality` job gains `uv run mypy src/ --platform win32`. NOT `--platform linux` as the epic text said — CI is ubuntu, where the bare run IS the linux run, so that would have been a no-op and the retro's success criterion would still not hold. Proved by AC 17's deliberate break: with msvcrt.locking() one arg short, --platform win32 reports "Too few arguments for 'locking' [call-arg]" and exits 1 while --platform linux stays green on the identical code. Break reverted; src/ byte-identical.
  - epic: 8
    action: "c2-9 must ship the internal_error state-panel copy (the sixth token shipped
      without a UI state; AD-16 requires them together) and also inherits the c1-6
      corrupt-database ruling — a durably corrupt cards.db answers the quiet-retry
      'Database updating' state forever with no repair path. Both need verbatim EXPERIENCE.md
      copy before the panels are built"
    owner: "Brad"
    status: done  # closed 2026-07-29 by story c2-9, and closed the way the item asked — BOTH rows are in EXPERIENCE.md's Voice-and-Tone copy table, not in TypeScript. `Internal error` (Q4): Headline "The companion hit a bug." / Body "Restart the companion in your terminal (`artificial-planeswalker companion`). The traceback is in that terminal — it's what a bug report needs." — deliberately NOT a retry, which is the distinction types.d.ts:67-69 draws between internal_error and database_unavailable, now machine-readable as RETRIES_QUIETLY['internal-error'] = false in states.ts. `Database updating, stalled` (Q5, the c1-6 ruling): Headline "Card database still updating." / Body "Reads haven't resumed for a while. Check your agent session — if no import is running, ask it to rebuild the database (`initialize_database`)." — the backend is unchanged (it genuinely cannot tell mid-import from garbage, per decide-once #4), so the distinguisher is elapsed time on the CLIENT and the threshold + switch are homed at c3-9, recorded in deferred-work.md. Both panels ship in src/components/StatePanel/ and both are asserted BYTE-FOR-BYTE against EXPERIENCE.md by tests/copy.test.ts, which reads the artefact itself — so the copy cannot drift from the contract without reddening.
  - epic: 8
    action: "confirm the PLANESWALKER_COMPANION_PORT env-var name during manual testing
      (checklist item 5) so c8-4 documents a settled name — unconfirmed since c1-3"
    owner: "Brad"
    status: done  # RULED 2026-07-26 at the C1 retro: renamed to COMPANION_PORT. Vendor prefix dropped; the COMPANION disambiguator kept deliberately, because MCP_TRANSPORT already contemplates sse/streamable-http and an MCP server over HTTP would need its own port (a bare PLANESWALKER_PORT could not tell the two apart). One constant in server.py:58 + two docstring/usage sites in __main__.py; tests read server.PORT_ENV_VAR so none changed. Gates green (1,684 passed / 1 skipped); plugin mirror rebuilt.
  - epic: 8
    action: "annotate c2-1 with the R1 changeOrigin requirement in epics-companion-app.md,
      per the gate-output homing rule"
    owner: "Amelia"
    status: done  # executed in-retro 2026-07-26; Story 2.1 now carries the changeOrigin AC + the action-item-3 mypy note
  - epic: 9
    action: "R1 — home `card_not_found` on c3-2 WITH its EXPERIENCE.md copy row, before
      the token ships. AD-16 requires a new reason token and its UI state together;
      C1 shipped `internal_error` alone and it cost c2-9 a repair AC. c3-2's ACs must
      name the unknown-card placeholder copy (FR-13) and c3-2 must not merge with
      the token alone."
    owner: "Brad (c3-2)"
    status: done  # closed 2026-07-31 by c3-2, in the form the item asked for: the token, all seven ripple sites and the verbatim EXPERIENCE.md row landed in ONE commit, with ui/tests/unknown-card-copy.test.ts asserting the "Unknown card" label against the artefact READ FROM DISK. AD-16 applied prospectively for the first time — c3-4 and c3-5 then repeated it unprompted (forbidden; no_image_data + image_fetch_failed two-at-a-time)
  - epic: 9
    action: "R2 — c3-9 stays ONE story and enumerates its five inherited deferrals
      as ACs at context time: the stalled-DB elapsed-time threshold + switch, runtime
      validation of wire values before StatePanel's `state` prop, the un-quoted EXPERIENCE.md
      copy-tail gate, a look at the five never-rendered states, and C1 checklist item
      4 (fresh install against an empty data dir). Flag it at sprint planning as the
      epic's heaviest story."
    owner: "Brad (c3-9)"
    status: done  # closed 2026-08-02 by c3-9 and it MET its success criterion exactly: shipped as ONE story with Q1-Q10 ruled at context time, and none of the five was discovered mid-implementation. Nine of ten questions ruled as proposed (Q5 refined). The story's own review then re-homed AC 11/25's browser halves to the epic manual-testing checklist as a DECISION
  - epic: 9
    action: "R3 — run the 14-item C2 manual-testing checklist BEFORE the feat/companion-c2
      -> master integration PR, per the C1 precedent that caught the COMPANION_PORT
      rename while it was still free. Item 1 (the footer underline) is the only proof
      the inline-block fix has; item 11 (Badge's tone wash) fails as invisible text."
    owner: "Brad"
    status: done  # AMENDED + CLOSED 2026-07-30: 9 of 14 items run, 5 CARRIED with homes (item 9 -> c2-6 if it fails, item 5 -> c8-4/c8-5, item 4 -> c4-11, items 7-8 + 14 -> Epic 8). The item R3 existed for is closed — the inline-block underline fix is verified on screen. Merge != release, so nothing carried reaches a user; the integration PR proceeds.
  - epic: 9
    action: "R4 — sprint-status `last_updated` is a date plus ONE clause from now
      on; the per-story narrative lives in the story records. The YAML-parse half
      of the c2-2/c2-3 deferral self-closed (a later story quoted the scalar; yaml.safe_load
      succeeds on the committed tree at 858cabb), but the line had reached 6,438 characters
      and made every diff of this file unreadable. Truncated in-retro."
    owner: "Amelia — done in this retro"
    status: done  # executed in-retro 2026-07-30: 6,438 -> 295 chars; yaml.safe_load re-verified on the edited file
  - epic: 9
    action: "one `what the gates cannot see` map for ui/: the declared-residue list
      now spans ~8 guards (cascade-blindness in the numeric-pairing AND companion
      guards, git ls-files untracked blindness, block-local parsers, the REVIEWED_HOSTS
      baseline, runtime-composed classes, var() indirection, the unstacked-curve-bar
      half, the copy guard's second-person half). Each is documented at its own guard;
      nobody has the list. Collect it into one ui/README.md section so a reviewer
      inherits a map instead of re-deriving it."
    owner: "Brad (c3-1 or the first C3 frontend story)"
    status: done  # closed 2026-07-31 by c3-1 (AC 21): ui/README.md's blind-spot map shipped with 11 rows, every citation verified against the guard's own source. GREW through the epic rather than rotting — c3-3, c3-7 and c3-9 each added rows, and c3-3's review found the map UNDER-DECLARING its own guard's holes, which is only findable because the map exists ("a declared blind spot is still a claim")
  - epic: 9
    action: "keep the same-day three-layer review before every PR — it is now THRICE-confirmed
      as the round-1-5/5 cause (c2-5, c2-8, c2-10 cleared in one round; c2-4 and c2-6
      took three Greptile rounds without it). Standing, not a one-off."
    owner: "Brad (standing)"
    status: done  # closed 2026-08-02 at the C3 retro: 9 for 9, and Critical/Major stayed at zero across ~130 patches. Its CLAIMED effect did not improve, measured: round-1 Greptile clears were 3 of 10 in C2 and 3 of 9 in C3 — flat, not rising. What C3 measured instead is WHERE the survivors live (5 of 10 findings in app/images.py; three consecutive stories with the finding in REVIEW-ADDED code), which is C3 action item 12. Standing agreement, unchanged
  - epic: 10
    action: "R1 — the Scryfall JSONL migration is a HOTFIX ON MASTER, now, ahead of
      the C3 integration PR. Scryfall replaced download_uri -> jsonl_download_uri
      and size -> compressed_size and moved from a top-level JSON array to gzip'd
      JSONL; every data path is dead on a public released product. Format change,
      not a key rename: parser.py:45 uses ijson.items(f, 'item')."
    owner: "Brad"
    status: done  # closed 2026-08-02, PR #38 merged at 7631147 — BOTH halves of the success criterion met: the importer completes against the live API (38,485 cards, 0 errors) AND tests/integration/data/test_scryfall_live_contract.py calls api.scryfall.com/bulk-data through the production resolver. Greptile 5/5 round 1, zero inline findings — NOTE for the record: this PR did NOT run the same-day three-layer review (it was pushed on request), so it is one data point either way, not evidence about that agreement
  - epic: 10
    action: "confirm A2 — watch the live 503 -> 200 transition in a real browser.
      FR-22 is the reason c3-9 exists and it is the epic's single highest-value unverified
      claim; the DOM half is gated, the visual half has never been seen."
    owner: "Brad"
    status: done  # CONFIRMED 2026-08-02 by Brad: a cards.db planted into an empty PLANESWALKER_DATA_DIR with the tab open made the page come alive with NO refresh. FR-22 observed, not merely DOM-gated. Closes A7 by implication — a browser caching the 503 would have prevented exactly this, so `cache: no-store` is honoured in practice (jsdom has no HTTP cache, so it had only ever been a source-level assertion)
  - epic: 10
    action: "complete the manual-testing checklist before the integration PR — A3-A7,
      B6, Blocks C/D/E. Findings become fixes on the C3 umbrella, per the C2 R3 precedent."
    owner: "Brad"
    status: done  # CLOSED 2026-08-02 in its amended form, exactly as C2's R3 was: A1/A2/A7 + Block B run, remainder CARRIED WITH A NAMED HOME EACH (table in the retro's Manual-Testing Outcomes). The two make-or-break claims are observed — FR-22 comes alive in a real browser, CM-2 is a measured zero-request warm run. Homes: A3/A4 -> c4-2 (+ c5-6 for A4's recovery), A5/A6 -> c5-6, B6 -> c4-4, C1/C2 -> c4-1/c4-2, C3/C4 -> c4-10, C5 -> c4-1, C6 -> c5-5, D1 -> c8-6 (NEWLY OWNED — AD-1's two-shells-one-implementation claim had no owner), D2 -> c8-4/c8-5, E1 -> c4-11, E2 -> c2-6. Known trade recorded rather than discovered later: A3-A6's three panels are still unrendered by a real engine, and c4-2 makes a failure ambiguous between panel and wiring
  - epic: 10
    action: 'F1 — a gate banning story-key-shaped strings from rendered UI text. Ban
      the FAMILY (a word-bounded c<digits>-<digits> regex — kept out of literal regex
      syntax here because a backslash escape breaks this file''s YAML), not the three
      known placeholders. A1 found c2-7/c4-2/c4-10/c6-8/c4-5/c4-7 rendering on the
      fresh-install first-run view; all are homed and correct today, and nothing anywhere
      would stop one surviving to c8-4.'
    owner: "Brad (c8-5, or earlier if a C4 story is nearer)"
    status: open  # success criteria unchanged: one test refuses a planted `c9-9` in a component's rendered text. PREMISE CORRECTED at the C4 retro (2026-08-07): C3's census of six keys never looked for a `c6-*` key, and the ONE survivor on a rendered deck view is `c6-8` in AppShell.tsx:117's nav placeholder -- on the glass on every surface since c2-6, not a c4 key at all. All six original keys are displaced, each by its own panel, asserted both ways in App.test.tsx. Gate still owed; still c8-5's. ** PREMISE CORRECTED A SECOND TIME, BY MEASUREMENT, LATER THE SAME DAY (Block I run):** "one key remains" is true only OF A RENDERED DECK VIEW, which is the only surface App.test.tsx ever asserts. Measured on a STATE-PANEL surface in a real browser: SIX keys render -- c2-7, c4-2, c4-5, c4-7, c4-10, c6-8, seven occurrences -- because the placeholders are displaced BY THE PANELS THAT REPLACE THEM, so any surface where those panels do not render still carries every one. The first screen a fresh install ever sees is exactly such a surface. Both halves confirmed in one run. PRIORITY RAISED and the gate's shape now specified by evidence: it must scan a rendered state-panel surface, not only a deck view.
  - epic: 10
    action: "adopt a live-contract canary — any contract owned by a THIRD PARTY gets
      at least one test that reads the third party. The Scryfall break was invisible
      to 2,472 tests because every one monkeypatches fetch_bulk_data_list. Proposed
      as a standing agreement; needs ratification."
    owner: "Brad"
    status: done  # RATIFIED 2026-08-07 at the C4 retro, on its own deadline, as a standing team agreement in its full form: any contract owned by a THIRD PARTY gets at least one test that reads the third party, run ON A CLOCK rather than on a commit. First and only instance: tests/integration/data/test_scryfall_live_contract.py, weekly. Noted at ratification: C5 adds no third-party contract, so the agreement will not be exercised next epic -- its first real test is whenever one arrives.
  - epic: 10
    action: "F4 — the failed-import / companion file-lock interaction needs a home.
      A failed first import leaves a schema-only cards.db; the companion's next poll
      opens it under c1-6's lazy engine and the user can then neither delete nor replace
      it while the companion runs. Recovery path, not import path — a second process
      WRITING into it is fine under WAL."
    owner: "Brad"
    status: done  # CLOSED 2026-08-07 at the C4 retro (ruling R3), one epic late -- and the lateness is itself the finding: the item asked for a ledger entry, no story wrote one, and a grep at this retro confirmed deferred-work.md had nothing. Entry now written with the measurement (display is correct; WAL permits a second writer so only wholesale file replacement is blocked; recovery-path not import-path) and HOMED ON c8-4, install/first-run readiness. Severity Medium -- reachable on the public v0.4.0 today.
  - epic: 10
    action: "correct the epic's '~12 MB / ~124 KB' image figures to the MEASURED 8.5
      MB / ~90 KB, and record ~99 ms CDN latency + ~10.3 ms warm-read where c4-4 will
      look."
    owner: "Amelia"
    status: done  # executed in-retro 2026-08-02: deferred-work.md's "never measured" entry resolved with the measured table; ui/README.md's c4-4 row carries the latency, the warm read and the 4x semaphore headroom (prettier-formatted, format:check green, 731 frontend tests re-run)
  - epic: 10
    action: "correct c3-8's images.py line count 1,475 -> 1,837 in deferred-work.md
      with the prose/code breakdown, so the parked split decision resumes from a fact
      rather than the impression that entry existed to prevent."
    owner: "Amelia"
    status: done  # executed in-retro 2026-08-02: 1,837 total = 1,370 prose (74.6%) + 377 code (20.5%) + 289 blank, measured two independent ways. Split PARKED by Brad pending manual testing; re-decide with c4-1's hydration cache in view
  - epic: 10
    action: "R2 — promote the banned-family lifecycle to a standing team agreement:
      a story owning a banned identifier family must retire it, re-key it, or keep
      it with a written reason, and a replacement must be probed against the spellings
      the RETIRED ban caught."
    owner: "Amelia"
    status: done  # executed in-retro 2026-08-02; recorded in epic-c3-retro-2026-08-02.md and the deferred-work entry closed. Worked failures: c3-6 (`from time import sleep`) and c3-7 (`Path.replace`) — in both cases the retired ban DID catch what its replacement missed. c3-8 is the worked keep
  - epic: 10
    action: "R3 — record database-updating-stalled's terminal consequence and confirm
      c5-6 owns all three siblings. ACCEPTED: RETRIES_QUIETLY stays false, NO EXPERIENCE.md
      amendment; a user who does what the panel says and succeeds still needs a manual
      refresh."
    owner: "Amelia"
    status: done  # executed in-retro 2026-08-02: the deferred-work entry re-homed from "C3 retro" to c5-6 beside its two siblings, so c5-6's reconnect resolves the family rather than a third of it
  - epic: 10
    action: "keep the same-day three-layer review before every PR — 9 for 9 in C3
      and still the mechanism holding Critical/Major at zero. Standing."
    owner: "Brad (standing)"
    status: done  # MET 2026-08-07: 12 for 12 across the epic's largest UX surface, ~270 raw findings -> ~150 patches, Criticals at ZERO. And this time its claimed effect DID move, measured from the GitHub API rather than the PR summary score: Greptile round-1 clean went 3/10 (C2) -> 3/9 (C3) -> 9/12 (C4), one round on every PR, 3 inline findings total, and none at all from c4-6 onward. Re-adopted as standing for C5.
  - epic: 10
    action: "review-added mechanisms re-enter review. A review patch that adds a MECHANISM
      (not a test, not prose) gets one adversarial pass before the commit lands. Three
      consecutive stories (c3-7, c3-8, c3-9) had Greptile's confirmed finding in code
      their own three-layer review had just added — the ~13 patches a review produces
      are the only lines in a story no layer ever looked at."
    owner: "Brad (standing, from C4)"
    status: done  # 11 of 12, and it FAILED ON THE VERY FIRST PR -- c4-1's Greptile P2 (an orphaned hydration promise resolving with the discarded generation's entry) sits squarely in the `generation` counter that c4-1's OWN three-layer review added, i.e. the rule arrived one story too late for the story it was written for. It then held for eleven consecutive stories: the only other two findings in the epic (c4-5's P1, c4-10's P2) were in original implementation, and c4-10's was withdrawn by Greptile itself. Re-adopted as standing for C5, unchanged.
  - epic: 11
    action: "R1 — trigger-gated inheritance, from c5-1. A story's context carries
      IN FULL only the deferrals its own surface can TRIGGER; everything else is one
      line plus a ledger anchor; the don't-break list caps at items the diff's own
      files can touch. Measured driver: per-story Dev Notes went 15 KB (C2) / 16 KB
      (C3) -> 41 KB (C4), compounding WITHIN the epic (c4-1 17 KB -> c4-9 60 KB) as
      inherited deferrals went 9 -> 15 and don't-breaks 9 -> 20 with nothing ever
      retiring."
    owner: "Brad (c5-1 onward)"
    status: done  # closed 2026-08-09 (C5 retro): criterion met on EVERY measured story — 20.5 / 19.3 / 10.4 / 14.4 / ~14.5 / ~13 KB against the 41 KB baseline, largest frontend story at a third, with +496 tests across the epic (verification did not thin). Minor gap: no KB self-check recorded in c5-5/c5-8. Promoted to a standing team agreement.
  - epic: 11
    action: "R2 — every new guard ships a FIRING PROOF: (a) a planted violation shown
      RED through the FULL npm test, never a standalone file run, and (b) one line
      stating what the assertion actually compares, read against the code rather than
      its own comment. Extends the non-vacuity-pairing agreement with the failure
      this epic committed SEVEN consecutive times (c4-6..c4-12), four of them High
      and five in the story's own flagship guard."
    owner: "Brad (standing)"
    status: done  # closed 2026-08-09 (C5 retro): criterion met 8 for 8 — ~90 planted violations through full suites, and the FOUR probes that came back GREEN (c5-5 x2, c5-4, c5-7's P15) each exposed a real guard hole and changed the shipped diff, which is the practice working, not failing. Promoted to a standing team agreement; the next frontier is the observation MECHANISM (jsdom/in-process blindness), owned by c5 items R3/R5.
  - epic: 11
    action: "R3 — ledger F4 (a failed first import leaves a schema-only cards.db the
      companion then file-locks) and home it on c8-4. Closes C3 retro action item
      6, which asked for exactly this and was never written."
    owner: "Amelia — done in this retro"
    status: done  # executed in-retro 2026-08-07: entry written into deferred-work.md with the WAL/blast-radius measurement and Home c8-4, severity Medium
  - epic: 11
    action: "One committed probe harness — validates the collected-test count, refuses
      a run carrying the vitest crash signature, uses a native uppercase-drive path,
      and carries the do-nothing negative controls. The harness lied FIVE times in
      C4 (lowercase-drive cwd x2, the standalone-runner crash, subprocess shell=True
      on Windows, a probe emitting unparseable TSX) and every story rebuilt the validation
      by hand."
    owner: "Brad (c5-1)"
    status: done  # closed 2026-08-09 (C5 retro) as RE-KEYED, not complete: the pytest half SHIPPED at c5-1 (scripts/probe_harness.py, hardened mid-story when it caught a SyntaxError plant reading as "0 failed", used by every C5 backend story) — the vitest half is STILL OWED and is now epic-c5 item R5 below, with a concrete home (before Epic 6's first frontend story). c5-7 ran 15 frontend plants BY HAND, which is the cost of the missing half, measured.
  - epic: 11
    action: "Fix the DESIGN.md citation guard: resolve the anchor and assert the cited
      line names the component, then re-base the ~60 stale DESIGN.md:NNN anchors across
      25 files. shell.test.ts currently requires only the STRING 'DESIGN.md' near
      every px literal and never resolves the line number; at least one citation now
      points at a real but WRONG component (FormatCheck.css -> DESIGN.md:423, the
      card-tile bullet)."
    owner: "Brad (C5, standalone)"
    status: done  # closed 2026-08-09 (C5 retro) as RE-KEYED, not complete: the ONE C4 item C5 never referenced at all, while stories kept writing NEW DESIGN.md:NNN citations against the still-blind guard. Now epic-c5 item R6 below, carrying a TERMINATION CLAUSE: not done by the C6 retro -> formally decline and demote the guard to a declared string-proximity check.
  - epic: 11
    action: "'Grep your own key' becomes a step in every story's Task 0 — grep -rn
      '<story-key>' ui/src src tests — not only a deferred-work.md read. FIFTEEN shipped
      source modules named c4-12 in their headers while the ledger named it twice,
      which inverts what the ledger is for."
    owner: "Brad (c5-1 onward)"
    status: done  # closed 2026-08-09 (C5 retro): run in 7 of 8 stories (no section found in c5-5) and it EARNED ITS KEEP — c5-1's sweep caught dump_openapi.py's ownership error, c5-7's caught falsified-prediction copies in a Python test docstring its expected set missed. Promoted to a standing team agreement.
  - epic: 11
    action: "Plugin-mirror check reachable from ui/ so a frontend-only npm test can
      see a stale mirror. The mirror IS guarded (tests/unit/companion/test_spa.py::TestThePluginMirror
      byte-for-byte, plus CI drift) but only on the Python side — and untracked bundle
      assets were a HIGH finding twice (c4-3, c4-7), both times the frontend-side
      blind spot."
    owner: "Brad (C5)"
    status: done  # closed 2026-08-09 (C5 retro) as RE-KEYED, not complete: left open all epic, it produced EXACTLY the predicted failure — c5-6's SPA bundle drift caught by CI post-push, not by npm test. Second epic running for this defect class (C4's two Highs at c4-3/c4-7). Now epic-c5 item R7 below, success criteria unchanged.
  - epic: 11
    action: "R5 — close four ledger items with their measurements: ETag on /api/cards/{id}
      SUPERSEDED (the cache makes the worst case structurally impossible and no-store
      would make an ETag inert); the images.py split DECLINED (c4-4 and c4-6 exercised
      route, pacer, disk cache and negative cache from a real browser and needed no
      change to any); <div>-in-<button> ACCEPTED (spec-letter violation, zero measured
      accessibility impact, c4-6 closed the harder interactive version); EXPERIENCE.md:34
      and :173 AMENDED (deck value is a price that does not exist, source counts appear
      in no artefact — so StatChip has no pending surface)."
    owner: "Amelia — done in this retro"
    status: done  # executed in-retro 2026-08-07: four dispositions appended to deferred-work.md; EXPERIENCE.md both rows amended using the c4-7 DESIGN.md price amendments as precedent
  - epic: 11
    action: "Run manual-testing Block I before the integration PR — A3/A4/A5/A6, the
      four state panels a real engine has STILL never rendered. Homed on c4-2 at the
      C3 retro; c4-2 acknowledged and did not run them, so they are now carried across
      two epics and the wiring only grows. Worth adding on the same run: c4-2's edge-triggered
      re-drive (cold open during a DB build, let it finish, confirm the glass leaves
      the stale 503 panel with NO refresh) — the High that story fixed and which has
      never been seen."
    owner: "Brad"
    status: done  # RUN 2026-08-07, headless Chrome over CDP against a real backend on an ISOLATED PLANESWALKER_DATA_DIR (the shipped cards.db copied once as the restore source; nothing written to the real data dir). A1 control PASS; A3 PASS and its whole point confirmed — a corrupt cards.db gives 503 database_unavailable -> "Card database is updating.", a DIFFERENT panel from A1's out of the SAME status; A4 half 1 PASS, escalated at t=60.1s on the 6th poll with both gates (60s AND 4 refusals) observed independently; A4 half 2 PASS as designed — C3 ruling R3 felt for the first time: database restored, wire 200, poll count moved by EXACTLY 0 over 45s, glass unchanged, recovers only on refresh; A5 PASS — panel unchanged, kept retrying 2->7 polls, never claimed disconnected, no stack; A6 CONFIRMED the ledgered defect and its tolerability judged (worse than recorded — the copy is actionable and wrong, home stays c5-6). BONUS: c4-2's edge-triggered re-drive CONFIRMED LIVE — deck reads 1->3, stale panel -> full deck view in ~5s, ONE page navigation, no reload. Two new findings: F1's census is wrong (SIX keys render on a state-panel surface, not the recorded one — the assertion only ever covered a deck view; C3 item 4's priority raised and the gate's shape now specified) and C3's F3 vertical anchoring is only half closed by c4-12. Only `internal-error` remains unrendered by a real engine; re-homed to the C5 checklist.
  - epic: 11
    action: "Sync .gitignore's /graphify-out/ hunk from master onto feat/companion-c4
      before the integration PR. The hunk went to master at 2f543ed (c4-3 ruling)
      and the epic branch never received it, so git add -A on the branch stages thousands
      of cache files. Fix is a master -> branch sync, not a hunk smuggled into the
      integration diff."
    owner: "Brad"
    status: done  # DONE 2026-08-07, merge commit 319a966. Brought across as a MERGE of master (its only commit the branch lacked was 2f543ed) rather than a cherry-pick, so the integration PR sees the same sha on both sides and cannot conflict on it. Dry run was clean. Verified three ways: git check-ignore resolves graphify-out/ to .gitignore:24; git status no longer lists it; and `git add -A --dry-run` now stages 4 paths instead of 534 — the 530 files of the regenerable graph index are out of reach of a stray add, which is the failure c4-11 hit by hand.
  - epic: 12
    action: "R1 — add a Windows integration lane to ci.yml running the companion real-socket
      test (scoped tests/integration/companion/, the file itself runs in ~4-5 s):
      the only test covering the process boundary (AD-10) is deselected by the ubuntu
      `-m 'not integration'` run on every push and would rot silently. Ruled YES at
      the C5 retro (dw ruling at the CI-gap entry)."
    owner: "Brad (C6 prep)"
    status: done  # SHIPPED TO MASTER 2026-08-09 via PR #62 at `4cf4bd6`. `companion-integration` job: windows-latest, py3.12, path-scoped to tests/integration/companion/, `timeout-minutes: 15` (added in review — a wedged child would otherwise burn the 6h default and, since cancel-in-progress is false for master, queue every later master run). First CI run green: `collected 1 item`, `1 passed in 7.20s`, 33s job total — which also MEASURES the reviewers' _BOOT_DEADLINE concern at ~4x headroom (cold CI is ~1.8x the warm local run, not an order of magnitude), so the constant stays untouched. Six prose sites swept, not the three planned: the first-pass verification grepped the SENTENCE being fixed rather than the CLAIM, and missed .gitattributes:29 (the most load-bearing site — this job is the first thing in CI ever to check the repo out onto a machine where core.autocrlf can be true), ui/.gitattributes:21 and test_scryfall_live_contract.py:16. TWO FALSE CLAIMS THE REVIEW CAUGHT AFTER THEY HAD ALREADY REACHED A SHIPPED WORKFLOW COMMENT: (1) `-m integration` never swept in the test_list_decks_with_strategy_field flake — it carries no marker and has always run in the `quality` jobs; path-scoping is right for a stronger reason (the marker selects live-fastembed tests). This error came from dw:5728 and propagates into R4 — see the R4 row. (2) Windows-as-platform-of-record is NOT "AD-2" (that is "the MCP server is the only writer"); no AD names a platform of record. Greptile 5/5, all five checks green. TWO THINGS EXPLICITLY NOT DONE: the FIRING PROOF was not run — R1 merged proving the lane RUNS, not that it DISCRIMINATES (probe_harness.py is pinned to `not integration` and structurally cannot plant here, so only a deliberate-break push can prove it); and `companion-integration` is NOT a required check, so a red lane does not block a merge. Both carried — see the two follow-up rows below.

  - epic: 12
    action: "R1-a (CARRIED from R1) — the firing proof. The Windows lane has never
      been observed RED. Plant a break in a seam the in-process suite structurally
      cannot reach (subprocess dispatch, bind->publish ordering, or the c1-8 reclaim
      path — NOT mint_token, which test_discovery.py::test_minted_tokens_are_never_repeated
      pins on ubuntu, and NOT raw socket bind, which test_server.py/test_client.py
      cover), verify `-m 'not integration'` stays GREEN locally on the planted tree,
      push, observe red-on-Windows/green-on-both-ubuntu, revert. Record both run URLs
      — the evidence is not reproducible later."
    owner: "Brad"
    status: declined  # TERMINATION CLAUSE EXECUTED 2026-08-13 (C6 retro, delegated): not done by the C6 retro, so formally declined and the lane's claim is DOWNGRADED from "covers the process boundary" to "runs the process-boundary test". Annotation: any future GENUINE red on the companion-integration lane retroactively supplies the firing proof — record the run URL and upgrade the claim then. The lane stayed green and useful all of C6.

  - epic: 12
    action: "R1-b (CARRIED from R1) — add `companion-integration` to branch protection
      as a required check. Until then a red lane does not block a merge and R1's stated
      purpose (a test with no automated home rots silently) is only half delivered."
    owner: "Brad"
    status: done  # closed 2026-08-13 (C6 retro) as RE-KEYED, not complete: still a two-minute GitHub settings change nothing in the tree can drift-check. Now epic-c6 item R6 below, executed with the C6 integration PR.
  - epic: 12
    action: "R2 — prose-sync sweep, one standalone pass: cross-module rulings get
      ONE canonical home (the deferred-work ledger entry that records the ruling);
      convert the 5+ Q3/AD-5 narration sites (state.py, security.py x2, main.py, test_routes_active_deck.py)
      to one-line pointers; DELETE scripts/dump_openapi.py's changelog paragraphs
      (content already in ledger + story records); absorb dw:5197's twice-confirmed
      one-sentence test_committed_schema.py docstring fix. STANDING RULE adopted with
      it: no new forward-looking cross-module prose in docstrings."
    owner: "Brad (C6 prep, standalone)"
    status: done  # closed 2026-08-13 (C6 retro) as RE-KEYED, not complete: the STANDING RULE half held through C6 (one c6-1 violation, patched in review, then 8 clean stories); the standalone sweep itself was never executed. Now epic-c6 item R7 below, scope unchanged, carrying a TERMINATION CLAUSE at the C7 retro.
  - epic: 12
    action: "R3 — repo-wide derived class->token guard, generalising c5-7 P15's fix:
      jsdom evaluates no stylesheet, so every status-semantic class->token binding
      (Badge tones, ManaPip colours, deck-row live tint, the pill dot) is invisible
      to DOM tests unless a source-reading guard binds it. One derived guard, shipped
      BEFORE Epic 6's first view story adds more surfaces of exactly this shape. Existing
      per-component guards kept, not deleted."
    owner: "Brad (C6 prep or first C6 UI story)"
    status: declined  # DECLINED BY BRAD 2026-08-09, formally rather than carried — the R6 precedent (a decline is better than a silent third carry). Built and reverted; the reasoning is in spec-r3-derived-class-token-guard.md (status cancelled). WHAT IT WOULD HAVE BOUGHT, measured not guessed: the guard reddened on a real plant (`.badge-positive` repointed to var(--negative)) with 1 failed / 1,872 passed — the hole is real, and it is exactly two components wide (Badge, StatChip). WHY DECLINED: review showed the "before Epic 6's first view story" justification was false — DESIGN.md's C6/C9 surfaces name colours by ROLE (swap row: out-tint/in-tint) and by letter grade, not by tone, so a name-matching guard would never see them. Cost/benefit did not survive that. Also found and NOT fixed: the family derivation re-introduced the open `--accent-*` prefix that CALM_TOKEN_FAMILY (token-usage.test.ts:1132) already ruled against with `exact: true`, both pairing proofs were vacuous, and the finder generated false positives on comma lists, `:not()` and descendant selectors. The per-component guards (P15's dot binding, ManaPip's 21-class gate) are untouched and still stand.
  - epic: 12
    action: "R4 — flake + marker, coupled: give the companion real-socket test its
      own pytest marker/scoped alias so a bare `-m integration` red says something
      (today it sweeps in the flake and the live Scryfall tests); make ONE bounded
      reproduction attempt on test_list_decks_with_strategy_field (sightings at c5-5
      and c5-6) — if it does not reproduce, annotate with both sighting dates and
      monitor; a THIRD sighting escalates to a real investigation."
    owner: "Brad (C6 prep)"
    status: done  # closed 2026-08-13 (C6 retro) as SPLIT AND RE-KEYED: the flake hit its THIRD SIGHTING at c6-7's baseline (2026-08-11), which this item's own terms escalate — and c6-7 ROOT-CAUSED it in the same sighting (same-microsecond created_at ordering under ties; file-in-isolation 56 passed). The escalated investigation is therefore already done; what remains is a deterministic ordering tiebreaker -> epic-c6 item R3 below. The marker/alias half (valid on the corrected model-and-network premise) -> epic-c6 item R4 below. [Premise correction of 2026-08-09 preserved: a bare `-m integration` never collected the flake — it sweeps live-Scryfall + real-fastembed tests; origin dw:5728.]
  - epic: 12
    action: "R5 — the vitest probe-harness half (RE-KEYED from C4 item 4): full `npm
      test` with a validated collected-test count before a run is scored, native uppercase-drive
      path, vitest crash-signature refusal, do-nothing negative controls. Three of
      the five recorded harness lies are frontend-specific; c5-7 ran 15 frontend plants
      BY HAND. Build before Epic 6's first frontend story."
    owner: "Brad (C6 prep)"
    status: done  # closed 2026-08-13 (C6 retro) as RE-KEYED, not complete: its home ("before Epic 6's first frontend story") was passed BY EXPLICIT RULING twice (c6-3 Q3, c6-5 Q6 — both declined to build a tool inside a feature diff, the c5-6/R3 precedent). Cost of absence, measured across C6: ~20+ plants hand-run across six frontend stories, and the vitest worker-fork flake SILENTLY DROPPED A WHOLE TEST FILE twice (c6-5) — the exact failure class the harness's validated-collected-count + crash-signature refusal automate; caught only by manual discipline every time. Now epic-c6 item R2 below: BUILD as C7 prep, scope fixed at the four recorded requirements, with a TERMINATION CLAUSE at the C7 retro.
  - epic: 12
    action: "R6 — DESIGN.md citation guard (RE-KEYED from C4 item 5, carried a second
      time BY EXPLICIT RULING): resolve the anchor, assert the cited line names the
      component, re-base the ~60 stale DESIGN.md:NNN anchors. TERMINATION CLAUSE:
      if not done by the C6 retro, formally decline and demote the guard to a declared
      string-proximity check so it stops reading as more than it is."
    owner: "Brad (C6 prep, standalone)"
    status: declined  # TERMINATION CLAUSE EXECUTED 2026-08-13 (C6 retro, delegated): not done by the C6 retro, so formally declined; the guard is DEMOTED to a declared string-proximity check (the one-comment demotion edit is epic-c6 item R10 below). Evidence recorded both ways: c6-7's review P3 shipped a WRONG citation value a resolving guard would have caught, but its P4 (a MISSING citation for half a shorthand) no guard of this shape would see — and the citation defects that did ship were caught by review. Third epic of carry is the drift the clause exists to stop.
  - epic: 12
    action: "R7 — plugin-mirror check reachable from ui/ (RE-KEYED from C4 item 7):
      npm test fails on a deliberately stale plugin/ mirror. Left open through C5
      it produced exactly the predicted failure (c5-6's CI bundle-drift catch, post-push)
      — second epic running for this defect class."
    owner: "Brad (C6 prep)"
    status: declined  # FORMALLY DECLINED 2026-08-13 (C6 retro, delegated) — the R3 precedent: a decline is better than a silent third carry. Evidence: C6 produced ZERO escapes of this class — 9/9 stories sha256-clean under the rebuild-after-last-edit standing rule (c6-6 and c6-8 each rebuilt TWICE to honour it; c6-9 verified both mirrors byte-identical) — and the mirror remains guarded by the Python byte-for-byte test plus the CI drift check. REOPEN TRIGGER: any new escape of this defect class reopens the item as written.
  - epic: 12
    action: "R8 — amend the standing review-added-mechanisms agreement to cover review-added
      TEST ASSERTIONS as well as mechanisms: c5-8's Greptile P2 was in an assertion
      its own review added; ruled NOT a retroactive violation (the rule as written
      scoped to mechanisms), amended prospectively."
    owner: "Brad (standing)"
    status: done  # closed 2026-08-13 (C6 retro) as ADOPTED — held as standing practice through all nine C6 reviews without incident (review-patch tests at c6-5/c6-8 landed inside the review pass itself and were re-verified there).
  - epic: 12
    action: "R9 — resolve UX-DR46's double assignment (the coverage map credits it
      to BOTH Epic 4 and Epic 5) before c6 story creation. The sibling drift was already
      executed in-retro: Story 6.4's cap-breach AC amended 422 -> 413 payload_too_large
      in epics-companion-app.md (5.5/6.1 were fixed at c5-5, 6.4 had been missed);
      the AD-6/AD-7 spine amendments stay homed on Epic 8."
    owner: "Brad (before c6-1 context)"
    status: done  # RULED 2026-08-09 (Brad, post-retro): DELIBERATE SPLIT, ANNOTATED — Epic 4 = the focus floor, Epic 5 = 46's connection-state half; UX-DR40's Epic 4/Epic 8 pair stamped as the same shape in the same stroke (Epic 4 states the cost, c8-6 revisits at release). The c4-11 defect note in epics-companion-app.md converted from warning to resolved ruling; both coverage-table rows now carry their slice inline.
  - epic: 12
    action: "R10 — AD-1 limit-literal family stays PRESENCE-KEYED with narrow per-file
      exemptions (c5-1's _LIMIT_FAMILY_EXEMPT shape — one file, one family, three
      tests with firing proofs — is the standing remedy); a use-keyed redesign was
      declined as costing more than the two recorded collisions did. A THIRD collision
      reopens the question."
    owner: "Amelia — done in this retro"
    status: done  # ruled 2026-08-09; disposition recorded inline at the ledger's dw:5104 entry and in the retro's Dispositions section
  - epic: 12
    action: "R11 — manual-testing Block J (table in epic-c5-retro-2026-08-09.md §8).
      RULED NOT RUN by Brad 2026-08-09: the WHOLE block is carried wholesale to the
      Epic C6 manual-testing checklist, c8-6 (SC-5 gate) as terminal backstop. Rationale:
      merge != release, and C6's own stories put eyes on most surfaces (6.3 absorbs
      the active-deck/two-tab checks, 6.9 the reconnect/degradation walk, daily dev-proxy
      use absorbs J5). Named consequence accepted: the integration PR merges with
      zero eyes-on-pixels verification of the C5 surface (pill colours, sub-1100px
      floor, exhaustion announcement). internal-error's first render is hereby carried
      a THIRD time (Block I -> C5 checklist -> C6) as the explicit ruling the Block-I
      precedent requires."
    owner: "Brad"
    status: done  # closed as ruled-and-carried 2026-08-09; the checklist items live in the retro doc's Block J table for the C6 retro to pick up. PICKED UP 2026-08-13: the C6 retro merged Block J with C6's own isolated-away surfaces into the Epic C6 manual-testing checklist (epic-c6-retro-2026-08-13.md par-8, J1-J8 + K1-K7) — epic-c6 item R1 below, recommended-run BEFORE the integration PR (the failed absorption premise is recorded in the retro doc par-3.2).
  - epic: 13
    action: "R1 — RUN the Epic C6 manual-testing checklist (epic-c6-retro-2026-08-13.md
      par-8: Block J's J1-J8 carried from C5, plus K1-K7 from what every C6 test isolated
      away) BEFORE the feat/companion-c6 -> master integration PR. Block J's C5 carry
      rationale ('C6's stories put real eyes on most surfaces') FAILED — c6-3 was
      ruled tests-only, c6-9 measured DOM timestamps headlessly; zero of eight J items
      were observed, and C6 added the agent view shell, crossfade, suggestion rows,
      header pills, empty-push line and every announcement to the unviewed surface.
      Running it here also spares J6 (internal-error first render) a FOURTH explicit
      carry ruling. Known suspect items get judged live: the Chromium disabled-button
      title tooltip (K4), the screen-reader run-on phrase (K5, Sally's call), the
      byte-identical-title announcement dedupe (K5)."
    owner: "Brad"
    status: done  # RUN 2026-08-13 against master c3ac8be (post-merge, per the timing override recorded below). OBSERVED as expected: J1, J2, J3, J7, J8, K1, K2, K4, K6, K7 — full run record in epic-c6-retro-2026-08-13.md par-8. Findings: the disabled-pill title tooltip FIRES in Chromium (c6-8's ledgered risk did not materialise — close its dw entry on this observation); empty-push copy ruled FINE (noun stays at 9.1); F5's discovery-corpse reclaim seen live in production. NOT RUN by Brad's ruling ('done enough'): K3, K5, J4, J5, J6 — all carried to c8-6, the checklist's terminal backstop. J6 is hereby carried a FOURTH time (Block I -> C5 -> C6 -> c8-6); that sentence is the explicit ruling the Block-I precedent requires. [Earlier: timing overridden by Brad post-retro — integration PR first, the C1 R3 precedent.]
  - epic: 13
    action: "R2 — vitest probe harness (RE-KEYED from C5 R5, second carry BY EXPLICIT
      RULING at this retro): full `npm test` with a validated collected-test count
      before a run is scored, native uppercase-drive path, vitest crash-signature
      refusal (the worker-fork silent-file-drop, sighted twice at c6-5), do-nothing
      negative controls. Value case measured in C6: ~20+ plants hand-run across six
      frontend stories; two silent file drops caught only by manual counting; C7 has
      five frontend-heavy stories coming. TERMINATION CLAUSE: not built by the C7
      retro -> formally decline and stop citing it."
    owner: "Brad (C7 prep)"
    status: done  # SHIPPED TO MASTER 2026-08-13 via PR #73 at `1f939a9`. `scripts/vitest_probe_harness.py` + 67 fixture-driven tests; all four recorded requirements discharged, all five checks green, Greptile 5/5 zero findings. TERMINATION CLAUSE NOT REACHED — built as C7 prep, one epic after its C5 R5 home was passed twice by explicit ruling. DESIGN: `--control` collapses requirements 1 and 4 into one mechanism (the do-nothing control PRODUCES the `--expect-total` a planted run is scored against), so there is no committed count constant for a story to bump — the Never clause that shaped the whole build. Refuses rather than scores on: crash signature (line-anchored, so quoting one cannot refuse forever), resolved-root mismatch, a tally that does not sum, a shrunken/grown collection, or a non-zero exit against a green tally; a refused run prints NOTHING pasteable, only `[REFUSED — NOT EVIDENCE]` on stderr. THREE THINGS MEASURED, NOT ASSUMED: `subprocess.run(["npm", …])` raises FileNotFoundError on Windows because npm is `npm.CMD` — that is WHY c4-10 reached for `shell=True`, and `shutil.which` is the structural fix; both transcript shapes were captured from real runs on `b90fa09` before the parser was written; `Path.resolve()` normalises `c:`→`C:` where `os.path.abspath` does not. THE FIRING PROOF CAUGHT A REAL BUG IN THE HARNESS: `text=True` decodes as cp1252 on this machine and vitest's non-ASCII glyphs appear only in FAILING output, so the harness crashed exactly when it had a failure to report — invisible to every green run. REVIEW CAUGHT THE HARNESS VERIFYING ITSELF: `_NPM_ARGS == ("test",)` compared a constant with a copy of itself while the real narrowing surface is `ui/package.json`; now pinned against ci.yml + package.json and mutation-checked (five drifts all bite). Also reopened the c5-1 hole on the vitest side (`check()` never read the exit code, so an afterAll/post-run failure parsed green and `--expect-green` certified it), and found both drive-letter guards `skipif(os.name != "nt")` while the only job running tests/unit/ is ubuntu — rebuilt case-flipped, so PR #73 is the first time they ever executed in CI. SIXTH SIGHTING of R5's cold-eslint flake recorded during the measurement run (setup 107.37 s) — R5 below records five. ONE THING DEFERRED (dw, this date): nothing binds an `--expect-total` to the tree that produced it; the spec's own no-git-operations boundary blocks stamping it with a HEAD, so it needs a ruling. ADOPTION CLOSED SAME DAY (the thing that killed R5 twice): investigation found the real gap was not the story boilerplate but `_bmad-output/project-context.md` — the file every bmad-build run loads as a persistent fact — which named NEITHER harness, not even the pytest one committed since c5-1. That silence is why six C6 stories hand-typed \"npm test from an uppercase drive path\" instead of reaching for tooling. A Testing Rules entry now covers both halves (control-first-and-warm, stage-before-you-plant, paste the proof line, a refused run prints nothing pasteable), so adoption rides the workflow's own context load rather than five historical story records. R7's one-canonical-home principle applied.
  - epic: 13
    action: "R3 — fix the test_list_decks_with_strategy_field flake with a deterministic
      ordering tiebreaker (created_at DESC then id, or equivalent) in the deck-list
      query, closing the escalation C5 R4's own terms triggered at the third sighting
      (c6-7, 2026-08-11, root-caused there: same-microsecond created_at under ties;
      file-in-isolation 56 passed). One test pinning the tie case."
    owner: "Brad (C7 prep)"
    status: done  # SHIPPED TO MASTER 2026-08-14 via PR #77 at `6f7899a` (Greptile 4/5 -> 5/5 after one valid P2 on the spec's own diff claim). Spec `spec-r3-deck-list-flake.md`; built with superpowers:systematic-debugging. **THIS ITEM'S PRESCRIBED REMEDY WAS ALREADY ON MASTER AND HAD NOT FIXED THE FLAKE** — a measured contradiction, recorded per the standing agreement. Both halves of the action text were discharged before this item was written: `list_decks` has ordered `created_at DESC, id` since `642cbc6`, and `test_list_decks_orders_deterministically_on_created_at_tie` already pinned the tie case. Doing what R3 asked would have been a no-op. THE REAL ROOT CAUSE IS ONE LEVEL UP from the ledgered one: `datetime.now(UTC)` advances in ~593 us steps on Windows, coarser than a repository call, and it drives TWO INDEPENDENT failures — (a) `assert updated_at > original_updated_at` on a clock that has not ticked (test_update_deck_name, test_update_deck_strategy; 85/200 measured tie rate), which has nothing to do with ordering and is why those two fired even in 3-test isolation runs, and (b) tied created_at making positional reads of list_decks wrong (3 rapid creates tie in 96% of runs; order != creation order 140/200). The c6-7 root-cause note explained only (b), i.e. one of three firing members. Baseline 22/30 runs red -> 0/30 after; file still collects 56. FIX IS TESTS-ONLY, zero production change (`git diff master --name-only -- src ui plugin` empty): the ordering contract was already correct AND already documented as arbitrary-under-tie in `src/companion/app/routes/decks.py`, so the test was asserting what the contract disclaims; `updated_at` is never compared anywhere in src/, only serialized. Tests now pin the earlier timestamp (new `_set_updated_at` beside `_set_created_at`) and match decks by id instead of position. A FOURTH, LATENT member — `test_merge_updates_timestamp` — was found by grepping the pattern rather than the cited lines (0/50 firing) and fixed so the family cannot regrow. THE PROBE HARNESS CAUGHT A BAD PLANT, NOT A BAD TEST: the first firing proof (delete both `deck_model.updated_at = datetime.now(UTC)` lines) left the FULL SUITE GREEN, because those assignments are redundant with `DeckModel.updated_at`'s `onupdate` — a hand-typed proof would have been mis-scored as 'my test fired'. Only after removing the `onupdate` did all three go RED; the strategy-drop plant reddened the strategy test alone. Redundant stamping left alone (out of scope). RESIDUAL, deliberately not fixed: `updated_at` is non-monotonic within a tick — safe today because nothing compares it, but a trap for anyone reaching for it as a change token/ETag. feat/companion-c7 must merge master to pick this up.
  - epic: 13
    action: "R4 — companion real-socket pytest marker/scoped alias (the marker half
      of C5 R4, on its CORRECTED premise): a bare `-m integration` today sweeps the
      live-network Scryfall tests plus real-fastembed instantiations, so a red says
      nothing scoped; give tests/integration/companion/ its own marker or alias so
      the process-boundary test is selectable alone."
    owner: "Brad (C7 prep)"
    status: open
  - epic: 13
    action: "R5 — raise the lintBothFixtures test timeout to cover the cold eslint-shellout
      start (~103-126 s setup observed vs ~6-11 s warm; one-line per-test timeout).
      FIVE sightings this epic (c6-2, c6-3, c6-5, c6-8, c6-9), each costing a red
      baseline run plus a warm re-run."
    owner: "Brad (C7 prep)"
    status: done  # DONE 2026-08-17, jointly with epic-14 R1 (same fix, one commit). Landed as a PROJECT-scoped `testTimeout: 180_000` on the `node` project in ui/vite.config.ts rather than the per-test timeout the item asked for. WHY THE DEPARTURE: the per-test form makes prettier reformat every `it()` in lint-gates.test.ts (367 lines of churn on a file whose provenance comments are its doctrine, and whose blame is how they are traced); the project is exactly the shell-out suites (ESLint, stylelint, a real Vite dev server), so scoping there is honest. It IS a widening — every node-project test now gets 180 s — recorded in the config comment. VERIFIED both ways with a throwaway 6 s probe: node project passes, dom project still fails at "Test timed out in 5000ms", so the `dom` default is untouched. Suite green after: ui 80 files / 2,305 tests, python 3,102 passed / 2 skipped. MERGED to master via PR #83 at e6a7e0e (2026-08-17); Greptile 5/5 with ZERO inline findings at round 1.
  - epic: 13
    action: "R6 — add `companion-integration` to branch protection as a required check
      (CARRIED from C5 R1-b, a two-minute GitHub settings change with no repo-tree
      representation). Executed with the C6 integration PR. Note: with C5 R1-a declined,
      the lane's recorded claim is 'runs the process-boundary test', and a required
      check is what keeps a red one from merging silently."
    owner: "Brad (with the integration PR)"
    status: done  # DONE 2026-08-13 (Brad, just after PR #72 merged) — master's FIRST branch protection rule. Verified via API: required check = 'companion real-socket test (windows, py3.12)' (the check-run display name; the job id companion-integration is not what protection matches on), strict=false, enforce_admins=false (admin bypass retained so direct doc-commits to master keep working), no PR-review requirement (solo workflow). Scope note, accepted: story PRs target the epic umbrella, so the rule guards integration PRs and direct pushes — exactly R1-b's stated purpose. Like its parent item, this lives only in GitHub settings; nothing in the tree can drift-check it.
  - epic: 13
    action: "R7 — prose-sync sweep (CARRIED from C5 R2, standalone, scope unchanged):
      one canonical ledger home per cross-module ruling; convert the 5+ Q3/AD-5 narration
      sites (state.py, security.py x2, main.py, test_routes_active_deck.py) to one-line
      pointers; delete scripts/dump_openapi.py's changelog paragraphs; absorb dw:5197's
      test_committed_schema.py docstring fix. TERMINATION CLAUSE: not done by the
      C7 retro -> formally decline."
    owner: "Brad (C7 prep, standalone)"
    status: open
  - epic: 13
    action: "R8 — standing agreements adopted at this retro: (1) STAGE BEFORE YOU
      PLANT — never run a planted red on an unstaged tree (c6-7's unstaged `git checkout`
      revert deleted the whole component); (2) ABSENCE-ONLY ASSERTS GET THEIR POSITIVE
      TWIN in the same suite (c6-7 plant 3: a fully-unwired row satisfies every absence
      assert); (3) MEASURED-CONTRADICTION RECORDING — a story-spec claim about a byte,
      a mechanism, or a blast radius is a prediction; the dev records what measurement
      said (c6-8/c6-9, six caught across the two stories incl. a defective measuring
      instrument)."
    owner: "team (standing)"
    status: done  # adopted in-retro 2026-08-13
  - epic: 13
    action: "R9 — rule UX-DR35's pin-eviction question BEFORE c7-3/c7-4 story context
      (the c6-7 Q7 boundary note, filed in deferred-work.md with its tripwire citation
      corrected at c6-7's review): as written, a deck_changed refetch would evict
      every pinned suggestion — decide whether agent-view pins survive a refetch before
      the refetch story is contexted."
    owner: "Brad (before c7-3)"
    status: done  # RULED 2026-08-14 (Brad): eviction is a MEMBERSHIP TRANSITION — evict only a pin whose card was in the departing deck's list and is absent from the new one; a pin on a never-in-deck card (every pinned suggestion) survives statelessly, no pin-time classification. UX-DR35 amended in epics-companion-app.md (DR text + Story 7.4 ACs, incl. the new suggestion-pin-survives AC = the owed regression test); ledger entry annotated, closes when c7-4 ships that AC's test.
  - epic: 13
    action: "R10 — execute C5 R6's demotion: amend the DESIGN.md citation guard's
      declaration (ui/tests/shell.test.ts) to state it is a STRING-PROXIMITY check
      that never resolves anchors, so it stops reading as more than it is. One comment
      edit; no behaviour change; the ~60 stale DESIGN.md:NNN anchors stay as-is by
      the decline."
    owner: "Brad (C7 prep)"
    status: done  # DONE 2026-08-17. One comment edit at the `documented()` helper in ui/tests/shell.test.ts, no behaviour change: the declaration now opens by naming it a STRING-PROXIMITY CHECK and enumerates the three things it does NOT do — open DESIGN.md, resolve a `DESIGN.md:NNN` anchor to a line, or check the citation is true (a comment reading "999px — DESIGN.md" beside `width: 999px` passes regardless) — then states what it DOES buy: no px literal reaches a component stylesheet with no stated source beside it, and a wrong-or-rotted source is review's. C5 R6's decline is cited in place, so the ~60 stale anchors stay as-is by that decline rather than by silence. Suite green, unchanged count. MERGED to master via PR #83 at e6a7e0e (2026-08-17); Greptile 5/5 with ZERO inline findings at round 1.
  - epic: 14
    id: "epic-c7-retro-item-1-land-c6-r5-lint-timeout"
    action: "R1 — LAND C6 R5 NOW: one-line per-test timeout on the lintBothFixtures
      tests in ui/tests/lint-gates.test.ts (:69, :85, and the :137 pair) to cover
      the cold eslint-shellout start. EIGHTH sighting, observed LIVE during the C7
      retro run 2026-08-16: cold `npm test` = 1 failed / 2304 passed of 2305 in 53.99s,
      the failure being lint-gates.test.ts:68 'Test timed out in 5000ms'; immediate
      warm re-run with no code change = 80 files / 2305 passed in 8.22s. Five sightings
      in C6, a seventh at c7-6, this is the eighth. Owned-but-unlanded across two
      epics; the fix is smaller than any single sighting's cost."
    owner: "Brad"
    status: done  # DONE 2026-08-17, closing epic-13 R5 with it — one fix, both items, ninth sighting never happened. `testTimeout: 180_000` on the `node` project in ui/vite.config.ts, carrying the full measurement record (warm first test ~966 ms vs 4-64 ms for the rest; cold setup ~103-126 s across C6; the retro's own 53.99 s red / 8.22 s warm re-run) and naming the root cause: `eslint.config.js` sets `projectService: true`, so the FIRST ESLint call in the process builds a TypeScript program and whichever test runs first pays all of it. DEPARTURE FROM THE PRESCRIPTION, in the open: the item asked for per-test timeouts on the four watched tests; per-test triggers a prettier reformat of every `it()` in lint-gates.test.ts (367 lines) and would cost the blame on that file's provenance comments. Project scope also covers the two ESLint tests the ledger did not name, which matters because the cost belongs to whichever test is first and reordering moves it. Widening acknowledged in the config comment; `dom` keeps the 5,000 ms default. A pointer comment at the head of lint-gates.test.ts sends a reader who sees the timeout to the measurements. VERIFIED with a throwaway 6 s probe in both projects (node passes, dom fails at 5000ms), then removed. MERGED to master via PR #83 at e6a7e0e (2026-08-17); Greptile 5/5 with ZERO inline findings at round 1.
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-2-nfr05-quiet-machine-remeasurement"
    action: "R2 — RE-MEASURE NFR-05 COLD OPEN ON A QUIET MACHINE (deferred from c7-7;
      the one open item touching a declared criterion). `cdp_harness budget`, 5 runs,
      one copied data dir, nothing else running. c7-7 recorded run A = 500/547/1076/1046/420
      ms (min 420 / median 547 / max 1076, EXIT 2 against the 1,000 ms budget) and
      run B minutes later = 420/512/754 (EXIT 0); c4-12 measured 311/363/428 on the
      same instrument, with no ui/src runtime byte moved this epic. If it still exceeds
      1,000 ms, open a C8 story against the 325 MB-database cold path; if it does
      not, record the quiet-machine figure beside c4-12's and close it. THIS IS WHAT
      MOVES THE VERDICT FROM accepted-with-open-items TO accepted."
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-3-run-c7-manual-checklist"
    action: "R3 — RUN THE C7 MANUAL-TESTING CHECKLIST (L1-L10, retro section 6) before
      the integration PR to master. Every visual, audible and focus-landing behaviour
      this epic shipped is asserted at CSS-source or jsdom level only, and c7-4/c7-5/c7-6
      each flagged items for it by name. Headline items: L1 the loop by eye; L2/L3
      the header veil and its reduced-motion 'Updating…' swap plus the omitted glow;
      L4 the screen-reader pass incl. silence behind an open agent view; L5 the real
      focus landing after a deletion; L6 the R9 pin-eviction arm; L8 the accepted
      staleness window as a user sees it. Carried in from C6 and still unrun: K3,
      J4, J5, and J6 — now on its FIFTH home, which deserves a decision rather than
      a sixth carry."
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-4-reconcile-the-update-verb"
    action: "R4 — SPEC RECONCILIATION, the 'update' verb: Story 7.2's AC (epics-companion-app.md:3073)
      and epic-c7-context.md:27 both enumerate the mutation tools as 'add, remove,
      update, create, import' (+delete). There is no update deck tool — server.py
      registers 17 @mcp.tool() functions and the mutating five are create_deck :274,
      delete_deck :321, add_card_to_deck :340, import_decklist :385, remove_card_from_deck
      :417, all five emitting after their session block exits. c7-2 recorded a reconciliation
      note but the two planning artefacts still say 'update'. Amend both to the five
      that exist, and cite the DERIVED enumeration guard (test_deck_changed_wiring.py:899-943,
      which AST-walks src/mcp_server/tools/ for repository write-method users and
      asserts wired == derived) as what keeps the list honest."
    owner: "Brad"
    status: done  # DONE 2026-08-17. Both artefacts amended. epics-companion-app.md, story 14.2's first Given: "add, remove, update, create, import" -> the four tool names it actually means (`create_deck`, `add_card_to_deck`, `remove_card_from_deck`, `import_decklist`), with `delete_deck` left to the deletion Given that already follows — which is why the first list reads as four, not five. A blockquote amendment note under the story records that there is no update-deck tool and never was, that story 14.2 reconciled it at implementation time without amending the artefact, and that the final Given is discharged by the DERIVED guard in tests/integration/mcp_server/test_deck_changed_wiring.py (class docstring "derived writers == wired emitters, by name, forever"; test_the_derived_mutating_tools_are_exactly_the_five_wired_ones) — so the prose is documentation and the guard is the contract. epic-c7-context.md's "Emit after persistence" bullet: same replacement, same citation, amendment dated inline. VERIFIED against src/mcp_server/server.py: the mutating registrations are exactly those five and there is no update tool. No line anchors added in either edit — tool names and test names instead, so neither can rot the way R10's ~60 DESIGN.md:NNN anchors did. Python suite 3,102 passed / 2 skipped. MERGED to master via PR #83 at e6a7e0e (2026-08-17); Greptile 5/5 with ZERO inline findings at round 1.
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-5-live-region-prose-says-three"
    action: "R5 — FIX THE LIVE-REGION PROSE (two-line edit). AgentView.tsx:506-507
      still reads 'one of exactly three live regions in the app … the other two are
      CardDetail's pin announcement and the connection pill's'. Since c7-5 there are
      FOUR — DeckAnnouncer.tsx:144 is the fourth. The GUARD is correct and exhaustive
      (App.test.tsx:2128-2148 names all three visible with no view open; :2137 pins
      the count); only the prose beside it rotted. EXPERIENCE.md:159 has the same
      shape: its enumeration excludes the deck announcer while the very next sentence
      in the same bullet legislates it — the identical class as the c4-12 alt-text
      amendment recorded two lines above at :158. THIRD consecutive epic for this
      class, and precisely what C6 R7's prose sweep exists to catch."
    owner: "Brad"
    status: done  # DONE 2026-08-17. Both sites corrected, and the count VERIFIED before writing it rather than taken from the item: `grep aria-live` over ui/src (excluding tests) returns exactly four production regions — ConnectionPill, CardDetail's pin announcement, DeckAnnouncer's deck-refetch announcement, and AgentView's heading — with every other hit being a comment explaining why that component is NOT live. AgentView.tsx: "exactly three" -> "exactly four", the other three named, and the correction dated in place with the reason (c7-5 added the fourth, the guard was updated then and this sentence was not). Both sites now point at App.test.tsx's live-region census as the AUTHORITY and say so, instead of asserting a number on their own word. EXPERIENCE.md's Live regions bullet: the deck-refetch region added to the enumeration in the same "artefact wrong, code right" amendment shape as the c4-12 alt-text note one bullet above, naming it as the same class, plus an explicit "four is the whole inventory — a region not named here is undeclared". Line anchors deliberately NOT reproduced (`EXPERIENCE.md:159`, `App.test.tsx:2128-2148` both dropped for section and file names) — same reasoning as R10's decline. Suite green: ui 80 files / 2,305 tests, python 3,102 passed / 2 skipped. MERGED to master via PR #83 at e6a7e0e (2026-08-17); Greptile 5/5 with ZERO inline findings at round 1.
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-6-decide-scripts-type-coverage"
    action: "R6 — DECIDE scripts/ TYPE COVERAGE. .pre-commit-config.yaml:28 scopes
      the mypy hook to ^src/, so scripts/ is ungated — and C7 added 776 lines to scripts/cdp_harness.py
      (now 1,556 lines, the 7th-largest file in the repo), the committed instrument
      that produced the SC-2 figures this epic closes on. Measured for the retro 2026-08-16:
      `uv run mypy scripts/` = 13 errors in 4 files (cdp_harness.py:1141 var-annotated
      + :1555 no-untyped-call; build_plugin.py 7; verify_image_data.py 2; validate_data_layer.py
      2). c7-7 flagged 'two pre-existing errors' — that is the cdp_harness count;
      the tree-wide figure is 13. build_plugin.py is the one that gives pause: it
      is on the release path and enforces the plugin mirror pre-commit depends on.
      Either widen the hook and clear them, or record an explicit ruling that scripts/
      is ungated by choice."
    owner: "Brad"
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-7-format-check-announcement-ux-ruling"
    action: "R7 — GET THE FORMAT-CHECK ANNOUNCEMENT A UX RULING. The pass->violation
      announcement was deferred at c7-5 and the deferred-work.md:4782 entry was REOPENED
      AS UNOWNED at the c7-5 status flip (8165f49): a UX ruling is owed before any
      story can home it. Named here so it does not silently become C8's problem without
      a decision."
    owner: "Brad (Sally's call)"
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-8-split-app-test-tsx"
    action: "R8 — SCHEDULE THE App.test.tsx SPLIT (C8). ui/src/App.test.tsx is 5,319
      lines — 1.97x the next-largest file in the repo (tests/unit/companion/test_images.py,
      2,703) and 3.4x the largest production module. C7 added 1,737 of those lines
      (git_evidence over e392788..e8436fc: added 1985 / deleted 56, the epic's busiest
      path by a wide margin) and ALL SEVEN stories serialised through it. Nothing
      in it is wrong and the suite is green; the cost is navigational and it compounds
      — the C7 census assertions at :2123-2148 sit ~3,200 lines from the c7-5 announcement
      tests at :5407 that changed the number they pin. Split by surface (App.boot
      / App.a11y-census / App.refetch), or add a size tripwire so the next epic cannot
      grow it silently."
    owner: "Brad"
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - epic: 14
    id: "epic-c7-retro-item-9-detect-epic-cannot-address-c-epics"
    action: "R9 — TEACH sprint_status.py THE c-PREFIXED EPICS, or record the manual
      gate as standing practice. --epic parses as int (retro_key = f'epic-{selected}-retrospective'),
      so `detect-epic --epic 7` silently resolves the NUMERIC epic 7 (deck-power,
      retro'd 2026-07-18) instead of epic-c7, and `update --epic c7` dies at argparse
      with 'invalid int value'. The C7 completeness gate was therefore applied by
      hand against sprint-status.yaml:689-696 (all seven c7-* keys done, pending_stories
      empty) and this retro's two status keys were hand-written with the file re-parsed
      to verify. WHETHER THIS IS A 6.11 REGRESSION IS UNDETERMINED and the evidence
      is gone: the skill tree is gitignored (.gitignore:14,17) so there is no history
      to diff; _bmad/_config/manifest.yaml keeps only the CURRENT version (6.11.0,
      lastUpdated 2026-08-16T00:22, installDate 2026-07-06) and no prior one; sprint_status.py's
      mtime is 2026-08-13 16:33, AFTER the C6 retro doc was written (08:20) and after
      all five earlier companion retros, so no prior companion retro ran against this
      copy; and no c1-c6 retro doc mentions the script, --epic or hand-editing either
      way. The limitation IS structural in this copy (detect-epic does int(m.group(1))
      on story keys and composes retro_key = f'epic-{selected}-retrospective'), not
      a stray annotation. Worth fixing regardless — but do not file it as a regression
      without evidence. Every companion retro from here will hit it."
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-14-retro-2026-08-16.md"
  - id: "epic-15-retro-item-84-fix-the-crlf-freeze-self-test-idempotent"
    epic: 15
    action: "Fix the CRLF freeze self-test: idempotent plant (re.sub rb\"\r?\n\" ->
      b\"\r\n\") in test_viewer_freeze.py:1172 + consider .gitattributes eol rule
      for src/viewer/template.html — suite is red today on autocrlf=true Windows checkouts"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-15-retro-item-85-release-doc-accuracy-sweep-before-the-0"
    epic: 15
    action: "Release-doc accuracy sweep BEFORE the 0.5.0 cut: NOTICE:14 oracle-cards
      -> default_cards (+ plugin mirror); README.md:59 + CHANGELOG.md:43 population
      label (~1,000 distinct printings, not full set); README.md:557 38% direction;
      README.md:513 shard arithmetic; optionally pin README Python floor to requires-python"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-15-retro-item-86-repair-the-m5-guard-anchor-row-presence"
    epic: 15
    action: "Repair the M5 guard: anchor row-presence regex to row shape (tokens.css:454/:477
      prose quotes falsify its premise) and invert VISUAL_TRANSITION_PROPERTIES to
      an allowlist so unknown transitioned properties fail by name"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-15-retro-item-87-local-byte-parity-for-the-plugin-mirror"
    epic: 15
    action: "Local byte-parity for the plugin mirror beyond the bundle: byte-compare
      SERVER_FILES + plugin/server/src/viewer in test_build_plugin.py; optionally
      add plugin/server/src/companion to the no-reuse sweep"
    owner: "Brad"
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-15-retro-item-88-full-shape-payload-check-in-test_image_c"
    epic: 15
    action: "Full-shape payload check in test_image_cache_docs.py:182 (one line) —
      close the prefix-then-exec gap"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-15-retro-item-89-markdown-guard-helper-consolidation-one"
    epic: 15
    action: "Markdown-guard helper consolidation: one shared tests/ helper for repo-root,
      section extraction, fence rule (three _ATX_HEADING constants with two behaviours;
      four scanner implementations). TERMINATION CLAUSE: not done by the Epic 16 retro
      -> formally decline"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-15-retro-item-90-cross-machine-session-hygiene-cloud-sess"
    epic: 15
    action: "Cross-machine session hygiene: cloud-session branches get real story
      names before merging; story work always lands via PR (1760ef4 bypassed the per-story
      Greptile gate); commit subjects use the dash id form (15-4) so attribution tooling
      works"
    owner: "Brad"
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-15-retro-2026-08-20.md"
  - id: "epic-16-retro-item-91-port-the-whitespace-id-filter-to-the-oth"
    epic: 16
    action: "Port the whitespace-id filter to the other two views: GroupsView's trimmed-non-empty
      cardIdsOf filter into TierListView, equivalent trim guard into SwapsView cardIdOf/unknown
      check, with 16.3-style pinned tests"
    owner: ""
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-16-retro-2026-08-21.md"
  - id: "epic-16-retro-item-92-push-tool-docstring-accuracy-pass-one-co"
    epic: 16
    action: "Push-tool docstring accuracy pass (one commit + plugin rebuild): companion_show_groups
      empty-card_ids wording -> group not displayed at all; _PUSH_MESSAGES docstring
      all-three-tables -> four; 64 KB envelope note copied into companion_show_tier_list
      (and swaps)"
    owner: ""
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-16-retro-2026-08-21.md"
  - id: "epic-16-retro-item-93-guard-hardening-pair-test-tying-_push_me"
    epic: 16
    action: "Guard hardening pair: test tying _push_messages keys to get_args(PushOutcomeToken)
      so a widened vocabulary fails by name; sibling-channel-empty assertions retrofitted
      into swaps/tier delivery tests + no_clients_connected mirrors for the three
      new kinds"
    owner: ""
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-16-retro-2026-08-21.md"
  - id: "epic-16-retro-item-94-release-gating-0-5-0-ruled-by-brad-2026"
    epic: 16
    action: "RELEASE-GATING (0.5.0, ruled by Brad 2026-08-21): resolve the empty-push
      copy-grammar ledger item (c6-6) — amend EXPERIENCE.md Voice-and-Tone cell to
      a display noun, substitute lowercased AGENT_VIEW_LABELS[kind] in emptyPushLine,
      update byte-gated copy tests; decide the all-skipped header-count contradiction
      in passing"
    owner: ""
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-16-retro-2026-08-21.md"
  - id: "epic-16-retro-item-95-decide-the-view-scaffolding-consolidatio"
    epic: 16
    action: "Decide the view-scaffolding consolidation now the four-view family is
      closed (build or formally decline): shared tile/placeholder-ladder/stale-release
      module; R1 is the live demonstration of clone cost"
    owner: ""
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-16-retro-2026-08-21.md"
  - id: "epic-16-retro-item-96-skills-mention-of-the-push-tools-ruled-b"
    epic: 16
    action: "Skills mention of the push tools (ruled by Brad 2026-08-21, reversing
      the 16.1 exclusion): add a when-the-companion-is-running push note to the deckbuilding-family
      skills, both copies + plugin rebuild"
    owner: ""
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-16-retro-2026-08-21.md"
  - id: "epic-17-retro-item-97-escape-seam-hardening-one-commit-iscompo"
    epic: 17
    action: "Escape-seam hardening (one commit): isComposing/defaultPrevented guards
      on the pill document listener; repair the two stale comments (ConnectionPill
      header, AgentViewsNav:33-37 AppShell citation); extend keyboard-floor non-vacuity
      loop to the fourth listener; composed App test for popover+tooltip Escape ordering;
      collapse duplicated focus-return block"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-17-retro-2026-08-23.md"
  - id: "epic-17-retro-item-98-companion_status-honesty-pair-distinguis"
    epic: 17
    action: "companion_status honesty pair: distinguish clients-unknown (None) from
      clients==0 in tool message and companion skill; add running-arm test driving
      clients through the real probe_health parse"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-17-retro-2026-08-23.md"
  - id: "epic-17-retro-item-99-open-launch-window-pair-sock-listen-befo"
    epic: 17
    action: "--open launch-window pair: sock.listen() before _open_browser (or correct
      the wrong bound-and-listening comment and record the race); companion-skill
      bullet naming the already-starting-up lock-held outcome"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-17-retro-2026-08-23.md"
  - id: "epic-17-retro-item-100-guard-the-history-revisit-retained-write"
    epic: 17
    action: "Guard the history-revisit retained write (ruled by Brad 2026-08-23):
      a history revisit of a non-latest entry must not touch retained[kind] — one-clause
      condition in openAgentView"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-17-retro-2026-08-23.md"
  - id: "epic-17-retro-item-101-release-gating-0-5-0-ruled-by-brad-2026"
    epic: 17
    action: "RELEASE-GATING (0.5.0, ruled by Brad 2026-08-23): cold-open diagnosis
      + re-measure against the branch tip (O1 trigger fired: 363->420->529 ms, 4%
      headroom, hero weight landed post-measurement); pin hero cache header; fix the
      deferred-stays-empty ledger contradiction in report + spec"
    owner: "Brad"
    status: done
    ref: "_bmad-output/implementation-artifacts/epic-17-retro-2026-08-23.md"
  - id: "epic-17-retro-item-102-deferred-hardening-bundle-post-0-5-0-can"
    epic: 17
    action: "Deferred hardening bundle (post-0.5.0 candidates): popover anchor ResizeObserver;
      skills tool-vocabulary subset-of-tool-census gate; drain-instrument hardening
      (bounded pump, per-run failure-recorder reset, cancellation logging, _outstanding_card_images
      units); tooltip last-confirmed copy hedge; positive control in connection-pill-copy
      gate"
    owner: "Brad"
    status: open
    ref: "_bmad-output/implementation-artifacts/epic-17-retro-2026-08-23.md"
