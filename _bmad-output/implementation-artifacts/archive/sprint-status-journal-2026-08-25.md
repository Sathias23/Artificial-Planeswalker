# Sprint-status journal — compressed out of sprint-status.yaml on 2026-08-25

Verbatim history moved during the implementation-artifacts archive pass. The live file keeps
one-line summaries; nothing was deleted.

## Header journal (last_updated history)

# last_updated: 2026-08-20 (STORY 15-5 MERGED via PR #89 at `48f5a79` into `feat/companion-epic-15`, now 30 commits ahead of master. Two commits: `e6ca8ee` the story, `2f2cee2` the Greptile round. All five CI checks green; Greptile 4/5 -> 5/5 after one round, both P2s valid and fixed. Epic 15 is now 15-1..15-5 done; only 15-6 (the SC-5 gate) remains. WHAT THE STORY BOUGHT: the plugin is the install route that has to carry the companion's UI and almost nothing proved it did — both SPA abort paths had never been fired, no assertion said a built tree carried the bundle at all, and the skills check was presence-only, so a `bmad-*` entry added to SKILLS would have shipped unnoticed. 788 new lines in `test_build_plugin.py` now pin the bundle byte-for-byte, the four product skills by set equality, the committed tree as exactly what a fresh build emits, and the mirrored bundle SERVING through the real SPA mount. Suite 3208/2 -> 3231 passed / 2 skipped. 15-4'S RELEASE-RELEVANT DEFERRAL IS CLOSED: the anchored `uv run --directory "$PLUGIN_ROOT/server" artificial-planeswalker companion` is documented for both clients and DERIVED from pyproject's console script by test rather than transcribed. GREPTILE EARNED ITS KEEP AGAIN, both P2s on this story's own new guards. (1) The per-subject `ls-files` preamble proves a named subject is still TRACKED, which is the most a named subject can prove — Vite's chunk names are content-hashed, so no list can name them, and one newly ignored chunk drops out while `git status --porcelain`, blind to ignored paths by construction, certifies the tree. All three drift steps now also ask what git is IGNORING under the tree the build just wrote. (2) The Codex block spent `$PLUGIN_ROOT` with no way to obtain one; Codex keeps its own version-keyed cache under `~/.codex`. THE LESSON: the guard written for (2) searched the whole document and PASSED ON THE BUG — the Claude Code block's `PLUGIN_ROOT=` sits above the Codex block and vouched for it — and was only caught by mutating the README back. A guard is not a guard until something has been shown RED through it, for the SECOND epic running. Next: 15-6, the SC-5 human gate (Brad only, cannot be automated or delegated).)
#                Previously — 2026-08-19 (STORY 15-4 MERGED via PR #88 at `71a2ccb` into `feat/companion-epic-15`, now 26 commits ahead of master. Five commits, all five CI checks green, Greptile 1 P2 (valid, fixed). Epic 15 is now 15-1..15-4 done; 15-5 and 15-6 remain. THE LESSON WORTH CARRYING: a documentation story shipped its own subject matter gated by nothing, and the audit — not the review layers — caught it; the three adversarial layers then found FOUR more holes in the first guard cut, every one of which passed green before it was planted. A guard is not a guard until something has been shown RED through it. SECOND LESSON: Greptile found a real defect on a DOCS-ONLY PR that all three review layers missed, because they compared prose against source and the bug was in filesystem semantics — F4's Windows-only 'cannot be deleted' generalised to POSIX, where the delete succeeds and strands the companion on a ghost inode. Next: 15-5 plugin distribution parity.)
#                Previously — 2026-08-19 (STORY 15-4 IMPLEMENTED AND REVIEWED -> review, on `feat/companion-epic-15`, three commits: `085c9c0` the docs, `8447c26` the prose guard, `d1cdaa0` the review patch set. Suite 3188/2 -> 3208 passed / 2 skipped (+20, all the new guard); frontend 80 files / 2305 UNMOVED; ruff, mypy (94 files) and the plugin mirror all clean. THE HEADLINE: the story was reported done with its own subject matter GATED BY NOTHING — 148 lines of README quoting exact runtime strings and no test tying them to source. The build workflow's Matrix Test Audit caught it; test_companion_docs.py now carries 20 assertions with ZERO hand-typed README strings (announcements come out of server.py's AST, exit statuses out of __main__.py's, panel copy out of copy.ts keyed on the kebab spelling of the shipped error token). Proof 8 is the load-bearing one and was re-run independently: DEFAULT_PORT 8765->8766 with the README UNTOUCHED reddens 3 doc tests. REVIEW was three layers, 34 findings: 0 intent gaps, 0 bad-spec loopbacks, 30 patched, 3 deferred, 3 rejected. Four review findings were PROVEN by planting: a `port 9999` fallback example, a dead `#no-such-heading` anchor, a Requirements bullet claiming 'Node 22 or newer is required' against the guarded section's opposite, and a full revert of the imagery attribution — ALL FOUR passed the first guard cut green. Two of the review's own new assertions then passed their first firing proof (vacuous) and were rescoped before landing. 16 firing proofs total. DEFERRED, and it matters for release: `uv run artificial-planeswalker companion` DOES NOT WORK for the plugin install route the README recommends first — uv resolves its project from the CWD and a plugin user has no checkout. Homed on 15-5, whose own AC says 'and the companion is launched'. Also flagged: App.test.tsx's 206-pin corridor test flakes under machine load (green idle here, 5.85s). NO RELEASE CUT — pyproject stays 0.4.0, zero occurrences of 0.5.0 in CHANGELOG, by ruling. Next: PR into master, then 15-5.)
#                Previously — 2026-08-19 (STORY 15-4 IN PROGRESS on `feat/companion-epic-15` at `2ea1f4a`. Spec approved and ready: spec-15-4-release-documentation-for-the-companion-app.md, ~6,500 tokens, oversized and kept — smallest of the four epic-15 specs. THREE RULINGS taken before code: no version cut (pyproject stays 0.4.0, no dated heading, no [0.5.0] link defs — 15-6's SC-5 gate has not run, so dating a release would assert a judgement Brad has not made); F4's database-lock defect DOCUMENTED not fixed, staying ledgered at Medium; the Scryfall attribution gains an imagery clause in README and NOTICE to match what the footer already says and the app already caches. epic-15-context.md RECOMPILED — the cached copy was stale against 15-3's epics edit and still carried the disproved ~12 MB figure. KEY CONSTRAINTS FOUND: exactly one test reads root README.md (test_image_cache_docs.py, lines 242-348 only) and ZERO read CHANGELOG.md, so the new `## The companion app` section goes at line 195, outside the guarded range the guard's own tests prove is immovable; root README/CHANGELOG are OUTSIDE Prettier's scope (it runs under working-directory: ui), so 15-3's ui/README.md repadding failure cannot recur; but README.md and NOTICE are both in build_plugin's SERVER_FILES and the sync hook does not run on commit here, so the plugin rebuild is mandatory or CI fails on both matrix legs. The dependency diff for release notes is v0.4.0..HEAD, NOT master...HEAD — this branch touches no dependency file. Next: implement 15-4.)
#                Previously — 2026-08-18 (STORY 15-3 MERGED via PR #87 at `f2e9b23` into `feat/companion-epic-15`, now 18 commits ahead of master. Three commits: `abb0fb9` the reconciliation, `ec7f3c5` a Prettier fix on ui/README.md, `2129ad3` the guard's module overview after the route-parity patch. CI green on all five checks; Greptile 5/5 after one P2 (a docstring still describing the superseded openapi() route source). Review was four layers: 27 patches, 4 deferred, 6 rejected, 0 intent gaps, 0 bad-spec loopbacks; followup_review_recommended: true (score 51). THE HEADLINE: reviewers broke the story's own new guard twice by running it - FR-04's driver check passed the exact claim it existed to ban, and route parity could not see the shipped /ws endpoint because OpenAPI does not model WebSockets. Both closed and probe-proven RED. NOTE the CI lesson now recorded in the spec: the ui lane is FOUR commands (lint, format:check, typecheck, test) and `npm test` alone missed a Prettier failure - any markdown edit under ui/ re-pads its whole table. Next story is 15-4 release documentation.)
#                Previously — 2026-08-18 (EPIC 15 UNDER WAY on `feat/companion-epic-15`, 10 commits ahead of master. 15-1 merged (PR #84, `f938d03`) and 15-2 merged (PR #85, `e424166`) — both done. Tree verified green at `e424166`: 3180 passed / 2 skipped, `ruff check` clean, `mypy src/` clean on 94 files, and `scripts/build_plugin.py` regenerates the mirror with no diff (so `plugin/` is NOT stale, despite the missing sync hook). OPEN from 15-2: its review recommended a follow-up pass (score 31, threshold 5) that has not been run, and 4 low findings were deferred in its frontmatter. 15-3 is IMPLEMENTED 2026-08-18 (not yet PR'd) and discharged 15-2's fourth deferral: the superseded ~12 MB is corrected at all four epic sites plus two more it found (`ARCHITECTURE-SPINE.md`, `walkthrough.html`). Next 15-4, then 15-5, 15-6.)
#                Previously — 2026-08-13 (C7 PREP UNDERWAY. Epic C6 is on master (PR #72 at `c3ac8be`) and its delegated retro rulings are FINAL. Closed since: R6 branch protection (master's first rule, required check = `companion real-socket test (windows, py3.12)`); R1 the C6 manual-testing checklist (10 observed, 5 carried to c8-6); R2 the vitest probe harness (PR #73 at `1f939a9` — `scripts/vitest_probe_harness.py`, the frontend twin of `scripts/probe_harness.py`). Next, in order: R3 flake tiebreaker, R4 companion marker, R5 lintBothFixtures timeout, R7 prose sweep, R10 guard demotion; rule UX-DR35 pin eviction (R9) before c7-3 context; then cut the C7 umbrella off master and begin c7-1. LOOSE END from R2: no story's Task 0 yet points at the harness, so c7-1 would hand-run plants exactly as C6 did.)
#                Previously — 2026-08-13 (C6 RETRO run and recorded in epic-c6-retro-2026-08-13.md, delegated mode. epic-c6 -> done; SC-1 + SC-3 closed. 10 action items (epic c6 R1-R10) appended. Termination clauses EXECUTED: C5 R1-a declined (lane claim downgraded), C5 R6 declined (guard demoted to declared string-proximity); C5 R7 declined on evidence (zero mirror escapes in C6, reopen trigger recorded); C5 R5 re-keyed to c6 R2 (vitest harness, BUILD as C7 prep, termination clause at C7 retro); C5 R4 split into c6 R3 (flake fix — root cause found at c6-7: same-microsecond created_at ordering) + c6 R4 (marker). Next, in order: run the C6 manual-testing checklist (retro §8, Block J + K1-K7) BEFORE the integration PR; integration PR feat/companion-c6 -> master with NO Greptile (standing rule) + R6 branch protection; C7 prep; rule UX-DR35 pin eviction (c6 R9) before c7-3 context.)
#                Previously — 2026-08-12 (Story c6-9 MERGED via PR #71 into feat/companion-c6 at `d4f908e`. Greptile: 5/5, zero findings — confirms the three-layer review's own one patch (the harness's failed-push diagnostic fix) needed no further correction. Suite at merge: frontend 2,123/75, Python 2,921/1/55 (+14 over baseline), tokens 70 unmoved. This was the epic's LAST story — C6 is now feature-complete. Next: the C6 retrospective.)
#                Previously — 2026-08-12 (Story c6-9 CODE-REVIEWED (bmad-code-review) -> done. Blind Hunter, Edge Case Hunter and Acceptance Auditor run in parallel against the diff at baseline `6586209`; Acceptance Auditor independently re-read the full spec and confirmed the DAR's arithmetic and citations. 0 decision-needed, 1 patch, 5 defer, 12 dismissed as noise/already-mitigated/already-ruled. PATCH: a failed push (bad token -> 403, or `--items` over the 60-item cap -> 400) was reported by `scripts/cdp_harness.py` identically to a genuine rendering failure ("surfaces never arrived"), hiding the real HTTP cause and diverging from `cmd_budget`'s own established status-code-checking idiom two functions up; fixed by threading `status_code`/response text into the result and surfacing them first in `_print_run`. The 5 defer findings are pre-existing harness gaps or future-run usability footguns (deck-ordering non-determinism, file-level companion-import allow-list granularity, `--card-ids` silently defeating the warm arm, a point-sampled image-warmth metric) — none touch the recorded 15/21/36 ms figures. This is the epic's last story. Next: raise the story PR into `feat/companion-c6`, then the C6 retrospective.)
#                Previously — 2026-08-12 (Story c6-9 IMPLEMENTED (dev-story) -> review — the 250 ms push budget is OBSERVED for the first time at 15/21/36 ms warm-art, and SC-3 closes.)
#                Previously — 2026-08-12 (Story c6-9 CONTEXTED off `6586209` -> ready-for-dev — the epic-closing verification story: degradation with the app closed (SC-3) and the 250 ms push budget (SC-1/NFR-05). Code archaeology at story creation: ACs 1/2/7 already shipped and pinned by c6-1/c6-2/c6-4 — the genuine work is an SC-3 leaf-import sweep, the FIRST-EVER observed 250 ms figure via a push arm on the committed scripts/cdp_harness.py (c4-12's instrument and precedent, incl. its no-Playwright / no-suite-assertion rulings), and the 10.3 ownership boundary stated up front (ws.py's c10-3 sentence stays correct — it names the concurrent-under-load case). Frontend suite predicted NOT to move (2,123/75); Python grows. 5 open questions with recommendations await Brad's pre-code ruling; Q2 (the clock definition — t0 at push_event() return, stop at last-surface DOM entry, bloom excluded by construction) is the load-bearing one.)
#                Previously — 2026-08-12 (Story c6-8 MERGED via PR #70 into feat/companion-c6 at `980db80`. Greptile: 5/5, zero findings — no post-merge catch, matching the three-layer review's Acceptance Auditor, which also returned zero findings after independently re-running every gate. A first for both layers on the same story, this epic (contrast c6-2 and c6-7, where Greptile caught real P1s the human-facing review missed). The one code-review patch (a retained push with an entirely absent `ts` collapsed its nav pill to quiet/never-pushed instead of active-with-no-time, making it permanently unreachable after dismissal — a real UX-DR34 violation; fixed by splitting activeness, keyed on retention presence, from time display) landed in the same commit as the story implementation, `fc813d8`. Suite at merge: frontend 2,123/75, Python 2,907/1/55 unmoved, tokens 70 unmoved. Next: c6-9 (the 250 ms budget measurement) or the C6 retro, whichever Brad calls.)
#                Previously — 2026-08-12 (Story c6-8 CODE-REVIEWED (bmad-code-review) -> done. Blind Hunter, Edge Case Hunter, and Acceptance Auditor run in parallel against the uncommitted diff at baseline `f390a46`. Acceptance Auditor independently re-ran every gate (frontend suite, lint, typecheck, format:check, Python suite, tokens count, mirror sha256 diff, an EXPERIENCE.md apostrophe hexdump) and reproduced every Dev Agent Record claim exactly — zero findings from that layer, a first for this epic's reviews. 0 decision-needed, 1 patch, 2 defer, 12 dismissed as noise/already-mitigated/already-ruled. PATCH: a retained push whose `ts` is entirely absent (not merely unparseable — the other half of the malformed-frame threat model `pushTime.ts`'s own docstring names, since `agentEventOf` validates only `kind`) collapsed its nav pill into the QUIET/never-pushed branch instead of active-with-no-time, because `useAgentViewPushTime`'s `?? null` made "never pushed" and "pushed, ts absent" the same signal — permanently unreachable via the pill after the next dismissal, a real UX-DR34 violation ("re-openable for the rest of the session"). Fixed by splitting activeness from time: a new `useAgentViewHasPush` selector keyed on `retained[kind]` presence decides quiet vs. active; `useAgentViewPushTime` keeps deciding what time (if any) to show. One new test for the absent-ts case (distinct from the already-covered present-but-unparseable-ts case). Both DEFER findings were already independently identified and ledgered by the dev during the story itself (the disabled-button `title`-tooltip-may-not-fire risk, homed to the c8-6 manual checklist; the UX-DR28 "tooltip" prose still contradicting UX-DR39 at the source artefact, homed to Story 8.3) — cross-referenced rather than duplicated in deferred-work.md. Frontend 2,122/75 -> 2,123/75; Python/tokens unmoved; both static mirrors rebuilt and sha256-verified identical. Next: raise the story PR into `feat/companion-c6`.)
#                Previously — 2026-08-11 (Story c6-7 MERGED via PR #69 into feat/companion-c6 at `228ecf8`. Post-merge (pre-merge in this case — the PR was still open) Greptile caught TWO MORE real P1s the three-layer review missed: (1) inspectable() treats an in-flight (undefined/loading) cache entry as inspectable — deliberate for deck cards — but a suggestion id can settle to a TERMINAL unknown (UX-DR22: can never be inspected), and a real click/hover in the window before that settle lands (unreachable in jsdom's near-synchronous promises) raced past the guard and left a stale hover/focus/pin; fixed with a per-row effect that releases its own targets the moment its entry resolves to unknown. (2) The row's two text lines are entirely content-driven with no floor — a malformed item with both an unresolvable card_id and no reason leaves every child in both lines empty, and an empty flex/grid item with no padding is 0px tall, collapsing the thumbnail (which spans both rows) to a sliver; fixed with min-height: 1lh on both rows (the lh unit sidesteps both the token-family stylelint gate and the px-citation guard cleanly). Both fixes covered by new tests (behavioral for the pin release, source-reading for the CSS floor). Suite at merge: frontend 2,039 passed / 73 files (baseline 1,995/73, +44 net), Python unmoved at 2,907/1/55. Pushed as a second commit (`00f401f`) rather than amended, since the branch was already public; bundle rebuilt a second time and sha256-verified. This is the THIRD data point for "Greptile catches what code review misses" in this epic (c6-2, and now c6-7 twice over — once in the three-layer pass itself via triple-independent-confirmation, once again from Greptile on the same PR). 3 items remain in deferred-work.md, none gating. Next: c6-8 (nav pills, unread markers, re-open and kind switching).)
#                Previously — 2026-08-11 (Story c6-7 CODE-REVIEWED -> done. Three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) of the suggestion-rows diff caught 3 decision-needed, 5 patch, 3 defer, 2 dismissed as noise — all 3 decisions ruled by Brad and applied as patches (8 total). HEADLINE: all three review layers independently found the SAME crash bug — `suggestionsViewOf` only checks `Array.isArray(items)`, never that each element is a non-null object, so a bare `null`/`undefined` array entry throws in `cardIdOf`/the row's key/the hydration effect, which React surfaces as unmounting the ENTIRE dialog — exactly the wholesale failure this story's own FR-13/AD-7 promise and its per-FIELD `typeof` gating exist to prevent, and untested despite exhaustive malformed-item coverage elsewhere. Fixed with an `itemOf` normalizer applied at the one boundary where raw array elements are read. Also patched: a first-paint stray `<img>` request for a malformed (empty) `card_id`, two wrong/incomplete DESIGN.md token citations (`empty-push-line.container` cited the shell's inset instead of the body's; `suggestion-row.gap` cited only half of a two-value shorthand — both now pinned by `tokens.test.ts` assertions on their actual string values, not just presence), a stale "action badge" phrase missed by the ripple sweep in `epics-companion-app.md` (annotated per the c5-5 precedent), a false code comment claiming a truncated `reason` is recoverable via the detail panel (it is not — `CardDetail.tsx` never reads a suggestion's `reason`; Brad ruled: fix the comment, truncation is intended), and two `deferred-work.md` ledger corrections (Q7's eviction tripwire citation, and an annotation on the image-coalescing closure noting an uncovered cross-surface case — both rulings themselves left standing). Re-verified after patches: lint/typecheck/format:check clean, frontend 2,036/73 unchanged (no regressions), Python 2,907/1/55 unmoved, bundle rebuilt a second time and sha256-verified identical across all mirrored files. 3 items deferred to deferred-work.md (screen-reader concatenation of badge/name/cost/confidence with no separators — needs UX input; a hydration tier with a test-coverage gap; malformed items sharing one `''` identity — no observed harm). Next: raise the story PR into `feat/companion-c6`.)
#                Previously — 2026-08-11 (Story c6-7 IMPLEMENTED -> review — the Suggestions view: the app's first agent-view rows. Frontend 1,995/73 -> 2,036/73; Python 2,907/1/55 UNMOVED; tokens 69 -> 70.)
#                Previously — 2026-08-11 (Story c6-7 CONTEXTED off `31ad3e3` -> ready-for-dev — the Suggestions view: rows fill the null SuggestionsView.tsx left for them; 7 open questions await Brad's pre-code ruling, incl. the action-badge wire gap, the DESIGN.md amendment dw:22 homed here, and the image-coalescing close-or-re-aim call.)
# previously: 2026-08-09 (Story c5-8 MERGED via PR #60 into feat/companion-c5 at `057a6d9`. Greptile caught two real findings after the PR was raised: P1 — the vitest port counter tracked the requested port, not the one Vite actually bound, so a retry could desync it and hand a later test a still-live server's port; fixed by reseeding the counter past the real bound port after every listen(). P2 — the review's own patch had added `assert record_two.port != record_one.port`, which Greptile correctly flagged as wrong (the OS can legally reissue the same ephemeral port on a healthy restart); reverted. Re-verified: integration test green, devProxyRoundTrip green, full frontend suite 1,868/69 unchanged, Python 2,770/1 skipped unchanged. **Epic 5 ("The Agent's Channel") is now COMPLETE — all 8 stories (c5-1..c5-8) done.** Sprint tracking does not point at a next c5 story; next is the C5 retro, then (per standing rule) a post-retro integration PR from feat/companion-c5 into master with no Greptile.)
# previously: 2026-08-09 (Story c5-8 CODE-REVIEWED -> done. Three layers, 5 patch applied (0 decision-needed, 0 defer, ~16 dismissed as noise). Headline patches: fixture teardown now raises an ExceptionGroup instead of swallowing every backend-stop failure but the first; the four websockets.connect()/close() calls now carry explicit timeouts, matching the module's own "every await carries a bound" claim; the restart phase now asserts the two backends bound different ports; AC 12's text corrected to match the shipped dw:5470 fix (a port counter + strictPort:false retry, not the literal "port 0" the AC described). Re-verified after patches: integration test green (4.17s), full suite 2,770/1 skipped byte-identical, ruff clean. Next: raise the PR into feat/companion-c5.)
# previously: 2026-08-09 (Story c5-8 DEV COMPLETE -> review. The one real-socket test ships; all three c5-8-homed dw debts paid, two of their premises falsified and recorded.)
# previously: 2026-08-09 (Story c5-8 CONTEXTED off `b498ac5` -> ready-for-dev — the one real-socket integration test, AD-10's single integration-marked backend boot.)
# previously: 2026-08-08 (Story c5-7 MERGED via PR #59 into feat/companion-c5 at `d0224ac`. Greptile caught one real P1 after the PR was raised — the pill's fixed footer-clearance offset assumed a single-line footer, and a real headless-browser check confirmed it genuinely covered the wrapped attribution text below the app's own stated ~1100px floor, which nothing had enforced in code (NFR-08). Fixed by enforcing that floor (`min-width: 1100px` on `.app-shell`); Greptile's score moved 4/5 -> 5/5, CI green, merged. Sprint tracking now points at c5-8.)
# previously: 2026-08-08 (Story c5-7 CODE-REVIEWED -> done. Chunked review, 1 decision-needed (ARIA landmark exclusion — accepted as-is, no code change), 5 patch applied, 2 defer, 17 dismissed as noise. Headline patches: a source-reading guard closing a P15-shaped gap for the deck name's typography (jsdom can't see CSS, so nothing caught a regression reattaching the uppercase micro role); AC 18's missing announcement assertion at the reconnecting step; AC 6's text reconciled with the shipped Q3 override; a second falsified backend-prose claim (wrong attribution of who consumes `connected_count`) caught by grep and fixed. Frontend 1,867 -> 1,868, Python byte-identical, bundle rebuilt a second time, mirror sha256-verified. Next: raise the PR into feat/companion-c5.)
# previously: 2026-08-08 (Story c5-7 CONTEXTED off `44530ac` -> ready-for-dev — the connection pill, the gauge on c5-6's machine. 23 ACs, 6 open questions for pre-code ruling; the story owns dw:4597 (the pill's DOM position, which three artefacts each assumed someone else decided) and the pill's own words (specified nowhere). Scope fence recorded: the /health tooltip is c10-1's. Key recon findings baked into the ACs: surfaceOf hides the loaded deck in exactly the 'down' state where the pill must still name it; shell.test.ts's full-window-layer guard already tolerates a corner-pinned fixed pill by design; App.test.tsx pins exactly ONE aria-live region and the pill ships the app's second.)
# previously: 2026-08-08 (Story c5-6 MERGED via PR #58 into feat/companion-c5 at `eabeb58`. CI caught one thing local review missed: the SPA bundle drift check — the socket.ts patch changed the built JS output and only the plugin mirror had been rebuilt, not src/companion/app/static/ itself. Fixed in a follow-up commit (`cd ui && npm run build`, recommitted, plugin mirror rebuilt in step by the pre-commit hook), CI green, merged. Sprint tracking now points at c5-7.)
# previously: 2026-08-08 (Story c5-6 CODE-REVIEWED -> done. Chunked 3-group review, 1 patch applied — a fail()/schedule() ordering bug in socket.ts that could silently and permanently halt the reconnect loop if a downstream status callback ever threw. 6 deferred, 25 dismissed as noise (several turned out to be exact matches for established codebase idioms once checked against surrounding code). Full suites green after the fix. Next: raise the PR into feat/companion-c5.)
# previously: 2026-08-08 (Story c5-6 CONTEXTED off `3027109` -> ready-for-dev — the first client-side WebSocket code in the repo; 23 ACs, 9 open questions, the three-sibling stale-panel family homed here by ruling.)
# previously: 2026-08-07 (Story c4-12 CONTEXTED off `86d5fb6` -> ready-for-dev — the LAST story of Epic C4: the empty deck state and the
#                cold-open render budget. One sentence of copy, one conditional render, and a number someone has to go and observe.
#                BRANCH PRECONDITION CLEAN for the first time in three stories — PR #50 is MERGED and `origin/feat/companion-c4` is at
#                `86d5fb6`; cut from the MERGE commit, not from `c435086`.
#                HEADLINE, and it inverts the story's apparent shape: TWO OF THE THREE PANELS AC 2 ASKS YOU TO HIDE ARE ALREADY HIDDEN,
#                AND ON A DIFFERENT CONDITION. `ManaCurve.tsx:114` returns null on `curve.total === 0`, `ColourDistribution.tsx:147` on
#                `distribution.total === 0`, and `AnalysisRow.css:52-54` already collapses the row with `:empty` — all three shipped
#                DELIBERATELY, naming this story, c4-9's as "c4-12's clause arriving early ... only the panels' own conditions are
#                theirs". Zero-data and zero-cards COINCIDE on an empty deck and DIVERGE on a land-only deck, which
#                `App.test.tsx:794-828` pins — so a card-count gate in `App.tsx` would duplicate a working mechanism AND turn that test
#                red. Only `FormatCheck` needs one, because its own header says "this panel's data is never empty: six rows, always".
#                SECOND HEADLINE, and it is what actually decides AC 3: THE 1-SECOND BUDGET IS THREATENED BY AN UNDOCUMENTED EFFECT
#                ORDERING. Five of the six named surfaces derive from `boards` and paint on the deck-detail commit; the sixth, the
#                format check, is a separate request — and `App.tsx:226` (the 99-card hydration sweep) is declared BEFORE `App.tsx:265`
#                (the format check). Transport is HTTP/1.1 (`server.py:233`, no h2) and Chrome caps 6 connections per origin, so the
#                request sits at QUEUE POSITION ~100 behind the sweep — whose measured browser tail is 847-1,795 ms (c4-6, n=4) against
#                its own 5.0 ms backend cost. `FormatCheck.tsx:236` renders null until its report lands, so the sixth surface does not
#                exist until the queue drains. SWAPPING THE TWO BLOCKS COSTS ONE LINE, and neither comment mentions the other's queue
#                position.
#                THIRD: DESIGN.md SPECIFIES THE EMPTY-DECK LINE NOWHERE. The entire spec is two EXPERIENCE.md table cells (`:70`,
#                `:113`); spacing, alignment, the container, a min-height and the `ul` semantics are all unlegislated. AC 6's "matches
#                DESIGN.md" is therefore UNSATISFIABLE for this branch until DESIGN.md is amended — which forces DESIGN.md-FIRST
#                ordering, since `shell.test.ts:995-1032` demands a citation that does not yet exist and an invented anchor is the very
#                defect this story is recording against the epic.
#                FOURTH: THE STATE IS AN UNCONFIRMED ASSUMPTION. `.memlog.md:22` tags it `(assumption)`, it is absent from the four
#                rulings Brad confirmed, and the reviewer who proposed it (`validation-report-2026-07-22.md:71-73`) recommended hiding
#                THE CURVE ONLY — colour distribution and format check were added by the author with no recorded rationale.
#                MEASURED read-only against the shipped DB. **RECORD CORRECTION: 42 DECKS, NOT 40** — two `standard` decks created
#                2026-08-06 after c4-10's census, so every "40 decks / 240 rows / 20 standard" figure in the ledger, in c4-11's §A and
#                at `App.tsx:203`/`:247` is stale. ZERO empty decks exist, so every fixture this story writes is NECESSARILY SYNTHETIC
#                and must be declared so in place — yet the empty state is the NORMAL one at creation (`create_deck` writes no card;
#                `remove_card_from_deck` never deletes the deck), reachable two ways, neither an error. The backend does not fail it:
#                plain 200, `cards: []` (array, never null), all three counts 0, and no `boards` field on the wire. FORMAT CHECK ON AN
#                EMPTY DECK RETURNS SIX CONFIDENT ROWS — one true `size` violation and FOUR VACUOUS GREENS ("Every card is legal", "No
#                card exceeds the copy limit", "No card is banned") — which is a stronger argument for AC 2 than either artefact makes.
#                AC 3's "100-card Commander deck" DOES NOT EXIST (0 commander decks; all 18 hundred-card decks are `brawl`) but the
#                measurement is RUNNABLE TODAY with zero CDN traffic: `813d0434` "Atraxa Counter Cabinet v2 (owned)", 100 mainboard /
#                99 tiles / 6 DFCs / 0 sideboard, the ONLY 100-card deck 99/99 warm at BOTH sizes (8.47 MiB / 13.10 MiB).
#                BACKEND ON THE CRITICAL PATH IS A NON-EVENT: deck detail 4.8 ms, format-check 5.0 ms (of which the validator itself is
#                0.07 ms), 99 card reads 39.3 ms total. If a browser measures 800 ms, ~99% is transport, queueing and parse — do not go
#                optimising Python. FOUR PREDICATES ARE LIVE AT ONCE (`hasCards` INCLUDES the sideboard per c4-11's review ruling; grid
#                tiles exclude it; curve and pips exclude lands) and `deck.ts:388-390` bans a fifth — the sideboard-only deck is the
#                hardest correctness question in the story.
#                NFR-05 IS OWNED BY EPIC 4 (`epics:745`, "the owner still holds acceptance") while the only gap-closing story, 10.3, is
#                PHASE 2 and carries a near-verbatim twin of AC 3 plus the "closed, or recorded as an accepted deviation" clause c4-12
#                lacks — the acceptance point ships and the repair does not. AC 5 is a VERBATIM DUPLICATE of story 7.4's AC and "blank
#                screen" is defined nowhere; AC 3 mis-cites SC-2, which carries no timing and belongs to Epic 7 (the 07-22 gate logged
#                the loose citation and ruled "no fix required"). SC-5 is Brad's at c8-6 and "cannot be automated or delegated" — this
#                story makes it ANSWERABLE and must not answer it.
#                The conformance sweep found real drift, almost all of it in the artefacts rather than the code (both known precedents —
#                `legality-row` padding and the 2.70:1 pinned ring — are genuinely RESOLVED): `DESIGN.md:141`/`:142`/`:145` still carry
#                off-scale paddings `DESIGN.md:379` bans BY NAME, the c4-10 repair only half-closed and its own text naming these two
#                files; ~60 STALE `DESIGN.md:NNN` ANCHORS across 25 files because `shell.test.ts:1021` validates the STRING and never
#                the line number, one now citing a real but WRONG component; four stylesheets say the token layer is closed at 64 or 65
#                when it is 69, pinned twice; ten stale `ui/README.md` claims; and three artefact-wrong-code-right residues (alt text,
#                price, the "100+" corridor figure). Ruled: land the cheap mechanical corrections, RECORD the ~60 anchors with the
#                guard's shape written down, homed on the C4 retro.
#                NO CDP TOOLING IS COMMITTED — every past eye-check was a throwaway script in a temp dir — and vitest/jsdom CANNOT
#                produce this number (no layout engine), which would be the coverage-that-reads-as-coverage class for a SEVENTH
#                consecutive story. Baseline 1,668 frontend / 64 files, tokens 69, CONTAINERS 24, COPY_MODULES 13 (no count pin), JS
#                224,110 B, CSS 20,316 B, Python 2,501 passed / 1 skipped. Next: dev-story c4-12.)
# previously: 2026-08-07 (Story c4-11 IMPLEMENTED -> review — dev-story. BRANCH PRECONDITION DISCHARGED: PR #49 (c4-10) was still OPEN at
#                activation, so it was merged at 9c9349a and `feat/companion-c4-11-keyboard-floor` was cut from that MERGE COMMIT,
#                per the story's own stop-and-say-so clause. The merge commit's tree is byte-identical to b1a4817
#                (dd38bc8e312bf5b43e2e80438f16ac49bafc0a7b), so the recorded `baseline_commit: b1a4817` stays content-accurate and
#                is preserved. Next: Task 0 — re-run the two carried-from-record baselines, re-verify §A–§D against the shipped
#                database, and rule Q1–Q17 before any code.)
# previously: 2026-08-06 (Story c4-9 CODE-REVIEWED -> done; previously CONTEXTED off 1ed2e83 — the colour distribution panel: one bar, its
#                segments, and a legend, added as the SECOND child of the `AnalysisRow` c4-8 built and left waiting. Three ACs of
#                markup; eight things that are not.
#                HEADLINE, and it is a measurement that changes a real deck on a real screen TODAY: "proportional to pip count"
#                (UX-DR18) resolves TWO ways and nothing anywhere says which. A split/Adventure/Omen cost is `'{3}{B} // {1}{B}'`;
#                counting the WHOLE STRING counts both halves, counting the FRONT FACE counts what you pay for what you cast.
#                Measured on deck ids: 10 OF 40 DECKS CHANGE THEIR BAR and 2 CHANGE THE ORDER OF THEIR SEGMENTS. `Prismatic
#                Dragon` falls 71 -> 45 pips (37% of its bar) and re-orders B>U>G>R>W to U>G~B>R>W; `Abzan Dragons` loses 20% and
#                black drops from 37% of the bar to 30%. The cause is the current-Standard TDM OMEN dragon cycle, not a corner
#                case. Q1 proposes FRONT FACE, because every other surface in the epic already reads the front face and a
#                whole-string bar would be the only one that does not.
#                SECOND: THIS PANEL WALKS BACK INTO THE HYDRATION WINDOW c4-8 WAS THE FIRST IN FOUR TO ESCAPE. `cmc` rode on
#                `CardSummary`; pip count does not — 2,830 of 3,225 faced cards (87.75%) carry a blank top-level `mana_cost`.
#                Hydration is worth +48 pips across 16 OF 40 DECKS, and 34 of the 46 live blank-cost copies are recoverable; the
#                12 that are not are all Pathway MDFC lands that correctly contribute nothing, so on live data hydration is
#                COMPLETE. Consequence: this is the epic's first panel whose PERCENTAGES MOVE AFTER FIRST PAINT, and c4-6's
#                accepted no-re-drive window is cited rather than re-opened. It also forces "no aria-live" from a nicety to a
#                load-bearing rule (Q2).
#                THIRD, and invisible from the ACs: THE OBVIOUS REUSE DOES NOT COMPILE. `DeckList` reads the hydration cache with
#                one `useCardEntry` per DeckRow COMPONENT; this panel is ONE component aggregating 99 rows, so a hook in a loop
#                over a changing list is the rules-of-hooks violation React breaks on. `readCardEntry` is not reactive and would
#                leave the bar permanently stale — plausible and wrong. Q3 proposes ONE subscription to the cache map.
#                FOURTH: THE ACCESSIBILITY SHAPE IS THE EXACT INVERSE OF c4-8's and copying the sibling would be wrong. c4-8 named
#                every bar `role="img"`; UX-DR18 makes the BAR `aria-hidden` and the LEGEND the accessible data path, and the
#                figure's alternative is VISIBLE rather than a hidden table — so the third-instance visually-hidden promotion
#                trigger does not fire, which is stated rather than left as an absence (Q9).
#                FIFTH, MEASURED AGAINST THE SHIPPED HEXES for the first time (the only prior measurement is in a formally
#                SUPERSEDED pre-Voltglass review): ALL 21 ADJACENT `--mana-*` PAIRS ARE UNDER 3:1, 10 of 21 under 1.3:1, worst
#                `--mana-b`/`--mana-colorless` at 1.03:1, best `--mana-w`/`--mana-r` at 2.30:1 — slightly WORSE than the 2.73:1
#                the superseded doc recorded. That is why UX-DR18 hides the bar. But the repair is measured and cheap: EVERY
#                segment clears the `--surface-well` track at >=6.62:1, so a 1px hairline in the track colour turns 21 failures
#                into 21 passes with one declaration and no new token. `DESIGN.md:209` gives `curve-bar` a `segment-hairline`
#                token for exactly this and `color-bar`'s frontmatter has none (Q7).
#                SIXTH: HYBRID AND PHYREXIAN PIPS ARE LIVE, NOT THEORETICAL — 29 hybrid + 7 Phyrexian pip copies across 8 decks,
#                including one `{G/W/P}` that is both at once — and nothing says what a `{W/U}` contributes. The Python that
#                already answers this for the agent, `compute_pip_signals`, counts BARE PIPS ONLY, so hybrids, Phyrexian and
#                generic-hybrids all count for nobody there: a FOURTH "two surfaces of one product answering the same question
#                differently", now across FIVE axes at once (Q4, Q16). `{C}` and `{S}` are ZERO live, so a colourless segment
#                ships untested against real data whatever is ruled.
#                THIS STORY IS THE FIRST `MANA_DATA_INK` JOINER — the allowlist has had exactly one entry since c2-8 and c4-8
#                declined the invitation with a reason. Q8 proposes that `--mana-gold` still does NOT ship (a pip is never gold;
#                `MANA_COLOUR_ORDER` excludes it deliberately), which CORRECTS `ui/README.md:706-710`'s prediction that c4-9
#                would be its first consumer, and the absence is asserted. Q12 proposes DECLINING the numeric mana-value parser
#                c4-8 re-homed here by name — a pip counter never converts a cost to a number — which makes
#                `curve.test.ts:212-236`'s comment ("c4-9's parser flips it") false and owed a correction.
#                TWO CORRECTIONS THIS STORY OWES THE RECORD, both measured. (1) `card_faces IS NOT NULL` MATCHES ALL 38,261 ROWS:
#                the column is NOT NULL and a non-faced card stores the JSON string 'null', so the only correct predicate is
#                `json_type(card_faces)='array'` -> 3,225 — the epic's coverage-that-reads-as-coverage failure, in the
#                MEASUREMENT INSTRUMENT. (2) c4-8's record states a FALSE REASON for a correct ruling: of the 27 live split-cost
#                rows, 8 are Adventures, 18 are OMEN and 1 is neither (`Emeritus of Woe // Demonic Tutor`). The ruling survives —
#                0 live rows have `cmc` != front-face mana value, re-verified — but any guard keyed on `Adventure` would
#                mis-handle 19 of 27.
#                Also inherited BY NAME from c4-8: the narrow-width decision for the first two-child screen, and the land-only
#                empty `.analysis-row` question. `StatChip`'s first surface is TRIGGERED and declined — the composition
#                reference's colour panel ends with a "Deck value" chip, i.e. a PRICE, which c4-7 measured out of existence.
#                No real deck has zero pips (so that AC is asserted against the derivation, never a fixture — the c4-8 High);
#                9 of 40 decks are mono-colour; the smallest real bar is 2 pips in a ONE-CARD deck. Tokens expected to HOLD at
#                69 — `color-bar`'s 14px is off the spacing scale, has a citable `DESIGN.md:212`, and the 2026-07-25 validation
#                report already flags it as over-tokenised. 46 ACs, 16 open questions, 9 inherited deferrals, 8 triggered
#                residues, 19 don't-breaks. Baseline 1,408 frontend / 55 files; Python 2,501 unchanged. Next: dev-story c4-9.)
# previously: 2026-08-06 (Story c4-8 CONTEXTED off 0fdb41b -> ready-for-dev — the mana curve panel: seven bars, a figure and a
#                visually-hidden table, mounted BELOW the grid in the left column. It is the first panel in the epic that is
#                COMPLETE AT FIRST PAINT — `cmc` is on `CardSummary`, so this story fetches nothing, touches the hydration sweep
#                not at all, and is the first in four that can write "c4-6's no-re-drive window: not triggered".
#                HEADLINE, and it inverts c4-7's: "DFCs bucket by their front face" is ALREADY TRUE FOR FREE — for all 2,830 faced
#                cards carrying a blank top-level `mana_cost` (the population that cost c4-7 a whole module and a dependence on the
#                sweep), `cmc` EQUALS the front face's mana value in 2,830 of 2,830 cases, 100%. But for 137 TRUE SPLIT cards
#                Scryfall's `cmc` is the SUM of both halves — `Cramped Vents // Access Maze` buckets at 11 instead of 4 — and ZERO
#                of those are in any of the 40 real decks (all 27 live split-cost rows are Adventures, where cmc is already the
#                front). Correct today BY ACCIDENT, one `add_card_to_deck` from wrong: the `SP//dr` family again. Q2 pins it with a
#                test rather than fixing it, because the fix is a numeric mana-value parser and `ui/` has none.
#                SECOND: THE BUCKET RANGE DROPS CARDS ON THE FLOOR. Buckets are 1..7+, so a 0-mana non-land has no bucket — 1 live
#                row (`Pym Particles`, c4-7's only `Other` row) and 4,351 corpus cards — and unlike `deckGroups.ts` there is NO
#                conservation identity and no number on screen that stops summing. Q1 proposes folding `cmc <= 1` into bucket 1.
#                THIRD: THERE ARE THREE LAND POLICIES AND TWO OF THEM MEET HERE. The ledger homes the A-vs-C disagreement on this
#                story BY NAME (Medium): `mana_curve.py:74` and `:277` test the WHOLE STRING, the frontend tests the FRONT FACE —
#                7 live rows across 5 decks, so `analyze_mana_curve` and this panel will report 1,896 and 1,903 non-land cards for
#                the same corpus. And a third nobody had noticed: `groupOf(t) === 'Land'` — the tidy-looking reuse — is NOT the same
#                test, because first-match-wins files `Artifact Land` under Artifact and would count 25 corpus lands as SPELLS
#                (34 corpus, 0 live, so no fixture would stumble into it). Q3 declines the Python fix with the MCP blast radius as
#                the reason and upgrades the divergence from latent to OBSERVABLE; Q4 writes the front-face test and pins it.
#                FOURTH: WHICH BOARDS IS SPECIFIED NOWHERE, and one half is already ruled in shipped source — `deckGroups.ts:199`
#                says the sideboard "is not part of the deck the curve and colour panels describe". The COMMANDER is mentioned in
#                no artefact and changes the curve in 16 of 39 decks; `deck_analysis.py:171-173` already includes it. Q5 matches it.
#                FIFTH: THIS STORY COMPOSES A ROW FOR A PARTNER THAT HAS NOT SHIPPED — `AppShell.tsx:127` assigns the 1:1 pair to
#                c4-8 by name, so the layout must render ONE child full-width today and TWO at exactly 1:1 the day c4-9 lands, with
#                no dead half-width gutter (the failure c4-7's Q1 rejected in the price column). Q6 proposes a flex primitive.
#                AND THE ONE THAT CHANGES A GUARD: `eslint.config.js:133-137` reserved a custom-property escape hatch for "a
#                computed bar height in c4-8" BY NAME, on the condition that the story CHANGES THE RULE AND SAYS WHY, IN THE OPEN.
#                So Q10 is a guard amendment (narrow, never disable) with `lint-gates.test.ts:133-172` and two fixtures pinning both
#                halves. Also first-in-repo: a `<figure>` and a `<table>` (git grep finds neither in ui/src), taking jsdom's phantom
#                `banner` count from three to four. Q8 proposes NOT stacking by colour, on a measurement: `colors` is `[]` for 2,842
#                of 3,225 faced cards, so 26 live rows / 34 quantity would paint as colourless from a structurally blank field —
#                the c4-6 `oracle_text` / c4-7 `mana_cost` family, a third time. Empty bars are the ORDINARY case (21 of 39 decks
#                have one; `Iron Man, Modern Marvel - reminder` is one card and SIX empty bars) and the scale extreme is 39-vs-0 in
#                one deck. Two doc corrections owed: the ledger still says "84 corpus cards" where c4-7 corrected `deckGroups.ts` to
#                82, and `ui/README.md:1402` says "c6-8's curve axis is next" where it means c4-8. Tokens expected to HOLD at 69 —
#                every `components.curve-bar` value resolves to an existing token. 39 ACs, 13 open questions, 8 inherited deferrals,
#                7 triggered residues, 20 don't-breaks. See the c4-8 block below.
# previously: 2026-08-06 (Story c4-7 reviewed -> done; PR #46 merged to feat/companion-c4 at 0fdb41b. 22 findings -> 1 ruling + 18
#                patches, 2 defers, 3 dismissed. See the story record.)
# previously: 2026-08-06 (Story c4-7 CONTEXTED off d51b467 -> ready-for-dev — the deck list panel, the text half of FR-05 and the
#                first story asked to potentially AMEND an accessibility rule rather than implement one. Three findings no artefact
#                records, all Q-gated. (1) THE PRICE COLUMN HAS NO DATA SOURCE AND NEVER CAN: UX-DR19, DESIGN.md:391 and the epic's
#                own user story ("checking quantities and prices") all demand a right-aligned price with a reserved 64px track — and
#                `PRAGMA table_info(cards)` lists 23 columns with no price, no schema declares one, and the Scryfall importer never
#                reads the `prices` object at all, so the data was NEVER IMPORTED rather than dropped downstream. A Python test
#                asserts the absence on purpose under Brad's c3-2 Q4 ruling. c4-5 closed the identical PROSE ac by absence; Q1 must
#                rule what a COLUMN does. (2) THE FRONT-FACE NAME IS FREE, THE COST IS NOT: 99.0% of the 3,225 faced cards store the
#                combined "A // B" in `name`, so the name half is one split away — while 87.8% carry a BLANK top-level `mana_cost`
#                whose real value sits only in `card_faces[0].mana_cost`, absent from `CardSummary`. 24 of 40 live DFCs, 38 rows. A
#                third shape (Adventure/Omen, `'{1}{B}{B} // {1}{B}{B}'`) needs SPLITTING not hydration. Unlike c4-5's oracle text
#                these pips are FIRST-PAINT content. (3) FIRST PRODUCTION CONSUMER OF `GroupHeader`, which shipped at c2-7 and has
#                had zero consumers since — and whose own header invites this story to home a correction to UX-DR44's h2-inside-h2
#                heading levels if it reads wrong in a real screen reader. The by-design red is NOT the obvious one: `App.test.tsx:494`
#                counts `getAllByRole('listitem')` over the WHOLE DOCUMENT, so every row breaks it and `:510`'s image arithmetic
#                cascades — ruled scope-don't-bump, because bumping preserves the coupling and hands the trap to c4-10. The live
#                "inset rule" cannot be written in a component stylesheet (stylelint admits only `var(--shadow-*)`) so it is a token,
#                68 -> 69, both pins; tokens.css:269 already reserves `Deck-row live tint -> instant (c4-7)` BY NAME. DESIGN.md's
#                `deck-row.columns: '34px 1fr auto 64px'` is not writable verbatim either — a bare `1fr` is banned. Two spec-silent
#                sections land here by name: the commander label (c4-4 Q5) and the sideboard (41 rows / 5 decks, dropped by a test
#                literally titled "c4-7 owns them"), and the conservation identities mean both must land somewhere. Q10 DECLINES the
#                `'Card // Card'` grouping fix c4-6 re-homed here: the DATA blocker is gone but the MECHANISM one is not — `boardsOf`
#                runs once at store-write time and its reference identity is what `deckMemory.ts` reads to detect a deck replacement,
#                so re-deriving after hydration would fire a spurious clear and release the user's pin mid-session, for 2,274 corpus
#                rows and 0 live ones. Row worst cases measured: quantity 34, 33-char front-face name (56 unsplit, 141 in corpus),
#                6-pip cost. 8 of 9 TYPE_GROUPS appear in real decks; `Other` has exactly ONE live row, not the two deckGroups.ts
#                claims — one of two doc corrections owed here. New unguarded gap: the plugin bundle mirror is checked by nothing.
#                44 ACs, 16 open questions, 9 inherited deferrals, 6 triggered residues, 18 don't-breaks. See the c4-7 block below.
# previously: 2026-08-06 (Story c4-6 reviewed -> done; 2 rulings + 10 patches, see the story record.)
# previously: 2026-08-05 (Story c4-6 CONTEXTED off bd72fc0 -> ready-for-dev — the DFC flip control, the epic's own "densest single
#                component" and the UX gate's H2. TWO STRUCTURAL BLOCKERS surfaced that no artefact records, and they are Q1 and Q2:
#                (1) the tile CANNOT KNOW it has a back face — CardSummary carries neither card_faces nor image_uris, and the only
#                hydrator in the app is c4-5's panel on the one card being looked at, so AC 1's "when its tile renders" needs a
#                deck-wide hydration sweep (proposed), a wire field (MCP blast radius), or a control that appears mid-Tab-traverse;
#                (2) the control is a <button> and FOUR shipped comments reserve its home INSIDE the tile's <button> — an interactive
#                descendant the HTML content model bans and React 19 is expected to warn on — while that containment is exactly what
#                keeps mouseenter/mouseleave from reading as leaving the tile. Proposed fix is a positioned wrapper with the pointer
#                handlers moved to it. HEADLINE MEASUREMENT, and it corrects the ledger: 2,778 of 38,261 cards get a control and
#                EVERY ONE HAS EXACTLY TWO IMAGED FACES — deferred-work.md:2032's "a [front, back] destructuring is wrong for three
#                real cards" cites the 3- and 5-face cards, and all three are SPLIT cards (one shared artwork) that get no control:
#                Smelt // Herd // Saw, There // They're // Their, Who // What // When // Where // Why. Second finding: 2,166 of the
#                2,778 are REVERSIBLE ART printings whose back is the same card with a different picture (back type_line literally
#                "Card", blank oracle) — correct behaviour that reads as a bug from a screenshot; 0 in any live deck, so the 612 true
#                transform/MDFC cards are the population Brad sees. Live decks: 42 of 2,027 rows flippable across 20 of 40 decks;
#                Prismatic Dragon renders AC 1 and AC 2 on ONE SCREEN (1 flippable Regent beside 9 Tarkir Omen dragons that must get
#                no control). Backend is COMPLETE — face= ships with resolve_face_images and eleven tests; this is a ui/-only story.
#                Also opened: the first inline <svg> in ui/src with no icon system to inherit, the first 3D transform (tokens.css:274
#                already reserves its reduced-motion row), useCardArt keying on cardId alone so a flip never re-arms the art state,
#                and a back face carrying 1,232 chars that finally makes c4-5's 14-line clamp fire. 32 ACs, 14 open questions, 6
#                inherited deferrals homed by name, 17 don't-breaks. See the c4-6 block below.
#                Previously: 2026-08-05 (Story c4-5 CONTEXTED off b26e8f4 -> ready-for-dev — the persistent card detail panel: transient hover/focus
#                inspection plus click-to-pin, and the first user-interaction state in the codebase. The headline is a MEASUREMENT that
#                corrects the received two-tier story: CardSummary already carries oracle_text, so hydration adds nothing drawable for a
#                single-faced card — while 100% of the 3,225 card_faces cards have a BLANK top-level oracle_text, making hydration the only
#                source of a type line and rules text for exactly that population (6 of 99 in the real deck). Two artefact defects found in
#                this component: the unpin control is required and specified nowhere, and the pinned ring is accent-dim on surface-overlay
#                (2.70:1) — the M4/C3 fix applied to the tile's live ring and never carried across. 41 ACs, 15 open questions, 6 inherited
#                deferrals homed here by name, 16 don't-breaks. See the c4-5 block below for the full record.
#                Previously: 2026-08-03 (Story c4-2 review DONE, status done. Previously: CONTEXTED off 2095050 on feat/companion-c4 — deck state bootstrap and the
#                type-grouped decklist. 32 ACs, 9 open questions, 10 inherited deferrals, 11 named don't-breaks.
#                Headlines: GET /api/active-deck publishes only 200/400/500 and STRUCTURALLY cannot answer the 503
#                the epic's AC describes (it holds no DbSession), so the error ACs are about the deck-detail read
#                alone; PANEL_FOR_REASON.deck_not_found -> 'no-active-deck' has been unreachable dead code since
#                c2-9 and this story is its first live producer — which makes panelFor() on a deck refusal CORRECT
#                in the same codebase where c4-1 AC 13 bans it on a card refusal; the grouping input is type_line
#                alone and the repo already holds TWO disagreeing land policies that differ on four cards in real
#                decks (front-face vs whole-string). Sharpest gate finding: shell.test.ts's git-derived coverage
#                guard makes any NEW src/components/ module a red test by design. Two corrections to earlier
#                records: @testing-library/react IS installed (c4-1's useCardEntry deferral says otherwise), and
#                28 of the 2,027 deck_cards rows are orphaned by DECK id. Baseline 816 frontend / 37 files.
#                Previously — Story c2-10 CONTEXTED off 8c864f8 CONTEXTED off 8c864f8 — the last story of Epic C2: footer attribution as a release condition. 14 landmines, 22 ACs, 5 open questions. Headlines: .app-shell-footer already ships --text-tertiary where AC 3 demands --text-secondary; --type-micro is 10px UPPERCASE by a DERIVED guard, so the legal sentence renders all-caps (Q1); the copy's artefact is DESIGN.md:375, not EXPERIENCE.md (its table has no footer row); and this is the first story to put an external host in the bundle, which fonts.test.ts R4 fails on by design.
#                Previously — Story c2-9 PR #26 MERGED at efa2435 — Greptile 4/5 -> 5/5; the flat decks prop ruled into a type-only discriminated union. Epic C2 is 9 of 10 merged; next c2-10.
#                Previously — Story c2-9 CODE REVIEW -> done — three layers, 2 rulings (AC 21 eye-check → epic checklist as a ruling; COPY_MODULES waivers accepted) + 15 patches applied (--accent exact-match closing the --accent-dim hole, letterless JSX text into the copy-guard content half, the App.test.tsx wiring assertion, states.test.ts pinning the load-bearing falses, live-requires-title in the TYPE, and ten more) + 2 defers to c3-9. Suites 487 -> 502, five gates green, bundle re-measured changed + mirror. Previously — Story c2-9 IMPLEMENTED -> review — the shared state panel and every system-state message. Q1-Q6 all as proposed (8th running). The copy is EXPERIENCE.md, gated byte-for-byte by a test that reads the artefact itself; internal_error and a new stalled-database state WRITTEN INTO the artefact, closing the C1 retro item and the c1-6 ruling. Two new guards, both on the TypeScript AST / as an allowlist. Q1 wired it onto the real screen. 390 -> 487 frontend, Python 1,753 unchanged, tokens 64 -> 65, bundle + mirror MEASURED CHANGED. Ten evasion probes, all ten caught; two real holes found in my own guard by probing it.
#                Previously — Story c2-8 CODE REVIEW -> done — 1 ruling ({2/R} solid-plus-glyph) + 11 patches: the colour-forwarding test, the data-ink guard's MARKUP half, the grow-not-clip source read, {W/W} to unknown, and the records sweep. 383 -> 390 frontend, Python 1,753 unchanged, bundle + mirror byte-identical.
#                Previously — Story c2-8 IMPLEMENTED -> review — ManaPip/ManaCost + the total Scryfall scanner; the --mana-* tokens finally have a consumer AND a gate. 308 -> 383 frontend, Python 1,753 unchanged. Nine evasion probes, all nine caught. Bundle + mirror measured byte-identical.
#                Previously — Story c2-8 CONTEXTED -> ready-for-dev — ManaPip/ManaCost; symbol inventory MEASURED over 32,318 real costs from the shipped DB: NINE families not the epic's four, plus a `//` separator in 338 of them. 19 landmines; DESIGN.md has no components.mana-pip key at all. 25 ACs, 6 open questions. Baseline 308 frontend / 1,753 Python.
#                Previously — Story c2-6 IMPLEMENTED → review — the two-column application shell, the FIRST component in the codebase. Six decide-once conventions set for ~35 later stories. 11 evasion probes, one of which passed and exposed a vacuous guard of my own.
#                Previously — Story c2-5 review PASSED → done — 15 review patches applied, 2 defers; suites 173/1,753.
#                Previously — Story c2-5 implemented → review — self-hosted Space Grotesk; 4 new gate families, 7 evasion probes, 172/1,753.
#                Previously — Story c2-5 → in-progress — PR #21 merged (ff39129), all five open questions answered "as proposed".
#                Previously — Story c2-5 contexted → ready-for-dev — self-hosted Space Grotesk; BLOCKED until PR #21 merges.
#                Previously — Story c2-4 PR #21 green (Greptile 5/5); review patches applied → review — 14/14, three new gate families.
#                Previously — Story c2-4 review → in-progress (14 patch action items); implemented → review; contexted → ready-for-dev.
#                Previously — Story c2-3 code review → done; 14 patches — every regex/list guard had an evasion its own proofs never probed — all applied, proofs re-run)
# project: Artificial-Planeswalker
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts
# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
# KEY NAMESPACES (three epic files feed this one status file):
#   epic-1..3   → _bmad-output/planning-artifacts/epics.md                        (MCP-server pivot, Phase 1)
#   epic-4..7   → _bmad-output/planning-artifacts/epics-deck-power-assessment.md  (renumbered from its raw 1-4)
#   epic-c1..10 → _bmad-output/planning-artifacts/epics-companion-app.md          (c-prefixed, numbering UNCHANGED)
# 08-20-2026 21:30 — STORY 15-6 DONE: THE SC-5 GATE RAN AND CLOSED (sc-5-gate-report-2026-08-20.md). Brad walked the live app, ruled it a deliberate product ("looks good", tabs, footer), then ruled the four open forks interactively: F1 open-modal-!=-surface, arrow-key flag re-accepted at the measured 206/101, M1 row added + M2/M3 accepted + M5 completeness guard extended with a planted RED proof. Inbox dispositioned by name (J6 DECLINED on its fifth home; D1 refused, stays unowned). EPIC 15 IS 6/6 CODE-COMPLETE — the 0.5.0 release cut, withheld by 15-4/15-5 ruling, is UNBLOCKED. Next: the Epic 15 retro, the integration PR to master (no Greptile, standing rule), then the release cut.
#                Previously — 08-20-2026 21:12
# 08-20-2026 21:12 — EPIC 14 VERDICT -> accepted; R3 CLOSED AS WAVED (Brad's rulings, master commits 72dce34 + cfec300): F7's quiet-machine closure was the named verdict-mover; the SC-5 preliminary ruling (sc-5-preliminary-ruling-2026-08-20.md, "it looks good ... we can always adjust after completion") covers the judgement half — story 15-6 cites it instead of staging a gate, and L1-L10 items surface as bugs, not gates. Also this stamp: umbrella reconciliation — claude/hello-7v0acf (15-6 spec) and master merged into feat/companion-epic-15, the canonical Epic 15 umbrella by Brad's ruling.
#                Previously — 08-18-2026 15:27
# 08-18-2026 15:27 — STORY 15-2 FLIPPED TO DONE (PR #85 merged at e424166). The story's own three commits shipped the work but never moved its tracking key, so the status view would have recommended building an already-merged story — the same drift the 08-18 09:47 entry below exists to correct, recurring within hours on the very next story. Root cause is unchanged and still open as E14 R9: sprint_plan.py --epic parses as int, so companion-epic keys are hand-written and drift whenever a story lands without someone remembering. Epic 15 stays in-progress; 15-3..15-6 remain backlog. Next recommended story is 15-3.
#                Previously — 08-18-2026 09:47
# 08-18-2026 09:47 — TRACKING RECONCILED WITH GIT (surgical status fix; sprint_plan.py generate was deliberately NOT run — it rebuilds development_status from the epic files and would have destroyed all 67 inline story comments and dropped the 8 gate keys). Three corrections: 15-1 -> done (PR #84 at f938d03); epic-15 -> in-progress; epic-4 -> done (stale since its 2026-07-12 retro). last_updated's prose is now this comment so the stamp itself parses as %m-%d-%Y %H:%M and the staleness check runs again — it was being skipped, and it is exactly the check that would have caught the 15-1 drift. Pre-edit backup: sprint-status.yaml.bak-2026-08-17, sha256 37d7b6c2db79632458493b015a9a6a3305448cef40680728335910db3ef5ca2d. Untouched by choice: the 8 unrecognized gate keys and the 4 action items with status 'declined' — both are deliberate local extensions, not damage.
#                Previously — 08-17-2026 19:32 (Retro cheap cluster CLOSED and merged to master via PR #83 at e6a7e0e, Greptile 5/5 zero findings. Five items: C7 R1 + C6 R5 (one fix — vitest node-project testTimeout 180s, project-scoped rather than the per-test form the item asked for, reasoning in the config; verified both ways with a 6s probe), C7 R4 (the "update" verb — no such tool, both artefacts amended to the five that exist, citing the derived wiring guard), C7 R5 (live regions three -> four, count re-derived from the tree, both sites now defer to App.test.tsx's census), C6 R10 (the DESIGN.md citation guard declared as the string-proximity check it is). Open action items 12 -> 7. NOTE: the earlier count of 11 was wrong — epic-10 F1 is open and its status line carries a trailing comment that a naive parse skips. Remaining seven all need a decision or a session, not an edit: C7 R3 (manual checklist, fifth carry), R6 (scripts/ mypy, 13 errors in 4 files), R7 (format-check UX ruling), R8 (App.test.tsx split), C6 R4 (pytest marker), C6 R7 (prose sweep, TERMINATION CLAUSE already expired at the C7 retro), epic-10 F1 (story-key gate, premise corrected twice by measurement). Next: Epic 15 — Release Readiness, 6 stories, all backlog.)
#                Previously — 2026-08-16 (Retro R2 EXECUTED: NFR-05 quiet-machine re-measure came back 378/420/517 ms EXIT 0, 5/5 under the 1,000 ms budget — F7 CLOSED via dated addendum in epic-14-retro-2026-08-16.md; ledger item epic-c7-retro-item-2-nfr05-quiet-machine-remeasurement flipped to done via sprint_status.py; no Epic 15 story opened. Residual ~15-20% drift vs c4-12's 311/363/428 recorded, not explained. Evidence: nfr05-quiet-remeasure-2026-08-16.json + spec-epic-14-r2-nfr05-quiet-machine-remeasure.md. Next: R3, the C7 manual checklist, then the integration PR to master.)
#                Previously — 2026-08-15 (c7-6 MERGED via PR #80 at 9f4ed89, Greptile 4/5 → 5/5 — its one round-1 finding was REAL and jsdom-invisible: the focus rescue could park focus behind a still-open agent view when a pointer click on non-focusable dialog content had blurred focus to body; fixed with a third decline guard + a hand-arranged-blur test, plant (d) proof. Next: c7-7, the epic-closing UJ-1 walk. A UX ruling is still owed on the format-check pass→violation announcement, see deferred-work.md.)'
#                Previously — 2026-08-15 (c7-6 IN PROGRESS via bmad-build. Spec spec-c7-6-deletion-and-views-during-refetch.md: three of its five ACs found already shipped (404-clear, non-clickable deck list, view-stays-valid, skip-link withdrawal); the two real gaps were the announcement firing from behind an open agent view and the focus-to-body drop when the deck surface unmounts. Backend needed nothing — delete_deck already emits after the row is gone.)
#                Previously — 2026-08-15 (c7-4 merged via PR #78, Greptile 5/5 with zero inline comments. Next: c7-5.)
#                Previously — 2026-08-14 (c7-3 merged via PR #76, Greptile 5/5. Next: c7-4.)
#                Previously — 2026-08-14 (Story c7-2 MERGED via PR #75 into `feat/companion-c7` at `dc04bd2`; first story built via bmad-build-auto; Greptile 4/5, its one P1 dismissed with a recorded ruling on the inline thread. Next: R9 UX-DR35 ruling, then c7-3.)
#                Previously — 2026-08-13 (Story c7-1 MERGED via PR #74 into `feat/companion-c7` at `72bf5fc`. Three-layer review found 1 patch (notify_deck_changed's timeout default aligned with sibling verbs) + 2 defer, no decisions needed. Next: c7-2.)
#                Previously — 2026-08-13 (c7-1 shared-notifier implemented on feat/companion-c7-1-shared-notifier off umbrella feat/companion-c7; status review, awaiting Brad''s three-layer review + PR.)
#                Previously — 2026-08-13 (c7-1 shared-notifier spec approved, implementation in-progress; epic-c7 lifted from backlog.)
#                Previously — 2026-08-13 (Epic C6 RETRO run and recorded in epic-c6-retro-2026-08-13.md; epic-c6 done, SC-1 + SC-3 closed, 10 action items appended.)
#                Previously — 2026-08-12 (Story c6-9 MERGED via PR #71 into feat/companion-c6 at `d4f908e`. Greptile 5/5, zero findings. Epic C6''s last story — C6 is now feature-complete. Next: the C6 retrospective.)
#                Previously — 2026-08-12 (Story c6-9 CODE-REVIEWED (bmad-code-review) -> done. 0 decision-needed, 1 patch, 5 defer, 12 dismissed. Full detail in the story record — per the C5 retro''s R4, this field is a date plus one clause.)
#                Previously — 2026-08-12 (Story c6-9 IMPLEMENTED (dev-story) -> review. All 5 questions ruled as recommended pre-code. Full detail in the story record — per the C5 retro''s R4, this field is a date plus one clause.)
#                Previously — 2026-08-11 (Story c6-7 IMPLEMENTED -> review: the Suggestions view — the app''s first agent-view rows, and the first surface to hydrate ids no deck ever seeded. All 7 open questions ruled by Brad AS RECOMMENDED before code; Q6 closed the image in-flight-coalescing ledger entry as "not wanted", which is what kept the story frontend-only (Python pinned at 2,907/1/55 and UNMOVED). DESIGN.md was amended FIRST (c4-12 order): components.suggestion-row gained padding/gap/a content-driven height/live-background/live-rule, and components.empty-push-line was added as empty-deck-line''s sibling, closing dw:22 at the story it was homed on by name. SuggestionsView.tsx grew from a half-component into a ul/li of button rows — full-row-height 63:88 thumbnail, category Badge in the neutral tone (Q1: the "action badge" has NO wire backing, and both artefacts were annotated in the same commit), front-face name, mana pips, optional confidence rendered as the wire token, one-line reason — each row carrying the five inspection verbs unrenamed, no tabindex and no onKeyDown. AgentView.tsx/.css were NOT edited, discharging three shipped predictions that said c6-7 would not need to. Item-field validation landed at the row (dw:209 CLOSED at exactly the point the entry names): the prop is typed UntrustedItem with every field remapped to `unknown`, so the typeof gates are compiler-REQUIRED rather than merely present, and a non-string card_id becomes '''' — which the cache already refuses terminally with unknown-card and zero requests, routing a malformed item into AC 4''s existing degradation instead of a new refusal invented at the row. UJ-1 step 6 now exists end to end: a pin set FROM A ROW survives Esc closing the view, with the second Esc releasing it. THREE PLANTS with predictions recorded (18/8/8 red): plant 1 additionally reddened the copy gate (unpredicted — it caught the plant''s hard-coded sentence), and plant 3 left the unknown-row refusal test GREEN because every assertion in it was an ABSENCE that unwired handlers also satisfy — a non-vacuity control was added in the same commit and re-running the plant now reddens it (c6-6''s "ask what the guard is watching" lesson paying out a second time). FOUR guards moved that the story predicted would not, each understood before being touched: keyboard-floor''s hit-box half (the app''s first new focusable since c6-5 — joined WELL_CLEAR with derived geometry), wire-contract''s name ban (the local alias was first spelled SuggestionItem, a shape the backend describes — renamed PushedItem), the px-citation gate (the row''s 1px border), and App.test''s "costs the app NO request" claim (this story deliberately spends one GET /api/cards/{id} per unique id — the test now counts BOOT routes by name and asserts the hydration count positively, sharpened rather than relaxed). One process error recorded: git checkout was used to revert plant 1 while the work was UNSTAGED, restoring the file to its c6-6 state and deleting the component; it was rewritten and re-verified green, and everything was staged before plants 2 and 3 — stage before you plant. Ledger: dw:22 CLOSED, dw:209 CLOSED, dw:45 and dw:49 NOT TRIGGERED (the rows carry neither a tabindex nor an onKeyDown, and the second was heeded by name), image coalescing CLOSED as "not wanted"; two new entries — Q7''s Epic-7 pin-eviction boundary note, and a declaration that this is the app''s first surface whose pixels nobody has seen, naming the four things no guard here can check, homed on the C6 manual checklist (c8-6). Bundle rebuilt after the last edit and sha256-verified identical across all 5 mirrored files. Next: Brad runs the three-layer review and raises the story PR into feat/companion-c6.)'
#                Previously — 2026-08-11 (Story c6-6 MERGED via PR #68 into feat/companion-c6 at `fd7396a`. Three-layer code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 1 decision-needed, 1 patch, 2 defer, 9 dismissed as noise; the decision ruled by Brad (Q7''s fallback title stays in agentView.ts, the c6-8 precedent) and the one patch applied pre-merge (suggestionsViewOf now type-guards payload.title/items instead of only defending their absence, matching Q6''s "malformed payload constructs the empty view" ruling). Suite at merge: frontend 1,995 passed / 73 files (baseline 1,937/71, +58 net), Python unmoved. 2 items remain in deferred-work.md, neither gating. Next: c6-7 (Suggestions view).)
#                Previously — 2026-08-11 (Story c6-6 CODE-REVIEWED -> done: three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) of the push-opens/replaces-in-place diff caught 1 decision-needed, 1 patch, 2 defer, 9 dismissed as noise. The decision-needed item — Q7''s fallback title living in agentView.ts rather than a container copy.ts — was ruled by Brad to stand as the c6-8 precedent (the code''s own rationale: a store cannot import a container''s copy module, and the nav pill needs the same constant). The one patch: suggestionsViewOf claimed to be "Total, by construction" per Q6''s ruling ("absent/malformed payload constructs the empty view"), but only guarded against ABSENT payload fields, not wrong-typed ones — a non-string payload.title would throw a TypeError on .trim() (agentEventOf validates only kind, so this is reachable from a version-skewed frame), and a non-array payload.items would silently corrupt count to undefined; fixed with typeof/Array.isArray guards, agentView.test.ts (23 tests) and tsc -b both clean after. Two items deferred to deferred-work.md: id/ts on the envelope are trusted without validation (a backend violating that required-field contract could make two id-less pushes collide in the shell''s replace-effect dedup, silently skipping the re-announcement — out of Q6''s ruled scope, requires a backend contract violation, not reachable today), and AgentView.test.tsx''s new ARM 3 fixtures hand-roll StatePanel markup instead of importing the component (test-quality only). Nine findings dismissed because they already matched an explicit pre-code ruling verbatim (AC 3 -> c6-7 per Q1/Q2, AC 6 partial per Q3, the aria-live region covering only the heading and not the count per Q3''s three-signal split, the known ungrammatical empty-push copy already documented and deliberately deferred) or had no real consequence (an unsafe test type-cast, a sub-frame entering/replacing race, a magic-number test wait, deferred-work.md prose bloat). Next: Brad raises the PR into feat/companion-c6.)'
#                Previously — 2026-08-11 (Story c6-6 IMPLEMENTED -> review: a push opens its view, and a repeat push replaces it in place — the loop the whole companion feature exists for is closed. All 7 open questions ruled by Brad AS RECOMMENDED before code. socket.ts now delivers `suggestions` WITH ITS PAYLOAD to a new onSuggestions callback (the system kinds carry a discriminant because their payload is never read; this one carries the envelope because the payload IS the content); connection.ts calls openSuggestionsPush and still holds no setState and names no store; agentView.ts grew AgentViewContent to {id, ts, kind, title, count, items}, all schema.ts-typed, plus a TOTAL builder — absent payload, absent items and a blank title all construct a valid view, which Q6 ruled is where the defence lives (agentEventOf stays kind-only). Replace-in-place ships as keyed RE-FIRES on a shell that STAYS MOUNTED: App.tsx deliberately carries no `key` (a remount would replay the 480ms bloom instead of the 240ms crossfade, re-capture the return-focus target while focus sits inside the view, and run the restore cleanup mid-open), and a pushId-keyed effect that skips its mount run re-fires focusHome(heading) plus a one-frame [data-replacing] flip. The crossfade is opacity-only over --motion-glide, so it needs NO new reduced-motion registration and tokens.css:294s inventory line is FULFILLED as written. Q4s announcement ships as a keyed heading Fragment, so a byte-identical title still mutates the live region — the common case, since agents that omit payload.title get the same fallback word twice. New container SuggestionsView renders EXPERIENCE.md:71 byte for byte for an empty push and nothing for a non-empty one (Q1s interim shape, named for the view so c6-7 extends it); new verbatim gate tests/empty-push-copy.test.ts. ARM 3 now prefers .state-panel-headline over the h1 for a DISCONNECTED target only — a connected restore target still wins, which keeps UX-DR46 intact. Suite 1,937/71 -> 1,995/73; Python 2,907/1/55 UNMOVED. Pins moved: four-kinds-drop split at socket AND App level, COPY_MODULES +2 (incl. the FIRST src/state/ entry — the fallback title is authored copy and a store may not import a container), CONTAINERS 29->31; keyboard-floor and the shipped-motion enumeration did NOT move, as predicted. THREE PLANTS, and the third was added mid-run because the second exposed a real hole: removing the replace effect left every live-region test green (the announcement is render-driven, not effect-driven), and a dedicated key-removal plant then showed the App-level AC-6 test could not see a broken announcement either because it used two DIFFERENT titles — a byte-identical-title test was added end to end in response (1,994 -> 1,995). Ripple sweep found TWO STALE PREDICTIONS about this story made by EARLIER ones — clean.css claimed the crossfade would compose two animations (it is a transition), eslint.config.js predicted a grid template through the style attribute (no inline style ships) — both corrected; fifth proof of the ripple lesson and the first where the find was a false prediction rather than a stale tense. Ledger: dialog-accessible-name CLOSED, agentEventOf kind-only PARTIALLY TRIGGERED (item validation stays c6-7s), copy-reading HONOURED and permanently open (the READING is in the Debug Log), plus TWO new artefact-gap residues — the {kind} substitution reads ungrammatically (an empty suggestions / an empty tier_list) and DESIGN.md has no empty-push block. AC 3 -> c6-7 and AC 6s timestamp/pill halves -> c6-8, both structurally per Q2/Q3. Block J still NOT RUN: zero eyes on pixels for the crossfade, the empty-push line or the announcement until the C6 manual checklist. Next: Brads three-layer review, then PR into feat/companion-c6.)'
#                Previously — 2026-08-11 (Story c6-6 STORY-CREATED -> ready-for-dev: a push opens its view, and a repeat push replaces it in place — the story that closes the loop the feature exists for. Context engine findings: the full push pipe already ships except one segment (socket.ts receives `suggestions` and deliberately drops it; agentView.ts has no production writer; the shell''s aria-live heading annotates itself as existing for this story''s replacement). The c6-5 review''s "remount mechanics owed to c6-6" resolves as keyed RE-FIRES, not a remount — a key on <AgentView> would replay the wrong animation (bloom 480ms vs the AC''s glide crossfade 240ms), capture the heading as the restore target mid-open, and run the restore cleanup while the view is still open. Both SuggestionsPayload fields (title, items) are OPTIONAL in the generated types, so defensive construction at the builder is mandatory independent of the malformed-frame case — and agentEventOf validates only kind, making this the first story to read payload fields (dw''s kind-only entry partially triggered here; item-field validation stays c6-7''s). A byte-identical fallback title mutates nothing and announces nothing, so AC 2''s live-region half needs a real DOM mutation (Q4). The composition reference has NO timestamp in the agent-view header (the "ago" grep hits are the letters inside "Dragons"), so AC 6''s timestamp/pill halves belong to c6-8 (Q3). ~30 c6-6 ripple sites enumerated up front. Guard pins predicted to move: socket.test.ts''s four-kinds-dropped pin, App.test.tsx''s AC-11 ignore test, COPY_MODULES, CONTAINERS; keyboard-floor and the shipped-motion enumeration predicted NOT to move (no new listener; the crossfade is opacity-only with a tokenized duration the reduced-motion block zeroes). Baselines: frontend 1,937/71 (must grow), Python 2,907/1/55 (must not move — frontend-only story; the wire shipped at c5-1/c5-5, the tool at c6-4). 7 open questions await Brad''s pre-code ruling (interim body before c6-7''s rows, AC-3 disposal to c6-7, AC-6 disposal to c6-8, replace-announcement mechanics, state-panel focus-restore refinement, payload defence, fallback title copy). Next: Brad rules the 7 questions, then dev-story.)
#                Previously — 2026-08-10 (Story c6-5 MERGED via PR #67 into feat/companion-c6 at `4a70670`. Three-layer code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 2 decision-needed, 2 patch, 4 defer, 5 dismissed as noise; both decisions ruled by Brad and all 4 patches applied pre-merge (scrim mousedown preventDefault() closing a real forward-Tab trap-escape path; agentView.ts''s "replace-in-place works for free" docstring claim corrected — the remount mechanics are still owed to c6-6; close pill''s hover/focus border reverted from a misapplied --accent back to the shipped --accent-dim, which passes 3:1 on surface-panel; scrim-dismiss guard widened to check both the mousedown AND mouseup target, closing the Q3-mirror drag case). 3 new tests cover the fixes non-vacuously. Suite at merge: frontend 1,937 passed / 71 files (baseline 1,871/69, +66 net), Python 2,907 passed / 1 skipped / 55 deselected, unmoved. 5 items remain in deferred-work.md, none gating. Block J (eyes-on-pixels) still NOT RUN — the shell has been reviewed by three adversarial layers and no human eye, backstopped by the C6 manual checklist at c8-6. R2/R5/R1-a/R1-b remain open, not gating. Next: c6-6 (a push opens its view, a repeat push replaces it in place).)'
#                Previously — 2026-08-10 (Story c6-5 CODE-REVIEWED -> done: three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) of the agent view shell caught 2 decision-needed, 2 patch, 4 defer, 5 dismissed as noise. Both decision-needed items ruled by Brad and applied as patches: (1) the focus trap could be escaped — a mousedown on the scrim or any non-interactive panel content blurs focus to <body> by browser default, and the trap''s forward-Tab branch had no catch-all for that state (unlike backward Tab''s); fixed with event.preventDefault() on the scrim''s own mousedown, covering the scrim specifically (a residual gap for non-interactive content INSIDE the panel — kicker/count text, body prose — was filed as a new defer rather than solved with the heavier document-level focusin-recovery pattern); (2) agentView.ts''s docstring claimed replace-in-place worked "for free", but <AgentView> carries no key in App.tsx so React reconciles a content replace as a prop update rather than a remount, meaning the mount-only focus-to-heading and entry-bloom effects would not re-fire on replace — docstring corrected to say the mechanics are still owed to c6-6, no behaviour change. Two more patches applied: the close pill''s hover/focus border had been changed from the shipped --accent-dim to --accent, citing AC 8/UX-DR6 out of scope (that ban is scoped to surface-overlay; the shell sits on surface-panel, where --accent-dim measures 3.05:1 and passes) — reverted to the shipped nav-pill token; and the scrim-dismiss guard checked only the mousedown target, not mouseup, so a drag starting ON the scrim and ending over panel content still dismissed (the exact mirror of the case Q3''s ruling was written to prevent) — fixed by tracking both ends of the gesture symmetrically. 3 new tests added to cover the fixes non-vacuously (the Q3-mirror case, scrim preventDefault, and its non-vacuity counterpart on in-panel content); one existing test updated to fire the now-required mouseup. Re-verified: lint + typecheck + format:check clean, frontend 1,934 -> 1,937 passed / 71 files, Python unmoved at 2,907/1/55, bundle rebuilt a second time and sha256-verified identical across all 5 mirrored files. 4 defer items filed to deferred-work.md (FOCUSABLE_SELECTOR''s tabindex="-1" gap for c6-7''s future rows; the document-capture Esc listener''s stopPropagation() blocking React''s own synthetic event delegation app-wide while a view is open, zero current impact since no onKeyDown exists anywhere in ui/src today; AgentViewContent.title''s missing non-empty guard, c6-6''s concern; the pre-existing copy-module ordinal drift, self-acknowledged in the diff) plus the one residual trap-escape gap from the decision above. Next: Brad raises the PR into feat/companion-c6.)'
#                Previously — 2026-08-10 (Story c6-5 IMPLEMENTED -> review: the agent view shell shipped — the app''s FIRST role="dialog", first aria-modal, first focus trap and first return-focus contract, plus the SEVENTH zustand store. All 7 open questions ruled by Brad as recommended before any code. Frontend 1,871/69 -> 1,934/71 (+63 tests, +2 files); Python unmoved at 2,907/1/55; tokens unmoved at 69; static + plugin rebuilt after the last edit and sha256-verified identical across all 5 files. THE HEADLINE: the Esc-layering test that CardDetail.tsx:99-101, inspection.ts:65-67 and CardDetail.test.tsx:521-524 have all declared untestable since c4-5 ("no overlay exists yet") is now WRITTEN AND GREEN — one Esc, a real pin set by a real tile click, both live document listeners, the view closing and the pin surviving; all three prose sites updated to say so. FIVE guard pins moved with their code, not the four the story predicted — shell.test.ts''s CONTAINERS list was the unpredicted fifth and failed the build until both new modules were registered (27 -> 29). TWO GUARD COLLISIONS RESOLVED RATHER THAN SUPPRESSED, and the first needs Brad''s eye at review: (1) keyboard-floor''s ring guard read the WHOLE rule body for --accent-bright, which made DESIGN.md''s nav-pill spec unshippable (that spec gives focus the same accent-bright TEXT colour as hover) — narrowed to outline/outline-color/box-shadow, which is what the test''s own name, message and Q6 rationale all say ("in every RING"), with a new firing-half test proving the narrowed guard still catches both ring spellings; the revert is small if Brad disagrees. (2) jsx-a11y correctly objected to a div with pointer handlers and role=dialog with a key handler — the trap and both scrim handlers became native listeners on refs rather than suppressions or an invented role. DESIGN CHOICE FORCED BY A GATE: the bloom is a transition out of [data-entering=''true''], not @keyframes, because token-usage''s CSS reader parses a keyframes block as two rules named from/to and the registration it would then demand (from { transform: none !important }) is ignored inside keyframes by spec — an accessibility override that parses cleanly and does nothing. AC 1''s "full-window scrim ... inset 32px" resolved in favour of the shipped slot: the scrim IS the inset box, AgentView.css declares no position and no z-index. Planted red removed the single stopPropagation() from the capture-phase Esc listener and turned 5 tests red across 3 files, collected count validated at 1,934 either side, reverted clean — note the story predicted keyboard-floor would stay GREEN, and it went red because the rewritten guard''s non-vacuity anchor now asserts that call''s presence in source. Ripple sweep found 41 sites against ~28 predicted (fourth time the grep-the-CLAIM lesson has held); CardDetail.tsx:424''s "only DOM id" was an unpredicted site. TWO STORY-CONTEXT CLAIMS FOUND FALSE: --scrim was NOT unconsumed (QuantityBadge.css and FlipControl.css already use it), and the hit-box guard is an enumerated classification rather than a declaration check, so agent-view-close had to join DECLARES_MIN by name. TWO new deferred-work entries: an intermittent vitest worker-fork crash that silently drops a whole test FILE (seen twice, 1,929/1,934 reading as green — did not reproduce in 7 consecutive runs), and the pre-existing contradictory copy-module ordinals in shell.test.ts. dw:4766-4773 annotated FULFILLED with its firing proof; dw:1457-1471 annotated STILL OPEN as review guidance and must be cited in the PR — the composed result has been seen by no human eye, Block J stays NOT RUN until the C6 manual checklist. Next: Brad runs the three-layer review and raises the PR into feat/companion-c6.)'
#                Previously — 2026-08-10 (Story c6-5 STORY-CREATED -> ready-for-dev: agent view shell with focus management and dismissal — the FIRST frontend feature story of Epic C6 and the repo''s first modal. Context engine findings: four epics of shipped code already address c6-5 by name — AppShell''s overlay slot + its guarded CSS (.app-shell-overlay, position:fixed inset var(--space-gutter) z-index 20), the Esc-layering contract written identically in CardDetail.tsx/inspection.ts/EXPERIENCE.md (agent view = document CAPTURE keydown, close + stopPropagation), and tokens.css''s reduced-motion inventory line "(c6-5)". No modal/trap/scrim machinery exists anywhere (focusHome.ts:38-46 disclaims both; --scrim is declared and consumed nowhere) — this story builds the app''s first focus trap and first role="dialog". FOUR shipped guard pins must move in the same commits as the code they guard: keyboard-floor.test.ts''s listener-set + no-capture pins, token-usage.test.ts''s enumerated shipped-motion pin (the 8px rise is a transform — zeroed durations make it instant, not absent), store-writes.test.ts''s STORES table (7th store), copy-rules.test.ts''s COPY_MODULES ("AGENT VIEW" / "Close · esc"). Scope split confirmed against the epic: auto-open/replace = c6-6, rows = c6-7, pills = c6-8, 250ms budget = c6-9. 7 open questions await Brad''s pre-code ruling, incl. three genuine spec holes (scrim-click pointer semantics, focus-restore fallback when the remembered element is gone, the nav-pill spec''s 7px 14px padding sitting on DESIGN.md''s own enumerated drift list = stylelint build failure). Baselines: frontend 1,871/69, Python 2,907/1/55 (must not move). Next: Brad rules the 7 questions, then dev-story.)
#                Previously — 2026-08-10 (Story c6-4 MERGED via PR #66 into feat/companion-c6 at `bf7bda0`. Three-layer code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 0 decision-needed, 4 patches, 4 defer, 4 dismissed as noise; all 4 patches applied pre-PR (docstring/message wording only, no behaviour change — see prior entry for detail). Greptile then reviewed the PR independently: 5/5 confidence, zero findings — the cleanest c6 review yet by Greptile''s own count (unlike c6-2, which it caught a real post-merge gap in). Suite at merge: Python 2,907 passed / 1 skipped / 55 deselected, unmoved by the review; frontend untouched. R2/R5/R1-a/R1-b remain open, not gating. Next: c6-5 (agent view shell with focus management and dismissal).)'
#                Previously — 2026-08-10 (Story c6-4 CODE-REVIEWED -> done: three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) of companion_show_suggestions caught 0 decision-needed, 4 patches, 4 defer, 4 dismissed as noise. All 4 patches applied pre-PR, docstring/message wording only, no behaviour change: the "displayed" docstring/message no longer claims suggestions are "on screen now" (the shipped SPA drops the suggestions kind until c6-5/6/7 land) — reworded to "delivered ... whether that tab currently renders is its own concern"; items_pushed''s docstring no longer says items were "sent" on every status (self-contradicted its own app_not_running clause and ShowSuggestionsResult''s attribute docstring) — reworded to "attempted"; the three non-happy-path push messages (app_not_running/payload_rejected/backend_error) now all state "the content is in chat regardless", matching Task 1''s spec; category''s 80-char cap is now documented in the tool Args alongside reason/title. 4 items deferred to the ledger, all pre-existing patterns shared with set_active_deck (c6-2) or pre-existing test-harness shape, none caused by this story: (1) outcome.clients interpolated into the displayed message with no None-guard, unreachable today, mirrors c6-2''s identical unguarded pattern; (2) no test drives the payload through the real FastMCP call_tool path, so the BaseModel-argument-coercion and cap-enforcement claims are verified only by schema-shape inspection + library-source citation; (3) the "never raises" contract has no test forcing the client stub to raise; (4) the "nothing here sorts, dedupes or trims" claim''s dedupe half is untested. Full suite re-run green (2,907 passed / 1 skipped / 55 deselected, unchanged), ruff + mypy --strict clean, plugin/ rebuilt and mirror-verified. Next: Brad raises the story PR into feat/companion-c6.)'
#                Previously — 2026-08-10 (Story c6-4 IMPLEMENTED -> review: companion_show_suggestions shipped as the FIRST companion push tool. All five open questions ruled as recommended before code (Q1 = the contract model IS the tool argument — the repo''s first BaseModel-typed @mcp.tool() parameter, so FastMCP publishes the payload schema as the agent affordance OQ-2 exists for, and an over-cap call is refused at the argument layer rather than inside the five-token vocabulary). Shape: NO database session, NO deck_not_found/database_not_initialized/error — status is AD-8''s five client tokens verbatim, pinned by set equality against PUSH_OUTCOMES. Result is {status, clients, items_pushed, message}; nothing payload-sourced is interpolated anywhere, so _truncate_for_echo has nothing to bound and the c6-2 Greptile lesson is honoured by testing ALL FIVE branches for sentinel absence and 400-char compactness at the 60-item cap. Planted red scored 3/3 on the payload-passthrough guard, confined to this story''s block. Ripple sweep found 7 live sites against 3 predicted — including docs/plugin-structure.md still claiming 16 tools, stale by five stories, found only because the grep was written against the CLAIM. Two deferred-work entries annotated NOT TRIGGERED (image in-flight coalescing + agentEventOf): both name c6-4 but describe a RENDERED surface, and the epic split made c6-4 the push tool only — the real trigger is c6-7; the coalescing entry''s home is mis-aimed by one story and the re-aim-or-close call is left to Brad. Suite 2,874 -> 2,907 (+33); tool pin 20 -> 21; plugin rebuilt and sha256-verified; frontend untouched. Next: Brad runs the three-layer review and raises the story PR into feat/companion-c6.)'
#                Previously — 2026-08-09 (Story c6-3 MERGED via PR #65 into feat/companion-c6 at `fa5f963`. Three-layer code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 0 decision-needed, 0 patch, 2 defer, 13 dismissed as noise — the cleanest c6 review yet (c6-1 had 4 patches, c6-2 had 3). Acceptance Auditor independently re-verified every code-mechanism claim in the diff and Dev Agent Record against live source and found zero AC violations. 2 items deferred to the ledger, both pre-existing and out of this story''s bounds: (1) the new AC-4 test covers only the deck_not_found refusal reason, not a mid-session refusal with different retry semantics (e.g. database_not_initialized, whose RETRIES_QUIETLY is true, the opposite of no-active-deck''s false); (2) the Q2 none-interlude test omits the request-log sweep Task 1''s switch test applies (the c6-2 Greptile lesson, asymmetrically applied across the story''s own three new tests). No Greptile findings surfaced post-merge (unlike c6-2). Suite at merge: frontend 1,871 passed / 69 files, Python 2,874 / 1 skipped / 55 deselected, both unmoved by the review. R2/R5/R1-a/R1-b remain open, not gating. Next: c6-4 (companion_show_suggestions, the agent''s first push).)'
#                Previously — 2026-08-09 (Story c6-3 CODE-REVIEWED -> done: three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 0 decision-needed, 0 patch, 2 defer, 13 dismissed as noise. Acceptance Auditor independently re-verified every code-mechanism claim in the diff and Dev Agent Record against live source (deck.ts, connection.ts, states.ts, deckGroups.ts, deckMemory.ts, CardTile.tsx) and found zero AC violations. Deferred to the ledger, both pre-existing and out of this story's bounds: (1) the new AC-4 test covers only the deck_not_found refusal reason, not a mid-session refusal with different retry semantics (e.g. database_not_initialized, whose RETRIES_QUIETLY is true, the opposite of no-active-deck's false); (2) the Q2 none-interlude test omits the request-log sweep Task 1's switch test applies (the c6-2 Greptile lesson, asymmetrically applied across the story's own three new tests). No code changed by review; suite counts unmoved from IMPLEMENTED. R2/R5/R1-a/R1-b remain open, not gating. Next: Brad raises the PR into feat/companion-c6.)
#                Previously — 2026-08-09 (Story c6-3 IMPLEMENTED -> review: the glass following the agent''s choice is now PROVEN rather than merely shipped. All 5 open questions ruled as recommended before code, and Q1 held: a TESTS-ONLY diff — ui/src/App.test.tsx is the only code file (+288 lines), so src/companion/app/static/ and plugin/** were not rebuilt. Three new App-level tests close 4 of the 5 recorded gaps: the deck A -> deck B switch with the previous deck''s pin released onto deck B''s own cold-open card, the 404-after-switch walked end to end from a mounted App, and the none-interlude stale pin documented as self-healing (Q2 accept-and-pin, not fixed). AC 3 is asserted as a request log — one /api/active-deck read, one /api/deck/{B}, ZERO /api/deck/{A} — counted by exact path equality so the legitimate format-check call cannot make it flaky, and c6-2''s Greptile lesson applied by sweeping EVERY request since the envelope for the old deck''s id rather than only the path the AC names. Found and fixed in-story: the file''s beforeEach resets four stores but misses the inspection slice and deckMemory''s lastBoards, so a nested beforeEach was scoped to the new block rather than widening the shared one. Deck B is a VERIFIED REAL second deck (Arabella Mobilize (Boros) v2 - owned) — the file had only ever had one, which is why the switch was unassertable. Task 5''s plant (onSystemEvent -> no-op, dw:3756''s regression verbatim) reddened 7 tests (all 3 new + the 4 shipped socket-event tests) with the collected count validated at 1,871 first, then reverted clean. Q4 answered from deck.ts:515-519 rather than a test: redriveDeckBoot() writes no store state, so the old deck holds until B lands atomically — no booting interim at any latency; recorded, nothing changed (c7-4 keeps teardown rules). Frontend 1,868 -> 1,871 passed / 69 files; Python 2,874 / 1 skipped / 55 deselected unmoved; lint + tsc -b + format:check clean; governance suites 348 passed untouched. Baseline''s first frontend run was the known lintBothFixtures cold-start flake — SECOND sighting, documented not hidden. Ledger: agentEventOf annotated not-triggered and left OPEN for c6-4+; dw:3756 gained a firing-proof line. AC 5 satisfied by record (c5-4 fan-out + per-client pins + spine cross-tab residual); the two-tab observation already lives in retro J2 carried by R11, so no checklist line was needed. R2/R5/R1-a/R1-b remain open, not gating. Next: Brad runs the three-layer review and raises the PR.)
#                Previously — 2026-08-09 (Story c6-2 MERGED via PR #64 into feat/companion-c6 at `dc3ba5d`. Code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 0 decision-needed, 3 patches, 1 defer, 6 dismissed; all 3 patches applied pre-merge (bounded compactness, session-close-before-HTTP, stale docstrings). Greptile's PR review then caught a real gap the first compactness patch left open: it only truncated the echoed deck_id on the deck_not_found branch — database_not_initialized/error still echoed the raw id, and the found-deck branches echoed deck.name (unbounded in storage; create_deck only refuses blank) into both deck_name and the displayed message. Fixed in-branch (commit c974e28): all four branches now route through one _truncate_for_echo() helper; _ECHO_LIMIT tightened 100 -> 48 after measuring the worst case (database_not_initialized pairs one echo with the 225-char shared DATABASE_NOT_INITIALIZED_MESSAGE, which blew the file's 400-char test convention at 100). Suite 2,810 -> 2,874 passed (+64) at merge, ruff + mypy --strict clean, plugin/ rebuilt and sha256-verified. R2/R5/R1-a/R1-b remain open, not gating. Next: c6-3 (the glass follows the choice).)
#                Previously — 2026-08-09 (Story c6-2 CODE-REVIEWED -> done: three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 0 decision-needed, 3 patches, 1 defer, 6 dismissed as noise. Patches applied: bounded the deck_not_found result''s echoed deck_id (new _DECK_ID_ECHO_LIMIT = 100, was unbounded and could blow the CM-1/AC-5 ~200-token budget on an oversized id — cross-confirmed independently by all three review layers); closed the DB session before the outbound HTTP call in companion_set_active_deck (the first companion tool whose helper does I/O other than the database, previously held a pooled connection open across up to 10s of network time); refreshed client.py''s module and _send docstrings, stale since they still described push_event as the module''s sole public verb. Deferred: deck_id not .strip()d before the DB lookup (pre-existing inconsistency across the MCP tool suite; this story correctly matched its cited skeleton, view_deck.py, which also does not strip). Suite 2,870 -> 2,871 passed (+1, the new compactness test), ruff + mypy --strict clean, plugin/ rebuilt and verified byte-identical.)
#                Previously — 2026-08-09 (Story c6-2 IMPLEMENTED -> review: the first companion MCP tool ships, and the PUT wire gained a client count.)
#                Previously — 2026-08-09 (Story c6-2 context CREATED -> ready-for-dev. Story file written by create-story: the tool reuses c6-1's generic _send core for PUT /api/active-deck (dw ledger need 2); deck_not_found is layered at the TOOL level above the client's closed five (dw need 1, AD-16); the tool validates deck existence itself via DeckRepository.get_deck and never contacts the backend for a missing deck; no active-deck state in the MCP package (CM-3); the exact-set tool pin in test_build_plugin goes 19 -> 20. One contract gap surfaced for pre-code ruling: epic AC 2 wants displayed + client count, but the shipped PUT answers 200 ActiveDeck {deck_id} with NO count — broadcast_active_deck_changed already returns the count as int (ws.py:424) and the route discards it; recommendation is an ActiveDeckSetReceipt PUT response model + regenerated committed OpenAPI/types artifacts, GET untouched. Also flagged: the client-test stub has no do_PUT (a PUT meets a stdlib 501), and _outcome_for/_attempt are push-hard-wired (200 parses as EventIngestReceipt) so the verb needs sibling helpers. 5 open questions await Brad's pre-code ruling. Next: rule the questions, then dev-story c6-2.)
#                Previously — Story c6-1 MERGED via PR #63 into feat/companion-c6 at `059ecdd`. Three-layer adversarial review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) caught 13 unique findings after dedup (16 raw), 0 high/medium: 4 patches applied (trimmed forward-looking c6-2 prose out of PushOutcomeToken/_send''s docstrings per Task 7''s R2 rule, c6-2''s actual needs moved to a dw: ledger line; fixed push_event''s docstring overclaiming \"never raises... ever\" against its own MemoryError test; corrected this file''s own wrong +41-passed count to +40; made the new _StubServer test lock actually guard the request log its comment claimed); 3 low-severity coverage gaps deferred (401-vs-403 boundary, narrower probe-to-send race window, EventIngestReceipt extra=\"forbid\" — all pre-existing/out of scope); 6 dismissed as noise, including a verified-false headline claim that _PUSH_TOTAL_SECONDS=10.0 could let a call run ~20s (the outer asyncio.timeout in push_event actually hard-caps the whole two-attempt call). Suite 2,770 -> 2,810 passed (+40, 1 skipped, 55 deselected, 2,811 collected) at merge, ruff + mypy --strict clean, plugin/ rebuilt and sha256-verified. All five pre-code questions RULED AS RECOMMENDED by Brad before code. R2/R5/R1-a/R1-b remain open, not gating. Next: c6-2.)'
#                Previously — Story c6-1 CODE-REVIEWED -> done on feat/companion-c6-1-leaf-client, awaiting PR merge.
#                Previously — EPIC C6 IN PROGRESS. Story c6-1 IMPLEMENTED -> review. push_event/PushOutcome/_send landed INTO the existing src/companion/client.py; suite 2,770 -> 2,810 passed (+40, 1 skipped, 55 deselected, 2,811 collected), ruff + mypy --strict clean, plugin/ rebuilt and sha256-verified after the last edit. All five pre-code questions RULED AS RECOMMENDED by Brad. Planted red fired on the retry-token guard through the FULL suite (3 red, all three retry guards, 2,811 collected) and was reverted. R2/R5/R1-a/R1-b remain open, not gating.
#                Previously — C6 PREP UNDERWAY. R1 Windows CI lane SHIPPED TO MASTER via PR #62 at `4cf4bd6` — green first run, 1 passed in 7.20s cold; two carried follow-ups opened as R1-a firing proof and R1-b branch protection. R4's premise corrected in place — the flake was never integration-marked. Umbrella `feat/companion-c6` cut off master at `37eb1bd`. R3 subsequently DECLINED by Brad.
#                Previously — C5 RETRO run and recorded in epic-c5-retro-2026-08-09.md (Block J later ruled not-run and carried to the C6 checklist; R9 ruled deliberate-split).
#                Previously — Story c5-8 MERGED via PR #60 at `057a6d9`; Epic 5 complete (c5-1..c5-8 done); next: the C5 retro.
#                Previously — Story c4-1 DONE — three-layer review applied 10 patches (1 High: a summary seeded mid-flight was clobbered by a settling refusal), ledgered 2 deferrals, dismissed 6; 808 -> 816 frontend, all gates green, bundle byte-identical after patches (the whole readCard path is tree-shaken until c4-2 imports it). Next: PR into feat/companion-c4, then c4-2.
#                Previously — Story c4-1 IMPLEMENTED -> review — the one card cache, the in-flight deduping and the card read. All 7 open questions AS PROPOSED (ninth story running), two of them strengthened by measurement rather than merely confirmed. THE VISIBLE CHANGE IS Q1s RENAME: src/api/decks.ts -> src/api/client.ts, with posture.test.ts:328s exhaustive door list, its comment and ui/README.md moved in the SAME COMMIT as the first card fetch — the guards property is "one door, named exhaustively", not the filename, and a second src/api/cards.ts would have failed a green test by design. The cache is a SECOND create() and still ONE cache: useSystemState subscribes with no selector, so folding it into useSystemStore would re-render the whole app on every tiles hydration (99 whole-app renders on the very sweep the story exists to make cheap). AC 12s bound is a CUMULATIVE PER-ID ATTEMPT COUNT (MAX_ATTEMPTS_PER_CARD = 3), not a retry loop — readCard has no timer and no backoff at all — which made Q6s damping question moot and re-homed it to c5-6. Q5 ruled YES: a 400 invalid_request on a CARD read draws the unknown-card placeholder, closed on BOTH fix shapes (the placeholder map AND encodeURIComponent on the id). Nine evasion probes, NINE CAUGHT, none passed; probe (j) — deleting a token from the total retryability map — failed in tsc alone while npm test stayed green, and that same asymmetry bit for real during implementation, which is deferral 1 arriving from an unexpected direction and is why npx tsc -b --force is in AC 27. 731 -> 808 frontend (36 -> 37 files); Python 2,447 passed / 1 skipped / 54 deselected UNCHANGED, as expected for a story that adds no Python. Bundle + mirror MEASURED CHANGED (JS 198.56 -> 198.65 kB; CSS BYTE-IDENTICAL, which is AC 16s confirmation that no pixels shipped). Nine inherited deferrals: 3 resolved, 5 re-homed by name (c4-4 x2, c5-6, C4 retro x2), 1 not-triggered-and-re-homed; two further shared-home entries checked and annotated. Next: three-layer code review, then PR into feat/companion-c4.
#                Previously — EPIC C4 OPEN — feat/companion-c4 cut off master at 61a787a; story c4-1 CONTEXTED -> ready-for-dev. The headline finding contradicts the story title slightly and changes the design: GET /api/deck/{id} ALREADY embeds a CardSummary per card (deck.py:275-281), so the hydration cache is TWO-TIER and its bulk tier arrives free in the one request c4-2 already makes — measured on the largest real deck (99 tiles): 38,182 bytes / 1 request for the summaries vs 212,436 bytes / 99 requests for the full Card rows, a 5.6x ratio. EXPERIENCE.md:86 states that contract in one sentence ("name and cost are known at hover time... the rest fills in place") and it had never been connected to this story. Second landmine: posture.test.ts:318 asserts the network-door list EXHAUSTIVELY as [src/api/decks.ts], so a new src/api/cards.ts is a RED TEST by design — Q1 rules it. Third: per-card routes are NOT retry-safe (c3-2 measured 503-outranks-400), which c3-9 wrote into three files expecting this story to walk into it. 28 ACs, 7 open questions, 9 inherited deferrals. Baseline 2,447 Python (1 skipped, 143s) / 731 frontend (36 files).
#                Previously — EPIC C3 SHIPPED TO MASTER — integration PR #39 at eb3f20a, after the Scryfall hotfix #38 landed first by ruling. Per R4 the narrative lives in epic-c3-retro-2026-08-02.md.)'

## Per-story journals with no story file

### epic-10

9/9 2026-08-02 and SHIPPED TO MASTER — integration PR #39 merged at eb3f20a (105 files, +39,814/-648, 54 commits), no Greptile per the standing rule. Closes SC-4, satisfies CM-2. R1's Scryfall hotfix (#38, 7631147) landed on master FIRST by ruling, so C3 merged onto a product that can obtain data. Merged master re-verified green: 2,447 Python (not integration) / 731 frontend. Merge != release — no tag, no CHANGELOG until c8-4

### scryfall-jsonl-migration

PR #38 MERGED to master 2026-08-02 at 7631147 — Greptile 5/5 at ROUND 1 with ZERO inline findings. stream_cards now decides the payload shape by READING THE BYTES (gzip magic number, then `[` vs JSONL) rather than trusting a suffix — c3-7's D1 ruling applied to the importer, which is why every existing array fixture kept working unedited; _resolve_download_uri accepts either key spelling and names the keys that ACTUALLY ARRIVED when neither is present (the bare KeyError is what made the live break undiagnosable). Probed: reverting to the pre-break ("download_uri",) reds the canary 5 ways against the live API. Verified end to end: 38,485 cards / 0 errors from scryfall_oracle_cards.jsonl.gz, 53 game_changer, 0 dup oracle_ids. Suite 1,753 -> 1,773. The canary reads the real endpoint THROUGH the production resolver and runs from a new WEEKLY scheduled workflow, because CI is -m "not integration" and a vendor's contract changes on the vendor's schedule, not on ours

### epic-11

SHIPPED TO MASTER 2026-08-07 via integration PR #52 at 1de85ce (no Greptile — it declines PRs over 100 files and this was 155). 12/12 stories, PRs #40-#51. Retro, Block I (all four state panels rendered by a real engine at last) and scripts/cdp_harness.py all included. Master re-verified green after the merge: 1,694 frontend / 65 files, 2,501 Python / 1 skipped. MERGE != RELEASE — no tag, no CHANGELOG until c8-4. Next: cut feat/companion-c5 off master, context c5-1 (the first story under R1 trigger-gated inheritance).

### epic-11-retrospective

2026-08-07: epic-c4-retro-2026-08-07.md. 6 rulings + 1 standing agreement ratified (live-contract canary); 13 action items, 2 executed in-retro. Headline: the verification-to-production ratio FELL every epic (2.16 -> 1.92 -> 1.33) while per-story Dev Notes tripled (15 KB -> 41 KB) — the growth is inherited context and record, not tests. Greptile round-1 clean 33% -> 75%.

### epic-13

9/9 2026-08-12; PRs #63-#71 into feat/companion-c6; retro run 2026-08-13 (epic-c6-retro-2026-08-13.md); SC-1 + SC-3 CLOSED (250 ms budget observed at 15/21/36 ms warm). INTEGRATION PR #72 MERGED at c3ac8be (2026-08-13, no Greptile per standing rule) — Epic C6 is on master. Merge != release until c8-4

### 14-2-every-deck-mutation-tool-emits-after-its-transaction-commits

PR #75 MERGED 2026-08-14 at dc04bd2. bmad-build-auto run: 4-layer review, 3 patches (emit-placement guard, resolver hardening, "update"-verb reconciliation note), 17 rejects, 0 gaps. Greptile 4/5 — one P1 (commit-then-refresh-fails suppresses the emit) dismissed with recorded ruling (r3782233939). Spec: spec-c7-2-mutation-tools-emit-after-commit.md (followup_review_recommended: true).

### 14-3-the-glass-refetches-on-deck-changed-coalesced-and-latest-wins

PR #76 MERGED 2026-08-14 at 1019955. bmad-build-auto run: 4-layer review, 10 patches (1 medium: unasserted caller-signal abort wiring; 9 low), 8 rejects, 0 gaps. Greptile 5/5, nothing dismissed. First story to run firing proofs via vitest_probe_harness (R2 loose end closed); green-certified at 2150 tests. New policy: non-404 refetch refusal drops (deck stays; UX-DR35). Format check re-keyed on detail identity (c4-10 Q7 overturn). Spec: spec-c7-3-glass-refetches-coalesced-latest-wins.md (followup_review_recommended: true).

### 14-4-refetch-never-tears-down-whats-on-screen

PR #78 MERGED 2026-08-15 at 0bfaf57. bmad-build-auto run: 4-layer review, 8 patches (1 medium: inDeckList's commander arm was deletable with the suite green; 7 low), 12 rejects, 0 gaps. Greptile 5/5, ZERO inline comments (second consecutive clean sheet). Ships the updating flag + header veil/"Updating…" reduced-motion swap and the R9 membership-transition pin eviction (same-deck refetch pin release fixed; pinned suggestion survives — c6-7 debt paid, deferred-work ledger entry CLOSED; DFC back-face e2e test). Green at 2183 frontend / 3020 backend. Flagged for review: R9 rule also governs deck switches (entailed, not AC-legislated); veil + reduced-motion swap are CSS-source-asserted → epic manual checklist. Spec: spec-c7-4-refetch-never-tears-down-whats-on-screen.md (followup_review_recommended: true).

### 14-5-the-change-is-announced-once-and-motion-is-never-the-only-signal

PR #79 MERGED 2026-08-15 at dac0bdc. bmad-build-auto run: 4-layer review, 8 patches (1 medium: the glow was never verified through the real refetch path / tile-instance persistence unpinned; 7 low), 14 rejects, 0 gaps. Greptile 5/5, ZERO inline comments (third consecutive clean sheet). Ships `refetchSettles` (success-arm only, so cold boot / deck switch / reconnect re-drive / 404-clear / drops are silent by construction) driving a props-free `DeckAnnouncer` — one polite region, "Deck updated — {N} card(s)", N = mainboard + sideboard, keyed-Fragment re-announce on identical text, empties when its deck leaves the glass; plus UX-DR16's one-rAF quantity glow (no keyframes, no transform, no new token) omitted entirely under reduced motion. Live-region censuses 2→3. c6-3's announce-on-switch question resolved structurally: switches are silent. Green at 2228 frontend / 3020 backend. Flagged for review: count semantics (main+side) and the singular "1 card" are extensions beyond the epic's worked example; announcement currently fires behind an open agent view (test written RED-ready for c7-6's suppression AC); glow look + reduced-motion omission are CSS-source-asserted → epic manual checklist. Two deferrals: format-check pass→violation announcement NEEDS A UX RULING before any story can home it (deferred-work.md:4782); Panel `live` animation re-homed to its first producer (zero producers today). Spec: spec-c7-5-announced-once-and-motion-never-the-only-signal.md (followup_review_recommended: true).

### 14-6-deck-deletion-and-agent-views-during-a-refetch

PR #80 MERGED 2026-08-15 at 9f4ed89. First c7 story via plain bmad-build (Brad-run four-layer review): 6 patches (all low, incl. 2 new firing-proven AC-3 coverage tests), 1 defer (panel→deck mirror focus drop, ledgered — same class as the half this story closed, at the opposite edge), 15 dismissed; verification-gap found NO gap. Ships the DeckAnnouncer modal gate (`&& !viewOpen` off a new primitive useAgentViewIsOpen; drop-not-defer, seen still advances) and the App-level surface-transition focus rescue (deck→panel, focusHome to the panel headline, THREE decline guards). Greptile 4/5 → 5/5: its one round-1 finding was REAL and jsdom-invisible — a pointer click on non-focusable dialog content blurs focus to body, and the rescue would have parked focus BEHIND the still-open view; fixed with the view-open decline guard + a hand-arranged-blur test (plant d). Green at 2256 frontend / 3020 backend. Flagged for the epic manual checklist: real screen-reader silence behind an open view; the real focus landing after a deletion (both jsdom-unprovable). Seventh sighting of R5's cold-eslint timeout recorded during review verification. Spec: spec-c7-6-deletion-and-views-during-refetch.md (followup_review_recommended: true).

### 14-7-the-loop-closes-uj-1-end-to-end

PR #81 MERGED 2026-08-16 at d6502db. bmad-build-auto run, and the epic's closer. Planning found FIVE gaps rather than a victory lap, the headline being that NO TEST ANYWHERE had observed a `deck_changed` originating in a mutation tool arriving at a socket (the wiring test had tool+notifier but no companion; the live-backend test had process+socket but hand-built its envelope). Ships: PHASE 8 inside the ONE real-process test (AD-10 stays one test/one function — `ci.yml` is PATH-scoped, so a second FILE would have passed the gate while breaking the architecture; the phase goes THROUGH src.companion.client on purpose and says so, while every hand-rolled phase stays hand-rolled); the UJ-1 Flow 1 walk in one sitting with "Brad never touched the app" as a capture-phase-recorded ASSERTION, the pin taken being a suggestion never in the deck (the R9 membership arm nothing had walked end to end); `ui/tests/read-only-glass.test.ts` (SIX rules — the sixth, `.send(`, added at review because the WebSocket write channel carries no verb, no init and no fetch); a real loopback 500 + drip-fed wedged listener at the tool level; the accepted staleness window written into the two sites that swallow (until FR-16); and `cdp_harness refetch` + `tests/unit/test_cdp_harness.py`. MEASUREMENTS, all three Flow 1 clocks on ONE copied data dir: commit->repaint 221/240/243 ms vs 1,000 (SC-2 CLOSED); bloom 17/19/37 ms vs 250; cold open 420/547/**1076** ms — 2 of 5 OVER NFR-05 (re-run 420/512/754), recorded rather than re-run away and DEFERRED for a quiet-machine re-measurement. 4-layer review: 0 gaps, 0 bad_spec, 19 patches (6 medium), 3 deferred, 12 rejects. THREE patches were real defects: ui/index.html sat OUTSIDE the read-only-glass sweep while rule 4 (`<form`, needs no JS to submit) exists precisely for it — a planted form passed; the refetch data-dir gate let `<real>/copy` through AND let an exported PLANESWALKER_DATA_DIR disarm the platform-default check; and the measurement never re-checked the repaint was push-driven, so a dropped socket would have been published as an SC-2 figure for the reconnect path. The step-03 MATRIX AUDIT earned its keep for the first time: it caught that cdp_harness.py had NO tests at all, so the refusal branch its own docstring calls trap 1 had never fired. Greptile 4/5 -> fixed: its one P1 was REAL and the same one-branch-of-the-pattern class as c6-2 — the review had already ordered the undo made failure-safe and the loop was fixed while the PRIMER's stayed bare; verified end to end (152/157/162 ms, copy read back at 99 rows / 0 for the added card). Two shipped claims amended in place: test_live_backend.py's "does not go through src.companion.client" now carries a stated exception, and test_deck_changed_wiring.py's "nothing here opens a socket" was FALSE and was corrected. Green at 2305 frontend / 3049 backend collected; bundle byte-identical, zero drift. Recorded for the retro: `mypy scripts/` is outside pre-commit's ^src/ scope and reports two pre-existing errors nobody sees. Spec: spec-c7-7-the-loop-closes-uj-1-end-to-end.md (followup_review_recommended: true, 3 deferred items in frontmatter).

### epic-14-retrospective

run 2026-08-16; epic-14-retro-2026-08-16.md; VERDICT accepted-with-open-items (criteria DECLARED); 9 action items (R1-R9) appended below; 10-item manual checklist (L1-L10). SC-2 closed on 221/240/243 ms observed. The one criterion still reading over budget is NFR-05 cold open (2 of 5 runs over 1,000 ms) — R2 is the quiet-machine re-measurement that closes it. C6 R5 fired an EIGHTH time live during the retro run. NOTE: sprint_status.py cannot write this key — --epic is int-only and resolves the numeric epic 7 — so this entry was hand-written and the file re-parsed to verify (see R9).

### epic-15

Release Readiness 6/6 CODE-COMPLETE 2026-08-20 - 15-1..15-5 merged (PRs #84, #85, #87, #88, #89), 15-6 the SC-5 gate RUN AND CLOSED (see its key). Remains: retro, integration PR to master, the 0.5.0 release cut (now unblocked). Branch feat/companion-epic-15 is 30 commits ahead of master. 15-5 closed the inherited deferral (the documented launch command did not work for the plugin install route; the anchored form is now documented for both clients and test-derived). Next: 15-6 the SC-5 gate — Brad only, cannot be automated or delegated, and it is what gates the version cut that 15-4 and 15-5 both deliberately did not take.

### epic-16

3/3 CODE-COMPLETE 2026-08-21 on feat/companion-epic-16 (branched from b3798a5, the epic-15 integration merge). Remains: retro, integration PR to master, then Epic 17 and the 0.5.0 cut. Statuses were stale (backlog) until the 2026-08-21 retro pre-check reconciled them against the merged PRs.

